# Tasks: Stakeholder Panel UI

## Derived From
.ai-context/plans/stakeholder-panel-ui.plan.md

## Sequence
- [ ] stakeholder-panel-ui.T01 — Login screen: form, submit handler, invalid-credential error display, calling `rbac-api-security.API02` and the already-built shared `src/shared/auth/` module's session-write function (built by `admin-panel-ui.T01`, not rebuilt here) — Acceptance: stakeholder-panel-ui.AC1, stakeholder-panel-ui.AC2. **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-20).
- [ ] stakeholder-panel-ui.T02 — `StakeholderPanelLayout`: route guard (no-session redirect) + Logout control, both consuming the shared `src/shared/auth/` module — Acceptance: stakeholder-panel-ui.AC10, stakeholder-panel-ui.AC11. **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-20).
- [ ] stakeholder-panel-ui.T03 — "My Requests" / "Pending Actions" list screen (`useMyRequests` over `internal-transfer-workflow.API10`, role-filtered) — Acceptance: stakeholder-panel-ui.AC2c. **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-20) — also fixed a real route collision with `admin-panel-ui.T05`, see `stakeholder-panel-ui.plan.md`.
- [ ] stakeholder-panel-ui.T04 — Submit Request screen: Employee-only Formik/Yup form over `internal-transfer-workflow.API01` — Acceptance: stakeholder-panel-ui.AC3. **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-20) — required `org-structure-management.T03` as a prerequisite fix.
- [ ] stakeholder-panel-ui.T05 — Adaptive request detail screen: Employee own-status view (`API02`) + Withdraw action (`API09`), at `/my-requests/[id]` (corrected from the originally-planned `/transfer-requests/[id]`) — Acceptance: stakeholder-panel-ui.AC4, stakeholder-panel-ui.AC5. **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-20).
- [x] stakeholder-panel-ui.T06 — Manager Decision panel: approve/reject over `internal-transfer-workflow.API03`, Yup-enforced required reason on reject — Acceptance: stakeholder-panel-ui.AC6. Required a prerequisite fix (`internal-transfer-workflow.T11`, Merged 2026-09-20) — `RequestDetail`'s shared base 403'd for any Manager before this task could render at all; see `status.md`. **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-20).
- [x] stakeholder-panel-ui.T07 — HR Decision panel: approve/reject over `internal-transfer-workflow.API04`, same required-reason pattern — Acceptance: stakeholder-panel-ui.AC7. Required prerequisite fixes (`internal-transfer-workflow.T12`, `user-management-console.T07`, both Merged) — the earlier `T07`–`T11` blocker audit already cleared this before the task started. **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-20).
- [x] stakeholder-panel-ui.T08 — Payroll task-completion form over `internal-transfer-workflow.API05` (including "No Action Needed") — Acceptance: stakeholder-panel-ui.AC8. No new blocker found — `API02` access already covered by `internal-transfer-workflow.T12`; confirmed `API05` itself performs no payload validation server-side, by design (already documented at `internal-transfer-workflow.T06`'s Gate 2), so this task's client-side Yup validation exists for real user experience, not to satisfy a server-side contract. **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-20).
- [x] stakeholder-panel-ui.T09 — IT task-completion form over `internal-transfer-workflow.API06` — Acceptance: stakeholder-panel-ui.AC8. No new blocker found — `API02` access already covered by `internal-transfer-workflow.T12`; confirmed `API06`, like `API05`, performs no server-side payload validation at all. First form in the project with array-typed fields (`systemsAccess`/`permissions`/`devices`) — no existing multi-value input pattern to reuse, so each is a comma-separated text input split into a trimmed array on submit, kept minimal rather than introducing a new repeatable-field UI component for a first, single-use case. **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-20).
- [x] stakeholder-panel-ui.T10 — Facilities task-completion form over `internal-transfer-workflow.API07` — Acceptance: stakeholder-panel-ui.AC8. No new blocker found — `API02` access already covered by `internal-transfer-workflow.T12`; confirmed `API07`, like `API05`/`API06`, performs no server-side payload validation. Plain string fields, no array-typed inputs this time, so no repeat of `T09`'s comma-separated-input judgment call. **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-20).
- [x] stakeholder-panel-ui.T11 — HR Final Mapping panel: `internal-transfer-workflow.API08` plus a Manager selector from `user-management-console.API04` (filtered to `role: "Manager"`) — Acceptance: stakeholder-panel-ui.AC9. No new blocker — both prerequisites (`internal-transfer-workflow.T12`, `user-management-console.T07`) already Merged. Client-side requires a Manager selection even though `API08`'s own payload treats `newManagerId` as optional, since selecting one is this panel's whole purpose — **confirmed correct by the reviewer, 2026-09-20, no change needed.** **Revised during Gate 2 at explicit reviewer direction:** the panel's gate was originally `role===HR && status==="Pending: Payroll, IT, Facilities"`, relying on `T12`'s carve-out to indirectly guarantee all 3 tasks were already Completed. The reviewer directed introducing a new, directly observable `Pending: Transfer` status instead (`internal-transfer-workflow.T13`, `AC25`, Merged) — this task's gate is now simply `status === "Pending: Transfer"`, no more indirect inference. **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-20).
- [x] stakeholder-panel-ui.T12 — Root route (`/`) redirect: no/expired session → `/login`; valid session → role's home page. Login itself gets the same already-authenticated check, redirecting away rather than showing the form. Acceptance: stakeholder-panel-ui.AC12 (added 2026-09-20). Added after the user asked what the first screen at the bare site URL would be — `/` was still the unmodified Next.js scaffold page, never assigned to any spec. New shared `homeRouteForRole` helper, reused by `LoginForm.tsx`'s existing post-login redirect, so the Admin-vs-everyone-else decision isn't duplicated. Red confirmed (4 new `page.test.tsx` tests + 2 new `LoginForm.test.tsx` tests, all failing for the right reason against the unmodified scaffold/no-existing-check), then Green — 11/11 new tests, full suite 627/627. **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-20).
- [x] stakeholder-panel-ui.T13 — Visual-polish pass + navigation: `StakeholderPanelLayout` gains a nav bar (My Requests for all roles; Submit Request for Employee only); every screen/panel gets consistent Tailwind spacing/typography — Acceptance: stakeholder-panel-ui.AC13 (nav links, testable); the visual styling itself is cited representatively against `int-standards.nextjs.md`'s already-approved Tailwind + shadcn/ui stack. Added 2026-09-21, same origin as `admin-panel-ui.T09` — one task per spec, navigation included alongside styling, by explicit user decision. Red confirmed for the nav bar (6 new tests failed — no nav rendered), all 11 components' existing tests re-run unmodified after restyling and confirmed still Green. Full suite 637/637. **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-21) — the last task in this spec.

## Coverage Gaps
`AC2b` (expiry-triggered force-logout) has no task in this file — **not an actual gap, reported per `generate-tasks.md`'s own instruction rather than silently left untested.** Its mechanism is built entirely inside the shared `src/shared/auth/` module, which `admin-panel-ui.plan.md`'s explicit cross-spec build-assignment decision put under `admin-panel-ui.T01` (this spec's own Gate-1-approved contract, implemented by a task in a different spec's `tasks.md`, by deliberate agreement — see `status.md`'s forward-pointer). No task is generated here for it to avoid a second, duplicate implementation.

All other ACs (`AC1`–`AC13` except `AC2b`, covered as above) are covered by at least one task above. `T12`'s `AC12` (2026-09-20) is covered by `T12` itself. `T13`'s `AC13` (2026-09-21) is covered by `T13` itself.

## Notes
- Sequencing step 1 ("Layout + Login + Logout") was split into `T01` (Login screen) and `T02` (Layout/guard/logout), and step 7 ("three structurally distinct task-completion forms") was split into `T08`/`T09`/`T10` (one per role) — both splits per `generate-tasks.md`'s instruction to split any step bundling more than one independently-verifiable unit of work. `AC8` is accordingly cited by three separate tasks, since the spec's own `AC8` covers all three roles as one criterion.
