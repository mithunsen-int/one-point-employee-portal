# Tasks: Admin Panel UI

## Derived From
.ai-context/plans/admin-panel-ui.plan.md

## Sequence
- [ ] admin-panel-ui.T01 — Shared `src/shared/auth/` module: session hook, token read/write + expiry check, guard predicate, logout function — implementing `stakeholder-panel-ui.AC1`/`AC2b`/`AC11`'s already-Approved contract (this spec's first, not sole, consumer) — Acceptance: admin-panel-ui.AC10 (representative citation only — this task builds the shared infrastructure `AC8`–`AC11` depend on; it implements another spec's ACs, not a dedicated AC of its own here). **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-20).
- [ ] admin-panel-ui.T02 — `AdminPanelLayout` + route guard (non-Admin/no-session redirect) + Logout control, consuming `T01`'s module — Acceptance: admin-panel-ui.AC8, admin-panel-ui.AC9, admin-panel-ui.AC10, admin-panel-ui.AC11. **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-20).
- [ ] admin-panel-ui.T03 — Dashboard screen (`useDashboard` over `transfer-admin-oversight.API01`) — Acceptance: admin-panel-ui.AC1. **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-20).
- [ ] admin-panel-ui.T04 — Transfer Request Monitoring list screen (`useTransferRequestsList` over `transfer-admin-oversight.API02`) — Acceptance: admin-panel-ui.AC2. **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-20).
- [ ] admin-panel-ui.T05 — Transfer Request Detail screen (`useTransferRequestDetail` over `transfer-admin-oversight.API03`) — Acceptance: admin-panel-ui.AC3. **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-20).
- [ ] admin-panel-ui.T06 — User Management screen: list + Formik/Yup create/edit forms + delete (`user-management-console.API01`–`API04` — `API05` not actually used, see status.md) — Acceptance: admin-panel-ui.AC4, admin-panel-ui.AC5. **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-20).
- [ ] admin-panel-ui.T07 — Department Management screen: list + Formik/Yup create/edit forms + delete, **no detail view** (`org-structure-management.API01`–`API04` — `API05` was never built, "Not yet set — deferred" per that spec; see status.md) — Acceptance: admin-panel-ui.AC6. **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-20).
- [ ] admin-panel-ui.T08 — Job Role Management screen: list + Formik/Yup create/edit forms + delete, no detail view (`org-structure-management.API06`–`API09`) — Acceptance: admin-panel-ui.AC7. **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-20) — all 8 tasks now Merged, spec Status moved to `In QA`.

- [ ] admin-panel-ui.T09 — Visual-polish pass + navigation: `AdminPanelLayout` gains a nav bar (all 5 screens); every screen gets consistent Tailwind spacing/typography and, where applicable, shadcn/ui `Table`/`Card` styling instead of plain HTML — Acceptance: admin-panel-ui.AC12 (nav links, testable); the visual styling itself is cited representatively against `int-standards.nextjs.md`'s already-approved Tailwind + shadcn/ui stack, since it adds no new user-facing behavior beyond the nav. Added 2026-09-21 after the user noticed the Dashboard renders with zero styling — found this was true of all 18 components across both UI specs, and that neither panel had any navigation between screens at all. By explicit user decision: one task per spec (not one per screen), and navigation links included alongside the styling. Red confirmed for the nav bar (1 new test failed — no nav rendered), all 6 screens' existing tests re-run unmodified after restyling and confirmed still Green (one text-shape mistake self-caught and fixed along the way — see status.md). Full suite 631/631. **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-21).

- [ ] admin-panel-ui.T10 — Create User form's Manager field becomes a `<select>` of available Managers, replacing the free-text ID input, shown when `role: "Employee"` — Acceptance: admin-panel-ui.AC5 (directly — a usable create-user action implies a usable way to supply the `managerId` field `AC5`'s own endpoint call already requires). Added 2026-09-21 at explicit user request. Populated from the same `useUsers()` data the screen already fetches (filtered to `role === "Manager"`) — no new endpoint or service call. Red confirmed (2 new tests failed — free-text input, no manager options rendered), then Green — one pre-existing test updated from `user.type(...)` to `user.selectOptions(...)` against a mocked list now including a Manager. Full suite 638/638. **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-21).

## Coverage Gaps
None identified — all 12 ACs (AC1–AC12) are covered by at least one task above. `T09`'s visual-styling scope (beyond the nav links themselves) has no dedicated AC — cited representatively, per this project's standing pattern for non-behavioral infrastructure/presentation tasks. `T10` is covered directly by the already-existing `AC5`, no amendment needed.

## Notes
- `T01`'s Sequencing-step bundle ("shared module + layout/guard/logout") was split into `T01`/`T02` at task-generation time, per `generate-tasks.md`'s instruction to split any step containing more than one independently-verifiable unit of work: `T01`'s functions are plain-Jest-testable with no UI, `T02`'s layout/guard/logout are RTL component tests consuming `T01`.
