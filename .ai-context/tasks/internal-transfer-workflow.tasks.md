# Tasks: Internal Transfer Workflow

## Derived From
.ai-context/plans/internal-transfer-workflow.plan.md

## Sequence
- [x] internal-transfer-workflow.T01 — `TransferRequests` Mongoose schema/model — Acceptance: internal-transfer-workflow.AC1 — **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-19)
- [x] internal-transfer-workflow.T02 — `POST /transfer-requests` (API01): submission, `departmentId`/`jobRoleId`/`location` validation, `assignedManagerId` snapshot — Acceptance: internal-transfer-workflow.AC1, internal-transfer-workflow.AC2, internal-transfer-workflow.AC3 — **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-19)
- [x] internal-transfer-workflow.T03 — `GET /transfer-requests/{id}` (API02): status/history/pending-stakeholders view, scoped to the requesting Employee — Acceptance: internal-transfer-workflow.AC18, internal-transfer-workflow.AC19 — **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-19)
- [x] internal-transfer-workflow.T04 — `POST /transfer-requests/{id}/manager-decision` (API03): approve/reject, conditional-required reason — Acceptance: internal-transfer-workflow.AC4, internal-transfer-workflow.AC5, internal-transfer-workflow.AC6, internal-transfer-workflow.AC9 — **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-19; `QT09` resolved as 403 before merge)
- [x] internal-transfer-workflow.T05 — `POST /transfer-requests/{id}/hr-decision` (API04): ≥90-day eligibility check, approve/reject, spawns 3 parallel tasks — Acceptance: internal-transfer-workflow.AC7, internal-transfer-workflow.AC8, internal-transfer-workflow.AC9 — **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-19)
- [x] internal-transfer-workflow.T06 — `POST .../payroll-task`, `.../it-task`, `.../facilities-task` (API05–API07): 3 independent task-completion endpoints — Acceptance: internal-transfer-workflow.AC10, internal-transfer-workflow.AC11, internal-transfer-workflow.AC12 — **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-19)
- [x] internal-transfer-workflow.T07 — `POST /transfer-requests/{id}/hr-final-mapping` (API08): gated on all 3 tasks `Completed` — Acceptance: internal-transfer-workflow.AC13, internal-transfer-workflow.AC14, internal-transfer-workflow.AC15 — **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-19). `AC13`'s "the employee's manager is updated" confirmed to mean a real `User.managerId` write, not just `TransferRequest.newManagerId` — implemented and tested.
- [x] internal-transfer-workflow.T08 — `POST /transfer-requests/{id}/withdraw` (API09) — Acceptance: internal-transfer-workflow.AC16, internal-transfer-workflow.AC17 — **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-19)
- [x] internal-transfer-workflow.T09 — Audit-trail integration: shared helper invoked after every successful transition in T02–T08, calling `transfer-audit-trail.API01` — Acceptance: internal-transfer-workflow.AC1 (representative — this task implements constitution.md's Non-Functional Constraint on audit logging, not a single dedicated AC of its own in this spec) — **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-19) — all 9 tasks now Merged, spec Status moved to `In QA`
- [x] internal-transfer-workflow.T10 — `GET /transfer-requests/mine` (API10): role-filtered discovery of requests relevant to the caller — Acceptance: internal-transfer-workflow.AC20, internal-transfer-workflow.AC21, internal-transfer-workflow.AC22, internal-transfer-workflow.AC23, internal-transfer-workflow.AC24 — added 2026-09-19; closed the gap found while scoping `stakeholder-panel-ui.spec.md`'s screens — **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-19) — all 10 tasks now Merged, spec Status returned to `In QA`.
- [x] internal-transfer-workflow.T11 — `GET /transfer-requests/{id}` (API02) carve-out: also allow the assigned Manager to view a `Pending: Manager` request assigned to them — Acceptance: internal-transfer-workflow.AC19 (amended 2026-09-20, Gate 1 re-confirmed 2026-09-20). Added after discovering, while building `stakeholder-panel-ui.T06`, that the shared `RequestDetail` base 403s for any Manager since API02 was Employee-owner-only with no exception — the entire "one adaptive screen" design was blocked at its foundation. HR/Payroll/IT/Facilities equivalents deferred to `T07`–`T10`, same rule, when each needs it. Red confirmed (new carve-out test failed 403≠200 against original code; pre-existing UT19 repointed to an unrelated Manager, confirmed still Green throughout), then Green — full suite 552/552. **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-20).
- [x] internal-transfer-workflow.T12 — `GET /transfer-requests/{id}` (API02) carve-out, part 2: HR (`AC22`'s rule) and Payroll/IT/Facilities (`AC23`'s rule) — Acceptance: internal-transfer-workflow.AC19 (already covers these roles generically; no further amendment needed). Added after auditing `stakeholder-panel-ui.T07`–`T11`'s remaining blockers all at once, by explicit user decision to build all three role carve-outs together rather than one per task. HR's clause covers both `stakeholder-panel-ui.T07` (Pending: HR) and `T11` (all 3 tasks Completed) in one condition. Red confirmed (5 new positive-case tests failed 403≠200; all 25 existing tests, including T11's own, already passed), then Green — full suite 574/574. **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-20).

- [x] internal-transfer-workflow.T13 — New `Pending: Transfer` status (AC25): set automatically when the last of the 3 parallel tasks completes (`API05`–`API07`); `API08` (hr-final-mapping)'s precondition simplified to check this status instead of the 3 task fields directly; `API02`'s HR carve-out and `API10`'s HR branch (AC22) both simplified the same way; `pendingStakeholdersFor` gets a new case. Acceptance: internal-transfer-workflow.AC13, AC14, AC18, AC22, AC25 (AC13/AC14/AC18/AC22 amended 2026-09-20; AC25 added 2026-09-20). Added at explicit user direction during `stakeholder-panel-ui.T11`'s Gate 2 review — closes the gap where `API02`'s response had no field indicating individual task-completion progress. Amendments need Gate 1 reviewer re-confirmation. New shared helper `allParallelTasksCompleted` reused by all 3 task-completion routes. Red confirmed across 7 test files (schema enum, 3 task-completion routes' transition tests, hr-final-mapping's precondition, `[id]` detail carve-out, `mine` API10 query — 13 failures for the right reason), existing fixtures deliberately updated to the new invariant (not silently left broken), then Green — 620/620 project-wide. **Merged** (Gate 2 reviewed by Test Reviewer, 2026-09-20; AC13/AC14/AC18/AC22/AC25 Gate 1 re-confirmed 2026-09-20).

## Coverage Gaps
None identified — all 25 ACs (AC1–AC25) are covered by at least one task above. `AC9` ("no resume for a Rejected request") is cross-cutting across both decision endpoints (`T04`, `T05`), since either can produce the `Rejected` state. `T11`'s AC19 amendment (2026-09-20) is covered by `T11` itself; `T12` implements the same already-amended `AC19` for the remaining roles. `T13`'s AC13/AC14/AC18/AC22/AC25 changes (2026-09-20) are covered by `T13` itself.
