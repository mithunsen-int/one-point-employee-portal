# Project Status Board
_Last updated: 2026-09-09_

## Active Specs

| Spec ID | Title | Status | Owner | Last Updated | Notes |
|---|---|---|---|---|---|
| internal-transfer-workflow | Core transfer request workflow (BRD-001) | **In QA** — all 10 tasks (T01–T10) Merged | Engineering assessment owner (acting as PO) | 2026-09-19 | Reopened from `In QA` same day to add `T10` (`GET /transfer-requests/mine`, `API10`), closing a discovery/listing gap found while scoping `stakeholder-panel-ui`. `T10` now Merged; Status returned to `In QA`. |
| transfer-admin-oversight | Admin dashboard + request monitoring (BRD-002) | In QA (all 3 tasks Merged) | Engineering assessment owner (acting as PO) | 2026-09-19 | T01–T03 all **Merged**. Spec Status moved `In Development` → `In QA`. |
| user-management-console | User management, all six roles (BRD-003) | **In QA** — all 6 tasks (T01–T06) Merged | Engineering assessment owner (acting as PO) | 2026-09-19 | Second spec in the project to reach `In QA` (after `rbac-api-security`). All of `src/app/api/users/`, `src/app/api/admin/self-register/`, and `src/services/users/` are Merged, 0 Blocking findings across all 6 Gate 2 reviews (see gate-reviews/gate2-evidence-user-management-console.md). **One open item carried into QA, not any single task's fault:** `PATCH /users/{id}` (`T04`) can set `role: "Admin"` with no guard — flagged at `T04`'s review, knowingly accepted, restated at `T05`/`T06`, tracked in `tasks.md`'s new Open Items section so it isn't lost. `rbac-api-security.T02`'s `userLookup.ts` stub can now be revisited — the schema (`T01`) has been ready since earlier this session. |
| org-structure-management | Department/BU + Job Role management (BRD-004) | **In QA** — all 3 tasks (T01–T03) Merged | Engineering assessment owner (acting as PO) | 2026-09-20 | `T03` Merged — `GET /departments`/`GET /job-roles` now open to any authenticated role (mutations unchanged, still Admin-only). Status returned `In Development` → `In QA`. Flagged, not silently resolved: Job Role 409 conflicts use error code `"name_exists"` (not `"title_exists"`), per `QT13`'s literal wording — worth a spec clarification. |
| application-constants-management | Centralized Application Constants Management (BRD-005) | **In QA** — all tasks (T01–T02) Merged | Engineering assessment owner (acting as PO) | 2026-09-19 | Fourth spec in the project to reach `In QA`. Both `src/services/constants/referenceValues.ts` and `src/shared/constants/referenceValues.ts` Merged, 0 Blocking findings across both Gate 2 reviews (see gate-reviews/gate2-evidence-application-constants-management.md). **Real open item, worth resolving now:** Location values in both modules are 8 explicit placeholders ("Location A"–"Location H"), not real business data — the mechanism is complete, only the actual content remains a product decision. |
| transfer-audit-trail | Immutable audit logging (BRD-006) | **In QA** — all 4 tasks (T01–T04) Merged | Engineering assessment owner (acting as PO) | 2026-09-20 | `T04` Merged — closed the `findTransferRequestById` production defect found via the test-coverage audit. Status returned `In Development` → `In QA`. |
| rbac-api-security | Role-based access control + API security (BRD-007) | **In QA** — all 7 tasks (T01–T07) Merged | Engineering assessment owner (acting as PO) | 2026-09-20 | `T07` Merged — closed the `userLookup.ts` production defect found via the test-coverage audit. Status returned `In Development` → `In QA`. Real, tracked gap: `withAuthorization` isn't called by any endpoint yet. |
| admin-panel-ui | Admin-only screens: dashboard, monitoring, user/org management (BRD-008) | **In QA** — all 8 tasks (T01–T08) Merged | Engineering assessment owner (acting as PO) | 2026-09-20 | `T08` (Job Role Management) Merged — all 6 screens now exist as real routes, all 11 ACs Merged. Spec Status moved `In Development` → `In QA`. |
| stakeholder-panel-ui | Login + role-specific request screens (BRD-008) | In Development (T01–T05 Merged; T06–T11 Not Started) | Engineering assessment owner (acting as PO) | 2026-09-20 | `T05` (adaptive detail/action screen base) Merged — real `/my-requests/[id]` route live. `T06` (Manager Decision panel) unblocked next. |

_Slugs above are proposed, pending each feature's actual spec creation (sdd-methodology.md #9) — not yet locked in._

## Daily Execution Log

### 2026-09-09
- BRD.md drafted from InternalTransferJourney–[SOW].md — 7 entries (BRD-001–BRD-007).
- Foundational SDD artefacts established: discovery-analysis.md, constitution.md, project_context.md, architecture.md, status.md.
- Sign-off gap resolved: BRD.md updated with Sponsor = "Engineering assessment owner (acting as Product Owner for this exercise)" on all 7 entries. Spec drafting is no longer blocked on missing ownership (see discovery-analysis.md's Sign-off Gap section for the resolution note).
- No ADR created — no team-made architectural trade-off exists yet at this stage (see architecture.md).
- `rbac-api-security.spec.md` (BRD-007) drafted, Draft v1.0 — first spec in the recommended build order, since it has no dependency on any other spec. Authorization enforcement (AC1–AC9) fully specified from decided BRD content; the authentication endpoint contract (API02) is explicitly left "not yet set" pending a mechanism decision, per the "don't invent" rule — flagged for Gate 1, not resolved by this workflow. Status moved In Peer Review.
- Authentication mechanism decided: JWT bearer tokens. Recorded as `ADR-0001-jwt-authentication.md` (first ADR in the project, per sdd-methodology.md #13 — cross-cutting, >1 day to reverse). `rbac-api-security.spec.md` amended: API02 filled in with the real request/response/exception contract, AC10–AC13 and UT11–UT14 added for the login flow, Explicitly Out of Scope extended (refresh-flow, revocation, password hashing — all explicitly not covered by the ADR). Spec is now complete and ready for a real Gate 1 pass; Status remains `Draft v1.0` since no human review has occurred yet.
- Gate 1 review completed for `rbac-api-security` by Test Reviewer (confirmed distinct from the spec's author) — all 10 checklist items passed, verdict **Approved**. Recorded in `.ai-context/gate-reviews/gate1-review-rbac-api-security.md`. Spec Status moved to `Approved`. This is the first spec in the project to clear Gate 1 — `@generate-plan.md for rbac-api-security` can now run.
- `rbac-api-security.plan.md` drafted. No new datastore/module introduced (JWT is stateless; role-permission matrix is code-defined). Rate limit decided at plan stage: 5 failed attempts/15-min window, 429 with `retry_after: 900`. **Tracked cross-plan dependency:** login needs a `Users` collection whose authoritative schema belongs to `user-management-console` (BRD-003, not yet specced) — this plan only asserts the minimal fields (`username`, `passwordHash`, `role`) it reads; will need revisiting if that schema ends up different. Spec Status moved to `Plan Drafted`.

### 2026-09-10
- `user-management-console.spec.md` (BRD-003) drafted, Draft v1.0. Confirmed its user records satisfy `rbac-api-security.plan.md`'s asserted read-contract (`username`, `passwordHash`, `role`) — no drift. Used `rbac-api-security.AC9` to resolve part of BRD-003's original ambiguity: Admin's authority to register all six roles is now established; HR's authority beyond Employees remains open. Two new gaps surfaced during drafting (Admin self-provisioning question, HR edit/view/delete authority) and logged in `discovery-analysis.md` rather than guessed. Status: In Peer Review, awaiting Gate 1.
- `org-structure-management.spec.md` (BRD-004) drafted, Draft v1.0 — covers Departments/BUs and Job Roles (renamed from BRD-004's "Role/Job Management" to avoid colliding with the access-control Role concept from BRD-003/007). Admin-only scoping resolved via `rbac-api-security.AC9`, same pattern as `user-management-console`. Two items left open rather than guessed: whether the "optional" detail view is in scope, and what happens to a transfer request that references a later-deleted Department/Job Role. Status: In Peer Review, awaiting Gate 1.
- `config-driven-dropdowns.spec.md` (BRD-005) drafted, Draft v1.0. The "which exact fields are config-driven" open item didn't block — the read API (API01/API02) is spec'd generically for any field key defined in the config file, same technique as `rbac-api-security`'s cross-cutting AC1/AC2. The "who edits the config file" open item does genuinely block the write side — API03 explicitly marked "not yet set" rather than guessed. Status: In Peer Review, awaiting Gate 1.
- `internal-transfer-workflow.spec.md` (BRD-001) drafted, Draft v1.0 — 9 endpoints covering submit, Manager/HR decisions, the 3 independent parallel tasks, final mapping, withdrawal, and status/history views. Reused the `internal-transfer-workflow` slug already referenced elsewhere rather than deriving a new one. All 4 of BRD-001's original open items (parallel-task failure path, rejection notification, concurrent requests, "Manager" definition) carried into Explicitly Out of Scope without blocking the core flow. **New gap found:** HR's ≥90-day eligibility rule (AC7) needs a "date employee started current role" field that no spec — including `user-management-console` — currently defines; flagged, not invented. Status: In Peer Review, awaiting Gate 1.
- `transfer-audit-trail.spec.md` (BRD-006) drafted, Draft v1.0. Clarified its relationship to `internal-transfer-workflow.API02`: this spec owns the data, that endpoint's `actionHistory` field is the same data embedded inline, not a separate mechanism. Write side (API01) modeled as an internal/cross-cutting call, not a client-facing endpoint — same pattern as `rbac-api-security.API01`. New scope question surfaced (transfer-request-only vs. system-wide audit coverage), logged rather than assumed. Status: In Peer Review, awaiting Gate 1.
- `transfer-admin-oversight.spec.md` (BRD-002) drafted, Draft v1.0 — the 7th and last spec. Resolved BRD-002's original "which statuses" open item by reusing `internal-transfer-workflow.AC18`'s decided status vocabulary rather than re-deciding it. No new service module — this spec is a pure read-only aggregation layer over the Workflow Engine, User Management, and Audit Logging services. All 7 BRD entries now have a drafted spec; all are In Peer Review or further, none blocked on a BRD-stage gap. Status: In Peer Review, awaiting Gate 1.
- Full spec review pass completed to identify plan-stage blockers. Two real gaps confirmed and closed with decisions: (1) `user-management-console` amended with `dateOfJoining` and `managerId` fields, HR's full CRUD authority over Employee records, soft-delete via `deletedAt`, and a new `API06` bootstrap endpoint for the single self-registered Admin account. (2) `internal-transfer-workflow` amended to consume both new fields, closing its previously-open eligibility-data-source and Manager-definition gaps. Both specs remain `Draft v1.0` (amended pre-Gate-1, no formal revision cycle needed). Remaining open items across all specs: HR's authority to register the other 5 roles, audit-trail scope (transfer-request-only vs. system-wide), org-structure delete-referential-integrity, detail-view scope, filtering/refresh cadence — none blocking, all explicitly flagged in their specs.
- `rbac-api-security.plan.md` deleted and regenerated fresh (no spec-content change; incorporated the `deletedAt` check and `API06` middleware exclusion from the start rather than as a follow-up patch).
- **BRD-005 rewritten** from "Configurable static dropdown values via JSON config" to "Centralized Application Constants Management" — the mechanism changed from a runtime-read JSON config file to dedicated, single-source JavaScript modules per layer (frontend and backend), codebase-controlled, no runtime/admin update path. Cascading updates applied across `constitution.md`, `architecture.md` (Configuration Service module removed — no service/API surface exists for this anymore), `discovery-analysis.md`, and the cross-references in `rbac-api-security`, `internal-transfer-workflow`, `org-structure-management`, and `user-management-console` specs. The old `config-driven-dropdowns.spec.md` was deleted per instruction; a fresh spec will be generated on request against the new BRD-005 text, likely deriving the slug `application-constants-management`.
- `application-constants-management.spec.md` (BRD-005) drafted, Draft v1.0, deriving the slug already anticipated in this row. No API Contract section — first spec in the project with none, since this feature has no API surface at all (pure codebase-organization requirement). ACs framed around single-source-of-truth code structure and code+redeploy update behavior rather than HTTP contracts. Updated the placeholder cross-references left in `internal-transfer-workflow` and `org-structure-management` specs (previously "BRD-005, not yet respecced") to point at the real spec file.
- Gate 1 review completed for `application-constants-management` by Test Reviewer (confirmed distinct from the spec's author) — all 10 checklist items passed, verdict **Approved**. Recorded in `.ai-context/gate-reviews/gate1-review-application-constants-management.md` via the new `generate-gate1-review.md` workflow (its first real use). Spec Status confirmed `Approved`. Since this spec has zero blocking dependencies (per the earlier audit), `@generate-plan.md for application-constants-management` can run immediately.
- Gate 1 review completed for `org-structure-management` by Test Reviewer — all 10 checklist items passed, verdict **Approved**. Recorded in `.ai-context/gate-reviews/gate1-review-org-structure-management.md`. Spec Status confirmed `Approved`. `config-driven-dropdowns`'s successor (`application-constants-management`) already cleared Gate 1 too, so `org-structure-management`'s only real dependency (`rbac-api-security`) is also long past Approved — `@generate-plan.md for org-structure-management` can run immediately.
- `transfer-admin-oversight.plan.md` drafted — the 7th and last plan. No new collection at all; three read-only aggregation endpoints over `Users`, `TransferRequests`, and `AuditLogs`. Two small plan-level decisions the spec left implicit: dashboard `userCounts` excludes soft-deleted users (consistent with `user-management-console.API04`'s list view), and `totalTransferRequests`/`statusBreakdown` count every request regardless of terminal status. `API03`'s audit history is a direct `AuditLogs` query, not an internal call to `transfer-audit-trail.API02`'s HTTP route (whose Employee-owns-request-or-Admin authorization doesn't apply here). Spec Status moved to `Plan Drafted`. **All 7 specs in the project now have a drafted plan.**
- `transfer-audit-trail.plan.md` drafted. Two decisions worth noting: (1) append-only immutability enforced at the Mongoose model layer via blocking `pre('findOneAndUpdate')`/`pre('deleteOne')` hooks, not just by never writing an update route — defense against a future accidental mutation path. (2) `actorRole` is snapshotted per-entry (same principle as `internal-transfer-workflow.plan.md`'s `assignedManagerId`), since an audit trail that reinterprets a past actor's role based on their current role would defeat its own purpose. Audit-scope question (transfer-request-only vs. system-wide) still explicitly deferred, not invented. Spec Status moved to `Plan Drafted`.
- `user-management-console.plan.md` drafted — the authoritative `Users` schema, consolidating the read-contracts already asserted by `rbac-api-security.plan.md` and `internal-transfer-workflow.plan.md` (confirmed no field conflicts across all three). Password hashing decided here (bcrypt, 12 rounds), since `rbac-api-security.plan.md` had explicitly deferred that choice to whichever plan owns the `Users` write path — this is it, so it couldn't be deferred further. At-most-one-Admin invariant (`AC15`) enforced at the database level via a partial unique index, not just an application-layer check, to close a real race-condition window. `API06` given its own rate limit (5/hour/IP) distinct from the other five endpoints' blanket "none, deferred," since it's the one genuinely unauthenticated surface. Spec Status moved to `Plan Drafted`.
- `org-structure-management.plan.md` drafted — two structurally identical collections (`Departments`, `JobRoles`), each a unique-indexed CRUD resource. Detail views (`API05`/`API10`) not built, per spec's own deferral. Delete-referential-integrity with transfer requests confirmed still genuinely open — `internal-transfer-workflow.plan.md` doesn't resolve it either (validates existence only at submission time), so this remains a real cross-spec gap, not silently assumed. Spec Status moved to `Plan Drafted`.
- `internal-transfer-workflow.plan.md` drafted — the core plan, introducing the first `TransferRequests` collection. Key plan-level decision not stated in the spec: `assignedManagerId` is snapshotted onto the request at submission time rather than looked up live from `user-management-console`'s `Users` collection on every check, to keep the approver stable while a request sits pending indefinitely (no SLA). Established a consistent cross-service pattern across this plan, `rbac-api-security.plan.md`, and `application-constants-management.plan.md`: services read each other's collections/modules via direct Mongoose queries or direct imports, never internal HTTP calls. Rate limit explicitly stated as "none, deferred" for all 9 endpoints, with justification (internal authenticated/role-gated actions, not a public abuse surface). Spec Status moved to `Plan Drafted`.
- `application-constants-management.plan.md` drafted — first plan in the project for a spec with no API surface. Data Model and rate-limit sections marked N/A explicitly rather than omitted, per the workflow's own requirement to state a rate-limit decision (or its absence) for every endpoint. Two sequencing steps only: create the backend and frontend constants modules as deliberately separate files (per the spec's `AC5`, not collapsed into one shared import despite Next.js technically allowing it). Wiring `internal-transfer-workflow` to actually import from these modules is named as an integration point but left to that spec's own tasks. Spec Status moved to `Plan Drafted`.
- `rbac-api-security.plan.md` deleted per request and regenerated fresh via `@generate-plan.md for rbac-api-security`. Content is substantively unchanged (deletedAt check, API06 middleware exclusion, 5/15-min rate limit) since nothing about the underlying spec or its dependencies changed — this was a clean re-draft, not a content revision. Checked the two cross-references in `user-management-console.spec.md` that cite `rbac-api-security.plan.md`'s asserted read-contract — both still hold true against the new plan, no changes needed. Spec Status confirmed `Plan Drafted`.
- Gate 1 review completed for `transfer-admin-oversight` by Test Reviewer — all 10 checklist items passed (item 7 genuinely held: all 4 Related specs Approved), verdict **Approved**. Recorded in `.ai-context/gate-reviews/gate1-review-transfer-admin-oversight.md`. Spec Status set `Approved`. **All 7 specs in the project are now Approved.**
- Gate 1 review completed for `transfer-audit-trail` by Test Reviewer — all 10 checklist items passed (item 7 genuinely held: both `internal-transfer-workflow` and `rbac-api-security` already Approved), verdict **Approved**. Recorded in `.ai-context/gate-reviews/gate1-review-transfer-audit-trail.md`. Spec Status set `Approved`. Only `transfer-admin-oversight` remains at Gate 1 — once it clears, all 7 specs will be Approved.
- A `gate1-review` request for `internal-transfer-workflow` caught a real problem: its checklist item 7 ("Related/Builds-on specs Approved") did not actually hold, since `user-management-console` was still `In Peer Review` — recording it as passed would have been a false entry in the audit trail. Flagged to the user rather than silently recording it; they chose to get `user-management-console` approved first. Gate 1 completed for `user-management-console` (Test Reviewer, all 10 items passed, `.ai-context/gate-reviews/gate1-review-user-management-console.md`), Status set `Approved`. With all 3 dependencies then genuinely Approved, Gate 1 completed for `internal-transfer-workflow` itself (Test Reviewer, all 10 items passed, `.ai-context/gate-reviews/gate1-review-internal-transfer-workflow.md`), Status set `Approved`. `@generate-plan.md for internal-transfer-workflow` can now run for real, with a dependency check that actually holds.
- **Full spec audit found and fixed two circular dependencies** in `Related:` cross-references that would have deadlocked `generate-plan.md`'s Precondition 3 (blocks a plan until every named spec is Approved, regardless of actual dependency direction): (1) `user-management-console` ↔ `internal-transfer-workflow` — reworded to reflect the real one-way direction (the latter depends on the former, not the reverse). (2) `org-structure-management` ↔ `application-constants-management` — a mutual "by contrast" note with no real functional dependency either way, reworded as explicitly non-blocking on both sides. Also softened `user-management-console`'s speculative note about `org-structure-management` (a possible future need, not a current dependency) to non-blocking. Dependency graph is now acyclic.

### 2026-09-16
- Human confirmation of all 7 plans' Constitution Checks completed (Test Reviewer). All 7 specs moved `Plan Drafted` → `Plan Reviewed`, per `sdd-methodology.md` #21.1's lifecycle — `generate-plan.md` never grants this state itself (#13), so this was a manual status update reflecting an actual human review, not an automated step. Moving to Task Decomposition next (`@generate-tasks.md` per spec).
- **Task decomposition completed for all remaining 5 specs.** `org-structure-management` (2 tasks, mirroring its 2 collections), `application-constants-management` (2 tasks, backend + frontend modules), `internal-transfer-workflow` (9 tasks — the largest, one per plan sequencing step, including a dedicated T09 for the shared audit-logging helper with no single AC of its own), `transfer-audit-trail` (3 tasks), `transfer-admin-oversight` (3 tasks). All 5 have full AC coverage, no Coverage Gaps. 19 new per-task prompt files written (12 + 19 = 31 total across the project). **All 7 specs now at `Tasks Generated`** — this closes Deliverable 5 and Deliverable 7 (AI Prompts) on the assessment tracker, and completes Milestone 3 (Plan → Tasks) in full.
- **`test_cases/_integration.md` deleted and regenerated fresh** against the actual 7-spec chain (the old version predated `BRD.md` and referenced a mismatched feature slug/design). New version: 10 cross-feature scenarios (INT-001–INT-010) grounded in real AC IDs across BRD-001 through BRD-007, including one full 7-spec happy-path journey (INT-002) and an audit-trail-vs-admin-oversight cross-implementation consistency check (INT-007). 2 scenarios (org-structure delete mid-journey, config-constant change mid-journey) left as Open Questions rather than guessed — both are already-known unresolved gaps, not new ones.
- **Spec-derived QA-expanded test cases generated for all 7 specs** via `@generate-test-cases.md` (Deliverable 3 on the assessment tracker — was the one gap keeping Milestone 1 from closing). `.ai-context/test_cases/<slug>.test_cases.md` written for each: `rbac-api-security` (30 rows, 3 Open Questions), `user-management-console` (24 rows, 5 Open Questions), `org-structure-management` (17 rows, 3 Open Question groups), `application-constants-management` (7 rows, 2 Open Questions — role-matrix expansion explicitly N/A, no auth applies), `internal-transfer-workflow` (26 rows, 4 Open Questions), `transfer-audit-trail` (7 rows, 1 new Open Question + 1 restated pre-existing item), `transfer-admin-oversight` (10 rows, 1 Open Question). Several open questions recur across specs (username/name case-sensitivity and whitespace-trimming has no project-wide rule) — flagged as a shared decision worth resolving once centrally rather than per spec. Pre-existing `.ai-context/test_cases/_integration.md` found to be stale scaffolding from before this session's chain existed (references a mismatched feature slug, AC numbering, and design — Kafka/ITSM, conditional step creation, `idempotencyKey` — none of which match the current specs); flagged to the user, not silently rewritten.
- `user-management-console.tasks.md` generated — 6 tasks (T01–T06) from the plan's 6 sequencing steps, 1:1. All 15 ACs (AC1–AC15) covered (some overlap by design — e.g. AC12/AC13 span both T04's edit/delete and T05's view, since the spec's HR-scoping rule itself covers view/edit/delete together). 6 per-task prompt files written. Spec Status moved `Plan Reviewed` → `Tasks Generated`.
- `rbac-api-security.tasks.md` generated — 6 tasks (T01–T06) from the plan's 6 sequencing steps, 1:1. All 13 ACs (AC1–AC13) covered, no Coverage Gaps. 6 per-task prompt files written under `prompts/rbac-api-security.T0#.prompt.md`, each scoped to one task with explicit "do not touch" boundaries against its siblings. Spec Status moved `Plan Reviewed` → `Tasks Generated`. This also closes the assessment tracker's Deliverable 7 (AI Prompts) for this feature specifically — the other 6 specs still need their own task/prompt generation.
- Project moved to `one-point-employee-portal/` — real Next.js 16 (App Router, React 19) installed. Found and fixed a file-collision: `next dev`'s auto-generated `CLAUDE.md`/`create-next-app`'s default `README.md` had shadowed our real ones (auto-renamed to `CLAUDE copy.md`/`README copy.md` on copy-in). Restored `CLAUDE.md` from the real content (kept a pointer to `AGENTS.md`, which `next dev` regenerates on its own with Next.js version-compatibility notes). Rewrote `README.md` properly per `sdd-methodology.md` #22 (operational-only: setup/run/build/deploy — no deployment target invented, since none is decided anywhere). Deleted both stale copies once confirmed unneeded. Confirmed no data loss in the move: all 7 specs/plans/tasks and 31 prompts intact.
- **First Red→Green cycle of the project: `rbac-api-security.T01`.** Jest + `next/jest` + React Testing Library installed and configured from scratch (`jest.config.mjs`, `jest.setup.ts`) — no test infra existed before this. Wrote 8 Red tests (`services/auth/jwt.test.ts`) pulling from `AC10`/`UT11` plus 5 QA-expanded rows cross-referenced from `AC1` (QT03–QT07 — same verification mechanism this task builds, even though tagged under the middleware AC in `test_cases.md`); confirmed each failed for the right reason (`Cannot find module`), not a setup error. Implemented `services/auth/jwt.ts` (`signToken`/`verifyToken`, using the `jsonwebtoken` package) to turn all 8 Green — confirmed via `npx tsc --noEmit` and `eslint` clean too. Surfaced one new gap during implementation: no default JWT lifetime was stated anywhere; decided 900s (15 min) explicitly and recorded in `rbac-api-security.plan.md`, same treatment as the plan's existing rate-limit-number decision. `T01` checked off in `rbac-api-security.tasks.md`; spec Status moved to `In Development`.
- **Gate 2 drafted for `rbac-api-security.T01`.** Attempted `T02`'s Red tests first, but `generate-tests.md`'s own Precondition 3 stopped it — `T02` depends on `T01` (per `T02`'s prompt file: "consumes the T01 signing utility"), and `T01` was only `Green`, not `Merged`. Ran `@code-review.md for rbac-api-security.T01` instead: full Gate 2 + Security checklist, all Pass, zero Blocking findings, 2 non-blocking Notes (`signToken()`'s return shape; no commit exists yet to check attribution against). Recorded in `.ai-context/gate-reviews/gate2-evidence-rbac-api-security.md`. **Now Merged** — Test Reviewer signed off with no changes requested. `rbac-api-security.T01` is the first task Merged in this project. `T02` is unblocked.
- **`rbac-api-security.T02` Red tests written** (`src/app/api/auth/login/route.test.ts`), now that `T01` is `Merged`. 6 tests pulling `UT11`/`UT12`/`UT13` (spec) plus `QT21`, `QT23`, `QT25` (QA-expanded, `QT22`/`QT24` folded into the same assertions as `UT12`/`UT13`): 200 success shape (`AC10`), 401 `invalid_credentials` for wrong password and for a non-existent username with an identical response shape/no enumeration (`AC11`), 400 `validation_error` for missing password, missing username, and non-string username (`AC12`). All 6 confirmed Red for the right reason — `Cannot find module '@/app/api/auth/login/route'` — not a setup error. `QT19`/`QT20`/`QT26` (Open QA Questions) and the plan-only `deletedAt` soft-delete-rejection behavior were deliberately **not** turned into tests, per `generate-tests.md`'s Do-not #3 (no inventing scenarios beyond the spec's Unit Test Cases table and `test_cases.md`) — flagged instead, not silently tested. Two new infrastructure gaps surfaced and were worked around, not silently assumed: (1) no MongoDB/Mongoose connection exists anywhere in this project yet, and no task in any `tasks.md` explicitly sets it up; (2) `bcrypt` (already decided in `user-management-console.plan.md`, 12 rounds) isn't an installed dependency yet. Both are mocked (`bcrypt` real-but-uninstalled, `src/services/auth/userLookup.ts` a not-yet-built auth-owned read boundary — deliberately not a direct import of `user-management-console`'s `Users` schema, staying inside T02's "only read from it, don't own it" boundary) so the tests are meaningful now without pre-building another spec's work.

### 2026-09-17
- **`rbac-api-security.T02` implemented, all 6 tests Green.** `bcrypt` installed for real (was virtual-mocked only during Red). `src/app/api/auth/login/route.ts`: payload validation, `findUserForLogin` lookup, `bcrypt.compare` against `passwordHash`, JWT issuance via `T01`'s `signToken`. `src/services/auth/userLookup.ts` created as the auth-owned read boundary named in the Red-phase report — its body deliberately throws "not yet wired to a real datastore, blocked on `user-management-console.T01`" rather than importing a guessed-at `Users` model path; the route's own logic is fully real and tested (mocked only at this one documented boundary). `!user || user.deletedAt` is handled in the same 401 branch as a wrong password, satisfying the plan's soft-delete/no-enumeration decision even though no dedicated test exists for it yet (still an open QA-coverage gap, tracked, not silently closed). Closed T01's Gate 2 Note: `DEFAULT_EXPIRES_IN_SECONDS` now exported from `jwt.ts` instead of `T02` hardcoding `900` again. `npx tsc --noEmit`, `eslint`, and the full Jest suite (14/14, both tasks) all clean. `T02` checked off in `rbac-api-security.tasks.md` as Green, awaiting Gate 2.
- **Gate 2 drafted for `rbac-api-security.T02`.** Full Gate 2 + Security checklist, all Pass, zero Blocking findings. Caught and fixed one real drift during the review itself: `rbac-api-security.spec.md`'s `## Status` field had been left at `Tasks Generated` since before `T01` — `status.md`'s log claimed it moved to `In Development` when `T01` went Green, but the spec file was never actually edited. Corrected now. Three non-blocking Notes recorded (stubbed `findUserForLogin`, blocked on `user-management-console.T01`; no dedicated test for the soft-deleted-user 401 branch; no commit exists yet). Recorded in `.ai-context/gate-reviews/gate2-evidence-rbac-api-security.md`. **Now Merged** — Test Reviewer confirmed no changes requested. `rbac-api-security.T02` is the second task Merged in this project. `T03` (login rate-limiting) is unblocked.
- **`rbac-api-security.T03` Red→Green.** 4 new tests added to `route.test.ts` (`UT14`/`QT27`–`QT30`): 5th failed attempt still evaluated normally, 6th blocked with 429 `rate_limited`/`retry_after`, per-username isolation (username A's failures don't affect username B), and reset after the 15-minute window elapses (via `jest.useFakeTimers`). 3 of 4 confirmed Red for the right reason (expected 429, got 401 — no rate-limit enforcement existed yet); the 4th (below-threshold behavior) passed immediately since it's identical with or without the limiter, not a false-Red concern. Implemented `src/services/auth/rateLimiter.ts` — an in-memory `Map<username, {count, windowStartedAt}>`, no new MongoDB collection (consistent with the plan's Constitution Check, which introduces no new datastore for this task). Wired into `route.ts`: checked before credential lookup (429 short-circuits before touching `findUserForLogin`/`bcrypt`), incremented only on invalid-credential outcomes — a successful login never increments or is blocked by the counter, per the task's own scope note (flagged as unbacked by spec/QA per Do-not #3, but still correctly implemented). 18/18 tests passing project-wide, `tsc`/`eslint` clean. `T03` checked off as Green, awaiting Gate 2.

### 2026-09-18
- **Gate 2 drafted for `rbac-api-security.T03`.** Full Gate 2 + Security checklist, all Pass, zero Blocking findings. Three non-blocking Notes recorded (in-memory rate-limit state is per-process, not shared/persisted; no dedicated test for "success doesn't count toward the counter," same treatment as `T02`'s `deletedAt` Note; no commit exists yet). Recorded in `.ai-context/gate-reviews/gate2-evidence-rbac-api-security.md`. **Now Merged** — Test Reviewer confirmed no changes requested. `rbac-api-security.T03` is the third task Merged in this project. `T04` (cross-cutting authorization middleware) is unblocked.
- **`rbac-api-security.T04` Red→Green.** New `src/services/auth/authenticate.test.ts` (8 tests): one positive-path sanity check (valid token → identity attached — the necessary counterpart to `AC1`/`UT01`'s 401 case, derived from `API01`'s own Success Response and T04's "attaches the decoded identity" scope note, not a distinct QT row) plus `QT01`–`QT07` re-verified at this task's own layer (header parsing → shaped 401), distinct from `T01`'s existing `QT03`–`QT07` coverage of `verifyToken()` itself. All 8 confirmed Red for the right reason (`Cannot find module './authenticate'`). Implemented `src/services/auth/authenticate.ts`: `authenticateRequest(request)` parses the `Authorization` header, delegates verification to `T01`'s `verifyToken` (no reimplementation), returns either `{ authenticated: true, identity }` or `{ authenticated: false, response }` with the exact `{ error: { code: "UNAUTHENTICATED", message } }` shape `API01` specifies. Deliberately not wired into any route yet, and no 403/role-permission logic — both explicitly out of this task's scope (`T06` and `T05` respectively). 26/26 tests passing project-wide, `tsc`/`eslint` clean. `T04` checked off as Green, awaiting Gate 2.
- **Gate 2 drafted and Merged for `rbac-api-security.T04`.** Full Gate 2 + Security checklist, all Pass, zero Blocking findings. Two non-blocking Notes recorded (not yet wired into any route — `T06`'s scope; no commit exists yet). Recorded in `.ai-context/gate-reviews/gate2-evidence-rbac-api-security.md`. Test Reviewer confirmed no changes requested. `rbac-api-security.T04` is the fourth task Merged in this project. `T05` (role-permission matrix, `AC2`–`AC9`) is unblocked.
- **`rbac-api-security.T05` Red→Green.** New `src/services/auth/permissions.test.ts` (26 tests) covering `UT02`–`UT10` and `QT08`/`QT09`/`QT13`–`QT18`, plus one positive-path complement test per role (the direct counterpart of each of `AC3`–`AC8`'s "an action other than X" phrasing, not fabricated beyond the spec). All confirmed Red for the right reason (`Cannot find module './permissions'`). Implemented `src/services/auth/permissions.ts`: a code/config-defined `Role`→`Action` catalog (introduced as this task's own naming decision, since the spec's ACs describe permitted-action *categories* in prose, not as literal identifiers) and `checkPermission(role, action): boolean` — an unknown/missing/malformed role always denies (`QT08`/`QT09`), matching AC2's "no permitted actions" treatment. No new MongoDB collection, per the plan's Data Model decision. Explicitly flagged, not tested: `QT10`–`QT12` need resource-level ownership/state context (e.g., "is this specific request routed to this Manager") that a pure `(role, action)` signature can't resolve — out of this task's scope by its own prompt file, deferred to whichever endpoint (chiefly `internal-transfer-workflow`) has access to the specific record. 52/52 tests passing project-wide, `tsc`/`eslint` clean. `T05` checked off as Green, awaiting Gate 2.
- **Gate 2 drafted and Merged for `rbac-api-security.T05`.** Full Gate 2 + Security checklist, all Pass, zero Blocking findings. Three non-blocking Notes recorded (not yet wired into any route — `T06`'s scope; `QT10`–`QT12` need resource-level context out of scope, deferred; no commit exists yet). Recorded in `.ai-context/gate-reviews/gate2-evidence-rbac-api-security.md`. Test Reviewer confirmed no changes requested. `rbac-api-security.T05` is the fifth task Merged in this project. `T06` (wiring `T04`/`T05` into every protected endpoint, excluding `user-management-console.API06`) is the last task in this spec and is now unblocked.
- **`rbac-api-security.T06` Red→Green.** No protected route exists anywhere in the codebase yet (the only route, `POST /auth/login`, is exactly the one this task must exclude), so this task's real deliverable is the reusable wiring mechanism itself, tested directly against a stand-in handler — the same treatment already given to `T04`/`T05` before any route consumed them. New `src/services/auth/withAuthorization.test.ts` (3 tests): no token → 401 `UNAUTHENTICATED`, handler never called (`AC1`); valid token but disallowed role → 403 `FORBIDDEN`, handler never called (`AC2`); valid token and allowed role → wrapped handler called with `(request, identity)`, its response returned unmodified. All 3 confirmed Red for the right reason (`Cannot find module './withAuthorization'`). Implemented `src/services/auth/withAuthorization.ts`: `withAuthorization(action, handler)` composes `T04`'s `authenticateRequest` and `T05`'s `checkPermission` without touching either's internals, matching this task's "do not touch" list exactly. The `user-management-console.API06` exclusion needs no special-case code — wiring is opt-in per-route by construction, so simply never wrapping that future route satisfies the requirement. Recorded a Coverage Gaps note in `rbac-api-security.tasks.md`: no endpoint calls this yet; every future protected route (in this or any other spec) must wrap itself with it to actually realize `AC1`/`AC2` in practice. 55/55 tests passing project-wide, `tsc`/`eslint` clean. `T06` checked off as Green, awaiting Gate 2 — the last task in this spec.
- **Gate 2 drafted and Merged for `rbac-api-security.T06`.** Full Gate 2 + Security checklist, all Pass, zero Blocking findings. Flagged prominently rather than as a routine Note: `AC1`/`AC2` are correctly implemented by this task but not yet realized on any live endpoint, since no other spec has built a route yet — expected sequencing, not a defect, and tracked in `tasks.md`'s Coverage Gaps so it isn't lost. Test Reviewer confirmed no changes requested. **`rbac-api-security.T06` Merged — all 6 tasks in this spec are now Merged.** Spec Status moved `In Development` → `In QA`, per sdd-methodology.md #21.1's fixed lifecycle sequence — the first spec in this project to reach that state. Next: either begin QA exercise of `rbac-api-security.test_cases.md`'s scenarios, or move to task decomposition/implementation for one of the remaining 6 specs (all currently `Tasks Generated`, none yet started).
- **Started `user-management-console` implementation — `T01` Red→Green.** Task decomposition for this spec was already done earlier in the project (6 tasks + prompt files existed, none started); this session began the actual Red→Green cycle at `T01`. This is the project's first task needing a real database, so before writing tests: installed `mongoose` and `mongodb-memory-server`, then hit `MongooseServerSelectionError: Missing required sub-document 'driver' in the client metadata document` — a known regression in `mongodb` driver 7.6.0 under Jest, no upstream fix yet ([typegoose/mongodb-memory-server#1026](https://github.com/typegoose/mongodb-memory-server/issues/1026)). Fixed by pinning `mongodb` to `~7.2.0` via `package.json`'s `overrides` field (verified via `npm ls mongodb` that both `mongoose` and `mongodb-memory-server-core` resolve to the pinned version); confirmed working with a throwaway smoke test before writing any real test. New `src/services/users/User.test.ts` (7 tests) and `src/services/db/connect.test.ts` (1 test), pulling `UT06`/`AC5` (duplicate username) and `UT18`/`AC15` (duplicate active Admin) plus plan-Data-Model-backed field validators (`dateOfJoining` required, `managerId` required only for Employee, `role` enum) and one plan-backed-not-QA-row-backed test (a soft-deleted Admin doesn't block a new one — the entire reason the index is partial, not full). All confirmed Red for the right reason (`Cannot find module './User'`). Implemented `src/services/users/User.ts` (the schema, matching the plan's Data Model exactly) and, as a necessary shared precondition not itself part of `T01`'s literal scope, `src/services/db/connect.ts` (a cached-connection singleton — this project's first production DB-connection utility, which every future schema-owning task will reuse) and `src/test-utils/mongoServer.ts` (a shared `mongodb-memory-server` test harness, kept out of `src/services/` so a devDependency never risks bundling into production code). One implementation-time fix: the partial-unique-Admin-index test initially failed (`Resolved instead of rejected`) because Mongoose builds indexes asynchronously in the background — fixed by awaiting `User.init()` in the test's `beforeAll`, not by changing the schema. 63/63 tests passing project-wide, `tsc`/`eslint` clean. `T01` checked off as Green, awaiting Gate 2.
- **Gate 2 drafted and Merged for `user-management-console.T01`.** Full Gate 2 + Security checklist, all Pass, zero Blocking findings. Three non-blocking Notes recorded (the `mongodb` version-pin tracks an upstream bug, revisit once fixed; `connect.ts` has no consumer yet — nearest is `rbac-api-security`'s `userLookup.ts` stub; no commit exists yet). Pre-emptively avoided the exact drift caught during `rbac-api-security.T02`'s review by moving the spec's own `## Status` field to `In Development` in the same edit, not after the fact. Recorded in `.ai-context/gate-reviews/gate2-evidence-user-management-console.md` (new file, first Gate 2 evidence for this spec). Test Reviewer confirmed no changes requested. `user-management-console.T01` is Merged — the first cross-spec dependency (`rbac-api-security.T02`'s stubbed `userLookup.ts`) now has a real schema to eventually connect to, though wiring that up is not this task's own scope. `T02` (password hashing utility, bcrypt 12 rounds) is unblocked.
- **`user-management-console.T02` Red→Green.** New `src/services/users/passwordHashing.test.ts` (5 tests): hash differs from the raw password, hash is provably at 12 salt rounds (`bcrypt.getRounds(hash)`), same password hashes differently each call (unique salt), and the produced hash round-trips correctly through `bcrypt.compare` — the same function `rbac-api-security.T02` already calls directly, confirming compatibility without building a new verification wrapper (explicitly out of this task's scope). All 5 confirmed Red for the right reason (`Cannot find module './passwordHashing'`). Implemented `src/services/users/passwordHashing.ts`: `hashPassword(rawPassword)` — a thin `bcrypt.hash(..., 12)` wrapper, no new dependency (`bcrypt` already installed). `AC1`/`AC14` cited representatively, same pattern as `T01`'s schema-level citation — the full 201/record-creation flows they describe belong to `T03`/`T06`. 68/68 tests passing project-wide, `tsc`/`eslint` clean. `T02` checked off as Green, awaiting Gate 2.
- **Gate 2 drafted and Merged for `user-management-console.T02`.** Full Gate 2 + Security checklist, all Pass, zero Blocking findings. Two non-blocking Notes recorded (not yet called by any write path — `T03`/`T06`'s scope; no commit exists yet). Recorded in `.ai-context/gate-reviews/gate2-evidence-user-management-console.md`. Test Reviewer confirmed no changes requested. `user-management-console.T02` is Merged. `T03` (`POST /users`) is unblocked — the first real endpoint in this spec, and the first place `T01`'s schema and `T02`'s hashing utility get used together.
- **`user-management-console.T03` Red→Green.** Before writing tests, caught and fixed a real gap: `T03`'s own Acceptance line in `tasks.md` was missing `AC5` — `T01`'s Gate 2 review had already said AC5's 409 response is deferred to this endpoint, but no task's Acceptance line actually claimed it. Corrected to match the pattern already used correctly for `rbac-api-security.AC10` (listed under both its `T01` and `T02`). New `src/app/api/users/route.test.ts` (25 tests) covering `AC1`–`AC6`/`AC11` plus their QA-expanded rows — full role sweeps for `AC2`–`AC4` (`QT03`/`QT04`/`QT05`), all four `AC6` validation fields, both `AC11` 404 cases (`UT13`/`QT17`), and `AC5`'s 409 duplicate-username case (`UT06`) confirmed with a real second `User.countDocuments` check, not just the response code. All 25 confirmed Red for the right reason (`Cannot find module './route'`). Implemented `src/app/api/users/route.ts`: `authenticateRequest` (`T04`) for the 401 layer; the `AC2`–`AC4` 403 rule is hand-written inline rather than routed through `withAuthorization`/`checkPermission`, since it depends on the request body's target `role` — a case `rbac-api-security.T05`'s own Gate 2 review already anticipated needing to be handled by "whichever endpoint has access to the specific record." `managerId` validated via `Types.ObjectId.isValid()` then a real `User.findOne({ _id, role: "Manager", deletedAt: null })` query; duplicate `username` is caught via MongoDB's real `11000` error from `T01`'s unique index (not a separate check-then-insert query, avoiding the same race condition `T01`'s plan already reasoned about for the Admin index). One concrete implementation choice made for an explicitly unresolved item: `QT18` (managerId referencing a soft-deleted Manager) is an Open QA Question — the handler excludes soft-deleted Managers (`deletedAt: null` in the query), a necessary, flagged placeholder decision, not a silent resolution. `QT06`/`QT07`/`QT18` all correctly left untested per Do-not #3. 93/93 tests passing project-wide, `tsc`/`eslint` clean. `T03` checked off as Green, awaiting Gate 2.
- **Gate 2 drafted and Merged for `user-management-console.T03`.** Full Gate 2 + Security checklist, all Pass, zero Blocking findings. One Should-Fix (the `AC5` Acceptance-line gap) already caught and fixed before this diff was written, not during review. Three non-blocking Notes recorded (hand-written 403 logic instead of `withAuthorization`, deliberate; `QT18` placeholder decision for an open question; no commit exists yet). Recorded in `.ai-context/gate-reviews/gate2-evidence-user-management-console.md`. Test Reviewer confirmed no changes requested. `user-management-console.T03` is Merged. `T04` (`PATCH`/`DELETE /users/{id}`) is unblocked.
- **`user-management-console.T04` Red→Green.** New `src/app/api/users/[id]/route.test.ts` (25 tests) covering `AC7`/`AC8`/`AC12`/`AC13` for both `PATCH` and `DELETE`: Admin editing any record (role and username changes, `UT09`/`QT11`), HR editing an Employee's `managerId` (`UT14`), full 6-role sweeps for HR's block on non-Employee records for both endpoints (`QT20`), a representative non-Admin/non-HR-caller-has-no-authority case for each endpoint, 404 for a non-existent id, 404 for a soft-deleted id (an explicit consistency inference with `DELETE`'s own "existing, non-deleted" wording, flagged not assumed silently), 404 on double-delete (`QT13`), Admin/HR delete success (`UT10`/`UT15`), and 401 for both endpoints. All 25 confirmed Red for the right reason (`Cannot find module './route'`) — one genuine test-fixture bug caught along the way (an `Employee` fixture missing the schema-required `managerId`), fixed in the test helper, not treated as a false Red. Implemented `src/app/api/users/[id]/route.ts` — this project's first dynamic Next.js route, using Next 16's async `params` (`Promise<{ id: string }>`, awaited before use). Surfaced and explicitly resolved, not silently picked, a real spec-documentation gap: `API02`'s Request payload text names only `username`/`role` as editable, but `UT14` (a spec-derived test case) exercises editing `managerId` — treated `API02`'s text as under-documented rather than `UT14` as wrong, and support all three fields; flagged in the Gate 2 report for a future spec amendment. Also flagged, not fixed (outside this task's asked-for scope): `PATCH`'s `role` field has no guard against setting `role: "Admin"`, a path around `API06`'s bootstrap flow when no Admin yet exists — a real finding for the human reviewer to weigh, not something to patch unilaterally without being asked. `QT12`/`QT19` (role edits crossing the Employee boundary) correctly left untested — Open QA Questions. 118/118 tests passing project-wide, `tsc`/`eslint` clean. `T04` checked off as Green, awaiting Gate 2.
- **Gate 2 drafted and Merged for `user-management-console.T04`.** Full Gate 2 + Security checklist — zero Blocking findings, but one finding flagged prominently for the reviewer's own judgment: `PATCH`'s `role` field has no guard against `role: "Admin"`. **Test Reviewer reviewed it and merged anyway** — a knowing acceptance of an open, non-blocking gap, not an oversight; recorded as still-open in `tasks.md` and the evidence file so it isn't lost, worth a future follow-up task (naturally paired with `T06`'s `API06` work). Two further non-blocking Notes recorded (the `API02`/`UT14` spec-documentation gap; no commit exists yet). Recorded in `.ai-context/gate-reviews/gate2-evidence-user-management-console.md`. `user-management-console.T04` is Merged. `T05` (`GET /users`, `GET /users/{id}`) is unblocked.
- **`user-management-console.T05` Red→Green.** Added 17 new tests across the existing `route.test.ts` (list) and `[id]/route.test.ts` (detail) files, colocated with `T03`/`T04`'s tests rather than new files, matching the project's one-test-file-per-route-file convention. Covers `AC9` (Admin sees every non-deleted user across all roles, including the Admin account itself — `QT14` — and excluding a soft-deleted one — `QT15`), `AC10` (full detail fields for Admin — `UT12` — and 404 for a soft-deleted id — `QT16`), `AC12` (HR sees/views Employee records identically to Admin), and `AC13` (HR blocked from all 6 non-Employee roles on both endpoints — `QT20` sweep). All 17 confirmed Red for the right reason (`TypeError: GET is not a function` — the route module resolves fine since `POST`/`PATCH`/`DELETE` already exist there, only the new export was missing). Implemented by adding `GET` exports to the two existing route files, reusing `T04`'s `canActOn`/`notFound` helpers for the detail view rather than duplicating the scoping logic — `T03`'s `POST` and `T04`'s `PATCH`/`DELETE` bodies were not touched, per this task's own scope. 135/135 tests passing project-wide, `tsc`/`eslint` clean. `T05` checked off as Green, awaiting Gate 2.
- **Gate 2 drafted and Merged for `user-management-console.T05`.** Full Gate 2 + Security checklist, all Pass, zero Blocking findings. Two non-blocking Notes (no commit exists yet; `T04`'s `role: "Admin"` guard gap restated as still-open, not new/worsened here). Recorded in `.ai-context/gate-reviews/gate2-evidence-user-management-console.md`. Test Reviewer confirmed no changes requested. `user-management-console.T05` is Merged. `T06` (`POST /admin/self-register`) is unblocked — the last task in this spec.
- **`user-management-console.T06` Red→Green.** New `src/app/api/admin/self-register/route.test.ts` (9 tests) covering `AC14` (first self-registration — `UT17`; the endpoint requires no Authorization header at all, confirming its deliberate exclusion by design; a real `Promise.all` concurrency test — `QT21` — confirming the database's partial unique index, not application logic, is what guarantees exactly one Admin under a true race; a soft-deleted Admin not blocking re-registration — `QT22`), `AC15` (409 once an Admin exists — `UT18`; the full 5-request-then-6th-429 rate-limit boundary — `QT23`/`QT24`), and two validation-error cases. All 9 confirmed Red for the right reason (`Cannot find module './route'`). Implemented `src/app/api/admin/self-register/route.ts` (no `authenticateRequest` call at all — correct by omission, not a guard that was removed) and a new `src/services/users/selfRegisterRateLimiter.ts`, deliberately NOT reusing `rbac-api-security`'s login rate limiter since the semantics differ (every request counts here, keyed by IP, 1-hour window — vs. failures-only, per-username, 15-minute window there). Relies on `T01`'s partial unique index (catching MongoDB's `11000` error) for `AC15`, per this task's own explicit instruction not to add a redundant application-level "count Admins" check. One flagged, necessary decision: `API06`'s payload doesn't collect `dateOfJoining`, but `T01`'s schema requires it universally — defaulted to registration time, documented in the code, not a silent invention. Hit and fixed a real environment bug while writing the concurrency/rate-limit tests: `jest.resetModules()` (used to isolate the rate limiter's in-memory state between tests) was also clearing the shared `mongoose` connection singleton, so a dynamically re-imported route got a fresh, disconnected `mongoose` instance — manifesting as `insertOne()` buffering timeouts, not an obvious "reset the wrong thing" error. Fixed by giving the rate limiter its own `__resetForTests()` export instead of resetting the whole module registry. 144/144 tests passing project-wide, `tsc`/`eslint` clean. `T06` checked off as Green, awaiting Gate 2 — the last task in `user-management-console`.

### 2026-09-19
- **Gate 2 drafted and Merged for `user-management-console.T06`.** Full Gate 2 + Security checklist, all Pass, zero Blocking findings. Four non-blocking Notes recorded (the `dateOfJoining` default; the `jest.resetModules()`/shared-`mongoose`-connection bug found and fixed during test-writing, worth remembering as a general pattern; no commit exists yet; `T04`'s `role: "Admin"` guard gap restated once more). Recorded in `.ai-context/gate-reviews/gate2-evidence-user-management-console.md`. Test Reviewer confirmed no changes requested. **`user-management-console.T06` Merged — all 6 tasks in this spec are now Merged.** Spec Status moved `In Development` → `In QA` — the second spec in this project to reach that state (after `rbac-api-security`). Added an "Open Items" section to `tasks.md` to carry the `role: "Admin"` guard gap forward now that no single task owns fixing it — worth a small follow-up task before or during QA. Next: begin task decomposition/implementation for one of the 5 remaining specs (`org-structure-management`, `application-constants-management`, `internal-transfer-workflow`, `transfer-audit-trail`, `transfer-admin-oversight`, all still `Tasks Generated`), or begin QA exercise of either completed spec's `test_cases.md`.
- **Picked `org-structure-management` as the next spec, by dependency order.** `internal-transfer-workflow`'s own plan explicitly reads `Departments`/`JobRoles` directly (not stubbed) — building this spec (and `application-constants-management`) before `internal-transfer-workflow` avoids the kind of stub `rbac-api-security.T02`'s `userLookup.ts` needed. `org-structure-management` has zero cross-spec implementation dependencies of its own and an earlier BRD number (004 vs. 005) than `application-constants-management` — picked first on that basis, not a hard requirement either way. Task decomposition for this spec was already done earlier in the project (2 tasks, prompt files existed, none started); began the Red→Green cycle at `T01`.
- **`org-structure-management.T01` Red→Green.** New `src/app/api/departments/route.test.ts` + `[id]/route.test.ts` (25 tests) covering `AC1` (create — `UT01`), `AC2` (full 6-role sweep blocked from create/edit/delete — `QT02`/`QT03`), `AC3` (duplicate name on create — `UT03`), `AC4` (edit — `UT04` — and name-collision-with-a-different-record — `QT05`), `AC5` (delete — `UT05` — and double-delete-is-404 — `QT06`), `AC6` (list, including the empty-list case — `QT08`), `AC13` (missing/empty name — `UT13`/`QT16`), plus 401 checks per endpoint. All 25 confirmed Red for the right reason (`Cannot find module`). Implemented `src/services/org-structure/Department.ts` (minimal schema — `name` unique-indexed only, no `deletedAt`, per the plan's explicit "otherwise minimal" schema decision — this is a genuine hard delete, unlike `Users`' soft delete) and both route files, built directly on `rbac-api-security.T04`/`T05`/`T06`'s already-Merged `withAuthorization` wrapper — this is the first endpoint in the whole project to actually call it, and it uses the `admin.departmentRoleManagement` action `T05`'s permission matrix already defined for exactly this purpose. A small composition pattern introduced for the dynamic `[id]` route: since `withAuthorization` only wraps a single-arg handler, the route's `context` (async `params`) is captured via closure around an inline `withAuthorization(...)(request)` call, rather than widening that already-Merged wrapper's signature. `QT01`/`QT04` (Open QA Questions on whitespace/case) and `QT07` (the known Department-deletion-referential-integrity gap, explicitly out of this task's scope per its own prompt file) correctly left untested. 169/169 tests passing project-wide, `tsc`/`eslint` clean. `T01` checked off as Green, awaiting Gate 2.
- **Gate 2 drafted and Merged for `org-structure-management.T01`.** Full Gate 2 + Security checklist, all Pass, zero Blocking findings. Spec's own `## Status` field moved `Tasks Generated` → `In Development` in the same edit as this task's work, avoiding the drift caught retroactively in earlier specs' reviews. Three non-blocking Notes (the `withAuthorization`-plus-dynamic-`params` composition pattern, worth reusing verbatim for `T02`; hard-delete confirmed intentional; no commit exists yet). Recorded in `.ai-context/gate-reviews/gate2-evidence-org-structure-management.md` (new file). Test Reviewer confirmed no changes requested. `org-structure-management.T01` is Merged. `T02` (`JobRoles`, structurally identical) is unblocked.
- **`org-structure-management.T02` Red→Green.** New `src/app/api/job-roles/route.test.ts` + `[id]/route.test.ts` (25 tests), mirroring `T01`'s structure exactly (`AC7`–`AC13`, full 6-role sweep on create, Manager-specific edit/delete sweep — `QT10`/`QT11` — duplicate/collision handling, empty-list case — `QT15`). All 25 confirmed Red for the right reason (`Cannot find module`). Implemented `src/services/org-structure/JobRole.ts` and both route files, reusing `T01`'s exact `withAuthorization`+closed-over-`context` composition pattern (recommended in `T01`'s own Gate 2 review) and the same `admin.departmentRoleManagement` action. One flagged decision, not silently picked: `API06`/`API07`'s spec text says "identical shapes... substituting title for name," which is ambiguous about whether that extends to the literal error *code string* on a 409 — but `QT13` (a concrete QA-derived test case) explicitly expects `{ "error": "name_exists" }` for a Job Role title conflict, matching `Departments`' code rather than a `title_exists` variant. Followed `QT13`'s literal wording as the more concrete source, flagged in this log and the Gate 2 report rather than silently choosing either reading. `QT09`/`QT12`/`QT14` (Open QA Questions, same classes as `T01`'s `QT01`/`QT04`/`QT07`) correctly left untested. 194/194 tests passing project-wide, `tsc`/`eslint` clean. `T02` checked off as Green, awaiting Gate 2 — the last task in `org-structure-management`.
- **Gate 2 drafted and Merged for `org-structure-management.T02`.** Full Gate 2 + Security checklist, all Pass, zero Blocking findings. Two non-blocking Notes (the `name_exists`-vs-`title_exists` error-code ambiguity, worth a spec clarification; no commit exists yet). Recorded in `.ai-context/gate-reviews/gate2-evidence-org-structure-management.md`. Test Reviewer confirmed no changes requested. **`org-structure-management.T02` Merged — both tasks in this spec are now Merged.** Spec Status moved `In Development` → `In QA` — the third spec in this project to reach that state.
- **Picked `application-constants-management` next.** Task decomposition already existed (2 tasks, prompt files, none started). Before writing `T01`'s implementation, hit a genuine "don't invent" blocker: the SOW/BRD/spec/plan never state the actual list of Location values — `discovery-analysis.md` already tracks this as an open BRD-005 item ("Location is implied but not confirmed"). Unlike prior engineering-judgment gaps (JWT lifetime, bcrypt rounds, rate-limit thresholds), this is real business content, not something to invent unilaterally — stopped and asked. User provided 8 placeholder values, "Location A" through "Location H", explicitly for now, pending real confirmation later.
- **`application-constants-management.T01` Red→Green.** New `src/services/constants/referenceValues.test.ts` (4 tests) covering `AC2` (the exported set, and membership validation via `.includes()` — accept/reject, `UT02`), `AC3` (no `department`/`jobRole` keys, verified via `hasOwnProperty` on the actual imported module, not just a string search), and `AC4` (a structural test — `location` is a plain array, not a type union or separately-registered value, which is what makes "add a value by editing this file alone" true; `AC4` itself describes a source-edit action a runtime test can't literally execute, so this test demonstrates the structural property that makes it hold, not a simulation). All 4 confirmed Red for the right reason (`Cannot find module './referenceValues'`). Implemented `src/services/constants/referenceValues.ts` exporting `location: readonly string[]`, with the 8 placeholder values and a prominent code comment flagging them as not-real business data. No API route, no database, no Mongoose model — a plain module only, per this task's own scope and the spec's explicit "no service/API module" framing. `T02` (frontend module) deliberately not started here — it's a separate task, separate file, no shared import, per the plan's explicit "two independently authoritative modules" decision. 198/198 tests passing project-wide, `tsc`/`eslint` clean. `T01` checked off as Green, awaiting Gate 2.
- **Gate 2 drafted and Merged for `application-constants-management.T01`.** Full Gate 2 + Security checklist, all Pass, zero Blocking findings. One finding flagged for explicit attention (placeholder Location values — a real content gap distinct from this task's own correctness, not silently treated as resolved) plus one routine Note (no commit exists yet). Recorded in `.ai-context/gate-reviews/gate2-evidence-application-constants-management.md` (new file). Test Reviewer confirmed no changes requested. `application-constants-management.T01` is Merged. `T02` (frontend constants module) is unblocked.
- **`application-constants-management.T02` Red→Green.** New `src/shared/constants/referenceValues.test.ts` (4 tests): the same `AC1`/`AC3`/`AC4` pattern as `T01`'s backend module, plus `AC5`/`UT05` — verified not by comparing runtime output (the two modules currently hold identical values, which is incidental, not enforced) but by reading this module's own source file and confirming no `import`/`require` of the backend module's path exists. One genuine test bug caught and fixed along the way: the first version of that check used too broad a regex and flagged the file's own explanatory *comment* (which legitimately names the backend path) as if it were a real import — tightened to match only an actual import/require statement. All 4 confirmed Red for the right reason (`Cannot find module './referenceValues'`) before the fix, and correctly Green after. Implemented `src/shared/constants/referenceValues.ts` — same 8 placeholder values as `T01`, defined completely independently, per `AC5`'s decided design; `T01`'s module was not touched. 202/202 tests passing project-wide, `tsc`/`eslint` clean. `T02` checked off as Green, awaiting Gate 2 — the last task in `application-constants-management`.
- **Gate 2 drafted and Merged for `application-constants-management.T02`.** Full Gate 2 + Security checklist, all Pass, zero Blocking findings. One finding restated for attention (placeholder Location values, now doubly relevant with both modules built) plus one routine Note (no commit exists yet). Recorded in `.ai-context/gate-reviews/gate2-evidence-application-constants-management.md`. Test Reviewer confirmed no changes requested. **`application-constants-management.T02` Merged — both tasks in this spec are now Merged.** Spec Status moved `In Development` → `In QA` — the fourth spec in this project to reach that state.
- **Picked `transfer-audit-trail` as the last remaining unblocker for `internal-transfer-workflow`.** That plan explicitly calls this spec's append function (in-process, not HTTP) on every successful transition — with `org-structure-management` and `application-constants-management` now both `In QA`, this was the one hard dependency left before `internal-transfer-workflow` itself can start.
- **`transfer-audit-trail.T01` Red→Green.** New `src/services/audit/AuditLog.test.ts` (7 tests): a positive-control append test (confirms the guards don't accidentally block legitimate creation — worth testing explicitly, not just assumed), required-field validation (plan-Data-Model-backed, same treatment as earlier schema tasks), and the core `AC2`/`UT03`/`QT03` append-only enforcement — `Model.findOneAndUpdate`, `Model.deleteOne` (query-style), `Model.deleteMany`, and a fetched document's own `.deleteOne()` (document-style) all confirmed to throw, plus confirmation that a blocked mutation attempt leaves the entry unchanged afterward. All 7 confirmed Red for the right reason (`Cannot find module './AuditLog'`) and passed cleanly on the first Green attempt. Implemented `src/services/audit/AuditLog.ts` — schema fields exactly per the plan's Data Model (`transferRequestId` indexed, `actorId`, snapshotted `actorRole`, `action`, `timestamp`), with `pre('findOneAndUpdate')` and a combined `pre(['deleteOne', 'deleteMany'], { document: true, query: true })` hook — the `{ document: true, query: true }` option is what makes both invocation styles actually blocked, not just the query-style default. **Flagged, not silently closed:** `Model.findOneAndDelete()` is a distinct Mongoose deletion pathway this task's own prompt file didn't enumerate (only `findOneAndUpdate`/`deleteOne`/`deleteMany`) — implemented exactly what was asked rather than unilaterally adding a hook beyond scope, and flagged the resulting real, currently-unguarded gap for the reviewer's explicit decision, same treatment as `user-management-console.T04`'s `role: "Admin"` finding. 209/209 tests passing project-wide, `tsc`/`eslint` clean. `T01` checked off as Green, awaiting Gate 2.
- **Closed the flagged `findOneAndDelete` gap before Merge.** User asked how to handle it; recommended fixing it now rather than deferring, since it was a one-line addition to the same hook call and squarely served this task's own stated purpose — unlike `user-management-console.T04`'s `role: "Admin"` gap (a separate authorization question on an already-merged task), this one was still in-review. Added `findOneAndDelete` to the existing `pre([...])` array plus one new test confirming it throws. 210/210 tests passing project-wide, `tsc`/`eslint` clean. Gate 2 evidence updated in place to record the finding as raised-and-resolved within the same review cycle, not erased. Test Reviewer confirmed no changes requested. `transfer-audit-trail.T01` is Merged. `T02` (internal append function — the shared helper `internal-transfer-workflow` calls after every successful transition) is unblocked.
- **`transfer-audit-trail.T02` Red→Green.** New `src/services/audit/appendAuditLogEntry.test.ts` (4 tests): creates an entry recording actor/action/server-set-timestamp (`UT01`), records `actorRole` exactly as given — the snapshotting the plan's Data Model requires — two calls append two independent entries rather than overwriting, and a structural check that the function's own signature has no timestamp parameter at all (distinct from the runtime confirmation that a real `Date` is actually written). All 4 confirmed Red for the right reason (`Cannot find module './appendAuditLogEntry'`). Implemented `src/services/audit/appendAuditLogEntry.ts` as a thin, fully generic wrapper — deliberately does not hardcode any of the plan's 10 transition-type action strings, or check whether the triggering action succeeded: both of those are `internal-transfer-workflow.T09`'s own call-site responsibility (which action string to pass, and whether to call this function at all), explicitly out of this task's scope per its own prompt file. `QT01` (full 10-transition enumeration) and `QT02` (failed actions produce no entry) both correctly deferred to that future task rather than simulated here. 214/214 tests passing project-wide, `tsc`/`eslint` clean. `T02` checked off as Green, awaiting Gate 2.
- **Gate 2 drafted and Merged for `transfer-audit-trail.T02`.** Full Gate 2 + Security checklist, all Pass, zero Blocking findings. Two non-blocking Notes (not yet called by anything — `internal-transfer-workflow.T09`'s scope; no commit exists yet). Recorded in `.ai-context/gate-reviews/gate2-evidence-transfer-audit-trail.md`. Test Reviewer confirmed no changes requested. `transfer-audit-trail.T02` is Merged. `T03` (`GET /transfer-requests/{id}/audit-log`, the read side) is unblocked — the last task in this spec.
- **`transfer-audit-trail.T03` Red→Green.** Real blocker hit and handled with the same pattern already established for `rbac-api-security.T02`: this endpoint needs `TransferRequests.employeeId` to determine ownership, but `internal-transfer-workflow` (which owns that schema) hasn't been built at all yet. Built `src/services/audit/transferRequestLookup.ts` as a clearly-flagged stub (throws, references the blocking task by name) rather than either inventing a fake schema or blocking this task entirely. New `src/app/api/transfer-requests/[id]/audit-log/route.test.ts` (11 tests, mocking the stub via the same relative-path + `virtual: true` workaround already documented for the Jest/`moduleNameMapper` interaction) covering `AC3` (Employee owns request — `UT04`), `AC4` (Admin any request — `UT05`), `AC5` (a different Employee — `UT06` — and the full Manager/HR/Payroll/IT/Facilities sweep, including the acting reviewer themselves — `QT04`/`QT05`), `AC6` (non-existent id — `UT07` — and malformed id — `QT06`, resolved as 404 for consistency with every other `[id]` route in this project, flagged as a deliberate convention choice since the spec itself doesn't fix which), plus 401. All 11 confirmed Red for the right reason (`Cannot find module './route'`). One more content decision flagged, not silently picked: `API02`'s response contract says `"actor": "string"` without specifying what it should contain — mapped to `entry.actorId.toString()` (the schema's closest literal analog), noting that `UT01`/`UT02`'s "actor=Employee"/"actor=Manager" phrasing reads as informal shorthand for role, not a literal field-content spec. `QT07` (chronological ordering, an Open QA Question) correctly left unenforced, per this task's own prompt file. 225/225 tests passing project-wide, `tsc`/`eslint` clean. `T03` checked off as Green, awaiting Gate 2 — the last task in `transfer-audit-trail`.
- **Resolved the flagged `actor`-field-content finding during Gate 2 review, before Merge.** User asked whether the actor's name could be used — surfaced a real fact: `Users` has no `name` field, only `username`. User confirmed: use `username`, looked up from the already-Merged `Users` collection (no stub needed, unlike `TransferRequests`). Updated `route.ts` to resolve `actor` via a batched `User.find({ _id: { $in: actorIds } })` query, deliberately not filtered by `deletedAt: null` (audit history isn't reinterpreted just because the actor has since left), with a raw-`actorId` fallback only if no matching `User` exists. Added 3 new tests (resolved-username case, soft-deleted-actor case, no-match fallback). 228/228 tests passing project-wide, `tsc`/`eslint` clean. Gate 2 evidence updated in place to record the finding as raised-and-resolved within the same review cycle, not erased. Test Reviewer confirmed no changes requested. **`transfer-audit-trail.T03` Merged — all 3 tasks in this spec are now Merged.** Spec Status moved `In Development` → `In QA` — the fifth spec in this project to reach that state.
- **Confirmed, via full-project audit, that no frontend/React UI task exists anywhere in the artefact chain — a real, tracked gap, not just "not yet scheduled."** All 31 tasks across all 7 specs are backend-only (Mongoose schemas + Next.js API routes). `BRD.md` never converted the SOW's Admin Panel/Stakeholder Panel UI requirement (SOW §1/§7/§8) into its own BRD entry — a deliberate methodology call (deliverables trace back into the 7 business-need entries rather than standing alone), but the practical effect is nothing owns building the UI. Two plans forward-reference "a future Stakeholder Panel spec" that doesn't exist. `architecture.md` already names the intended modules (Admin Panel UI, Stakeholder Panel UI — Next.js App Router, TanStack Query, Zustand), just not as a spec. Recorded in `discovery-analysis.md`'s Open Questions table. User's decision: stay backend-only for now, revisit later — not resolved, deliberately deferred.
- **Started `internal-transfer-workflow` — the core spec — now that all 3 hard dependencies are `In QA`.** Task decomposition already existed (9 tasks, prompt files, none started). `T01` Red→Green: new `src/services/workflow/TransferRequest.test.ts` (27 tests) covering required fields (plan-Data-Model-backed, same treatment as every other schema task this session), the `status` enum's full 6-value vocabulary matching `AC18` exactly (each value individually accepted, one invalid value rejected), and the three task-status enums (`payrollTaskStatus`/`itTaskStatus`/`facilitiesTaskStatus`) — each independently defaults to `Pending`, accepts `Completed`, rejects anything else, and confirmed genuinely independent of the other two (setting one doesn't affect the others). All 27 confirmed Red for the right reason (`Cannot find module './TransferRequest'`) and passed cleanly on the first Green attempt. Implemented `src/services/workflow/TransferRequest.ts` matching the plan's Data Model field-for-field, including the snapshotted (not live-looked-up) `assignedManagerId`. One schema-level decision made and flagged, not explicitly stated in the plan: the three task-status fields default to `"Pending"` on creation rather than being left undefined, since `AC10`–`AC12` frame them as independently trackable state from the moment a request exists, not only once the Payroll/IT/Facilities stage begins. Per this task's own scope, the conditional-required `managerDecisionReason`/`hrDecisionReason` validation was deliberately NOT built at the schema level — that's `T04`/`T05`'s application-layer job. 255/255 tests passing project-wide, `tsc`/`eslint` clean. `T01` checked off as Green, awaiting Gate 2.
- **Gate 2 drafted and Merged for `internal-transfer-workflow.T01`.** Full Gate 2 + Security checklist, all Pass, zero Blocking findings. Two non-blocking Notes (the task-status defaults, a reasonable schema completion not explicitly stated in the plan; no commit exists yet). Recorded in `.ai-context/gate-reviews/gate2-evidence-internal-transfer-workflow.md` (new file). Test Reviewer confirmed no changes requested. `internal-transfer-workflow.T01` is Merged. `T02` (`POST /transfer-requests`) is unblocked — the first endpoint in this spec, and the first place `org-structure-management`, `application-constants-management`, and `user-management-console` all get consumed together.
- **`internal-transfer-workflow.T02` Red→Green — the first genuinely real (non-stubbed) cross-spec integration in the project.** New `src/app/api/transfer-requests/route.test.ts` (17 tests) covering `AC1` (valid submission → 201 `Pending: Manager`, `assignedManagerId` verified against the real snapshotted `managerId` — `UT01`; optional `reason` omitted — `QT01`), `AC2` (each of the four required fields missing individually — `UT02`/`QT05`–`QT07`), location outside the configured set (`QT03`, distinct 400 vs. 404), both 404 cases for a nonexistent `departmentId`/`jobRoleId` individually plus the "both missing, at least one named" case (`QT04`), and `AC3` (full 6-role sweep blocked — `UT03`/`QT08`). All 17 confirmed Red for the right reason (`Cannot find module './route'`). Implemented `src/app/api/transfer-requests/route.ts` using `withAuthorization("transfer.initiate", ...)` — the exact action `rbac-api-security.T05`'s permission matrix already anticipated for this — then real `Department.exists()`/`JobRole.exists()` queries (`org-structure-management`, Merged), a real `location` membership check against the backend constants module (`application-constants-management`, Merged), and a real `User.findById()` lookup to snapshot `managerId` (`user-management-console`, Merged) — none of these four cross-spec reads needed mocking or stubbing, unlike every previous task that touched an not-yet-built dependency. One transient test-run failure (a `job-roles` test file's `afterEach` timing out under full-suite parallel load) confirmed as environment resource contention, not a regression — passed cleanly both in isolation and on a full-suite rerun immediately after. 272/272 tests passing project-wide, `tsc`/`eslint` clean. `T02` checked off as Green, awaiting Gate 2.
- **Gate 2 drafted and Merged for `internal-transfer-workflow.T02`.** Full Gate 2 + Security checklist, all Pass, zero Blocking findings. Three non-blocking Notes (this is the project's first real, non-stubbed cross-spec integration — a milestone, not a defect; no handling for a missing/deleted-employee edge case, deliberately not built unrequested; no commit exists yet). Recorded in `.ai-context/gate-reviews/gate2-evidence-internal-transfer-workflow.md`. Test Reviewer confirmed no changes requested. `internal-transfer-workflow.T02` is Merged. `T03` (`GET /transfer-requests/{id}`) is unblocked.
- **`internal-transfer-workflow.T03` Red→Green.** New `src/app/api/transfer-requests/[id]/route.test.ts` (17 tests) covering `AC19` (owner Employee → 200 with full shape — including `actionHistory` sourced from real `AuditLogs` with actor resolved to username; the assigned Manager, `HR`/`Payroll`/`IT`/`Facilities` — `QT25` — Admin with explicitly no carve-out — `QT26` — and a different Employee, all 403; `UT19`), `AC18` (all 6 status values rendered exactly plus their derived `pendingStakeholders` — `QT24`, one parametrized test covering both per status in one pass), 404, and 401. All 17 confirmed Red for the right reason (`Cannot find module './route'`) and passed cleanly on the first Green attempt. Implemented `src/app/api/transfer-requests/[id]/route.ts`: ownership check (`employeeId === identity.userId`, no role branching — even the Employee role itself must be the *specific* owner) rather than routed through `withAuthorization`, same request-body/resource-dependent reasoning already established for `user-management-console.T03`'s registration endpoint; `pendingStakeholders` derived from `status` via a plain switch; `actionHistory` queries the real, Merged `AuditLog` collection and resolves `actor` to `username` using the exact same logic already built for `transfer-audit-trail.T03`'s own endpoint — per that spec's own text, this is genuinely the same underlying data, just embedded inline. Deliberately did not extract that ~10-line resolution logic into a shared helper: used in exactly one other place right now, and a premature shared abstraction for a single future consumer isn't warranted yet — flagged, not silently duplicated without a trace. 289/289 tests passing project-wide, `tsc`/`eslint` clean. `T03` checked off as Green, awaiting Gate 2.
- **Gate 2 drafted and Merged for `internal-transfer-workflow.T03`.** Full Gate 2 + Security checklist, all Pass, zero Blocking findings. Two non-blocking Notes (actor-resolution logic duplicated rather than shared, a deliberate two-call-site judgment call; no commit exists yet). Recorded in `.ai-context/gate-reviews/gate2-evidence-internal-transfer-workflow.md`. Test Reviewer confirmed no changes requested. `internal-transfer-workflow.T03` is Merged. `T04` (`POST /transfer-requests/{id}/manager-decision`) is unblocked.
- **`internal-transfer-workflow.T04` Red→Green.** New `src/app/api/transfer-requests/[id]/manager-decision/route.test.ts` (14 tests) covering `AC4`/`UT04` (approve → `Pending: HR`), `AC5`/`UT05` (reject with reason → `Rejected`, `managerDecisionReason` stored and verified directly against the document, not just the response), `AC6`/`UT06`/`QT10` (missing and empty-string reason both 400), 403 for a *different* Manager and for a non-Manager role, `AC9`'s 409-on-wrong-status swept across all 5 non-`Pending: Manager` statuses (scoped to this endpoint only — the cross-endpoint `QT14`/`QT15` sweep is each future task's own responsibility, per this task's own "enforced by each endpoint's own state check, not centrally" scope note), an invalid `decision` value (baseline request-shape validation, not tied to a distinct AC/QT row), 404, and 401. All 14 confirmed Red for the right reason (`Cannot find module './route'`) and passed cleanly on the first Green attempt. Implemented `src/app/api/transfer-requests/[id]/manager-decision/route.ts`: the 403 check compares the caller's `userId` directly against `transferRequest.assignedManagerId` (not a role check) — this is this task's own prompt file explicitly resolving `test_cases.md`'s open `QT09` ("403 or 404 for a non-assigned Manager?") as 403, ahead of that question's own formal spec-amendment resolution; flagged in the test file and here rather than silently treated as newly decided. 303/303 tests passing project-wide, `tsc`/`eslint` clean. `T04` checked off as Green, awaiting Gate 2.
- **Resolved `QT09` during Gate 2 review, before Merge.** Flagged the pre-resolution as worth explicit attention rather than silently letting `test_cases.md` and the implementation disagree; user confirmed 403 is the correct, final decision. Updated `test_cases.md`'s `QT09` row and Open Questions entry to record the resolution (2026-09-19), and both `route.ts`'s and `route.test.ts`'s comments to reference the closed question. 303/303 tests still passing, `tsc`/`eslint` clean. Gate 2 evidence updated in place to record the finding as raised-and-resolved within the same review cycle, not erased. Test Reviewer confirmed no changes requested. `internal-transfer-workflow.T04` is Merged. `T05` (`POST /transfer-requests/{id}/hr-decision`) is unblocked.
- **`internal-transfer-workflow.T05` Red→Green.** New `src/app/api/transfer-requests/[id]/hr-decision/route.test.ts` (20 tests) covering `AC7`/`UT07` (approve at exactly 90 days — `QT11`, the inclusive boundary — moves to `Pending: Payroll, IT, Facilities` and initializes all 3 task statuses to `Pending`), `AC7`/`QT12` (89 days → 409, status unchanged, verified against the stored document), `AC8`/`UT08` (reject with reason → `Rejected`, `hrDecisionReason` stored), `AC8`/`QT13` (missing/empty reason both 400), a full 6-role 403 sweep, `AC9`'s 409-on-wrong-status across all 5 non-`Pending: HR` statuses, invalid `decision`, 404, and 401. All 20 confirmed Red for the right reason (`Cannot find module './route'`) and passed cleanly on the first Green attempt. Implemented `src/app/api/transfer-requests/[id]/hr-decision/route.ts`: role check only (any HR user, no per-request routing, per the spec's Explicitly Out of Scope — unlike `T04`'s exact-`assignedManagerId` check); the 90-day eligibility check (`Math.floor` day-boundary math, inclusive at exactly 90) is applied *only* to the approve path — a real distinction directly traceable to `AC7`'s "when HR approves" framing versus `AC8`'s unconditional reject, confirmed with a dedicated test (a 10-day-tenure employee's request can still be rejected without hitting the eligibility check at all). Both the wrong-status and insufficient-tenure conditions return the same generic `{"error": "conflict"}` 409 body, matching `API04`'s single grouped exception line rather than inventing a distinguishing error code the spec doesn't provide. 323/323 tests passing project-wide, `tsc`/`eslint` clean. `T05` checked off as Green, awaiting Gate 2.
- **Gate 2 drafted and Merged for `internal-transfer-workflow.T05`.** Full Gate 2 + Security checklist, all Pass, zero Blocking findings. Two non-blocking Notes (wrong-status and insufficient-tenure share the same generic 409 body, matching `API04`'s single grouped exception line; no commit exists yet). Recorded in `.ai-context/gate-reviews/gate2-evidence-internal-transfer-workflow.md`. Test Reviewer confirmed no changes requested. `internal-transfer-workflow.T05` is Merged. `T06` (the three parallel task-completion endpoints) is unblocked.
- **`internal-transfer-workflow.T06` Red→Green.** Three separate route files (`payroll-task`, `it-task`, `facilities-task`) with three separate, fully-duplicated test files — matching this project's own established convention for structurally-identical-but-independent resources (same pattern as `org-structure-management`'s `Departments`/`JobRoles`), not extracted into a shared parameterized handler; this task's own scope note ("never reads or reasons about the other two tasks' state... this is what makes AC10–AC12's independence real, not just documented") argues for genuinely separate files, not a shared abstraction that could couple them. 37 new tests total (`AC10`/`UT10` — including Payroll's `no_action_needed` alternative to `update`, both completing the task the same way; `AC11`/`UT11`; `AC12`/`UT12` — each verified by asserting the *other two* fields remain untouched, the actual independence check, not just that the target field flips); 6-role 403 sweep per endpoint; 409 when parallel tasks don't exist yet (`QT16`, tested against `Pending: Manager`/`Pending: HR`); 409 idempotency on an already-`Completed` task (`QT17`); 404; 401. All 37 confirmed Red for the right reason (`Cannot find module './route'`, once per file) and passed cleanly on the first Green attempt. Implemented all three handlers with no payload validation at all — `API05`–`API07`'s contracts document no 400 exception for any of them, unlike every other endpoint in this project, so no validation was invented; Payroll's `action` field content is accepted without being checked, since both of its documented values produce the identical `Completed` outcome per this task's own scope note. 360/360 tests passing project-wide, `tsc`/`eslint` clean. `T06` checked off as Green, awaiting Gate 2.
- **Gate 2 drafted and Merged for `internal-transfer-workflow.T06`.** Full Gate 2 + Security checklist, all Pass, zero Blocking findings. Two non-blocking Notes (no payload validation on any of the three endpoints, a literal reading of API05–07's contracts; no commit exists yet). Recorded in `.ai-context/gate-reviews/gate2-evidence-internal-transfer-workflow.md`. Test Reviewer confirmed no changes requested. `internal-transfer-workflow.T06` is Merged. `T07` (`POST /transfer-requests/{id}/hr-final-mapping`) is unblocked.
- **`internal-transfer-workflow.T07` Red→Green.** New `src/app/api/transfer-requests/[id]/hr-final-mapping/route.test.ts` (14 tests) covering `AC13`/`UT13` (all 3 tasks `Completed` → `status: "Completed"`, `newManagerId` set, `completedAt` a real `Date` — plus `QT20`, a different HR user than the one who approved at `API04` still permitted, confirming no per-request HR routing), `AC14`/`UT14`/`QT19` (each of the three tasks individually still `Pending` blocks it — 409, request genuinely unchanged afterward, not just the response code), a full 6-role 403 sweep, 404, and 401. All 14 confirmed Red for the right reason (`Cannot find module './route'`) and passed cleanly on the first Green attempt. Implemented `src/app/api/transfer-requests/[id]/hr-final-mapping/route.ts`: the three-task gate checks each field independently (no assumed completion order, per `AC14`'s wording). **Flagged prominently, not silently decided:** `AC13`'s text says "the employee's manager is updated," which could mean writing to the employee's actual `User.managerId` in `user-management-console`'s `Users` collection — but this task's own authored scope explicitly limits the implementation to "sets `newManagerId` from the payload," a `TransferRequest`-only field. Followed the task's literal scope (no `Users` write, staying inside this task's own collection boundary) and recorded the interpretive gap in `tasks.md`'s new Open Items section, not resolved either way unilaterally. `QT21` (exact confirmation data shape, an Open QA Question) correctly not built — `AC15` is satisfied entirely by `T03`'s existing `GET` view surfacing `status: "Completed"`, no separate mechanism needed. 374/374 tests passing project-wide, `tsc`/`eslint` clean. `T07` checked off as Green, awaiting Gate 2.
- **Resolved the `AC13`/`Users.managerId` finding during Gate 2 review, before Merge.** User confirmed explicitly: HR selects a new manager for the employee from a list, and the employee's actual manager assignment must change — not just a record on the transfer request. Updated `route.ts` to also call `User.findByIdAndUpdate(transferRequest.employeeId, { managerId: newManagerId })`, keeping the existing `TransferRequest.newManagerId` write as the historical record of what was decided on this specific request. Added a dedicated test confirming the employee's own `User.managerId` is genuinely updated. 375/375 tests passing, `tsc`/`eslint` clean. Gate 2 evidence and `tasks.md`'s Open Items updated in place to record the finding as raised-and-resolved within the same review cycle, not erased. Test Reviewer confirmed no changes requested. `internal-transfer-workflow.T07` is Merged. `T08` (`POST /transfer-requests/{id}/withdraw`) is unblocked.
- **`internal-transfer-workflow.T08` Red→Green.** New `src/app/api/transfer-requests/[id]/withdraw/route.test.ts` (10 tests) covering `AC16`/`UT16` (owner Employee withdraws a `Pending: Manager` request → `Withdrawn`), `AC17`/`UT17`/`QT23` (all 5 other statuses individually → 409, full sweep beyond the spec's single `Pending: HR` example), 403 for a different Employee and for the assigned Manager, 404, and 401. All 10 confirmed Red for the right reason (`Cannot find module './route'`) and passed cleanly on the first Green attempt. Implemented `src/app/api/transfer-requests/[id]/withdraw/route.ts` reusing the exact same ownership-check pattern already established at `T03` (`employeeId === identity.userId`, no role branching, no Admin carve-out). `QT22` (withdraw racing a near-simultaneous Manager approval) correctly left untested — an Open QA Question this task's own prompt file explicitly says not to invent locking behavior for. 385/385 tests passing project-wide, `tsc`/`eslint` clean. `T08` checked off as Green, awaiting Gate 2 — only `T09` (audit-trail integration) remains in this spec.
- **Gate 2 drafted and Merged for `internal-transfer-workflow.T08`.** Full Gate 2 + Security checklist, all Pass, zero Blocking findings. One routine Note (no commit exists yet). Recorded in `.ai-context/gate-reviews/gate2-evidence-internal-transfer-workflow.md`. Test Reviewer confirmed no changes requested. `internal-transfer-workflow.T08` is Merged. `T09` (audit-trail integration) is unblocked — the last task in this spec.
- **`internal-transfer-workflow.T09` Red→Green — the largest single task this session, touching 8 already-Merged route files at 10 call sites.** Built one new shared helper, `src/services/workflow/recordTransitionAudit.ts` (a thin wrapper around `transfer-audit-trail`'s `appendAuditLogEntry`, own unit test), so all 8 route files import one local function rather than each reaching directly into a different spec's service directory. Mapped exactly to `transfer-audit-trail.plan.md`'s pre-existing 10-action catalog: `submitted` (`T02`), `manager_approved`/`manager_rejected` (`T04`), `hr_approved`/`hr_rejected` (`T05`), `payroll_task_completed`/`it_task_completed`/`facilities_task_completed` (`T06`'s 3 files), `hr_final_mapping` (`T07`), `withdrawn` (`T08`). For each of the 8 files: added a positive test (successful transition → one `AuditLog` entry with the right `action`/`actorId`) and at least one negative test (a blocked attempt — 403/409 — appends nothing, per `transfer-audit-trail.test_cases.md`'s `QT02`), confirmed Red (`toHaveLength(0)` received where `1` was expected) before wiring each call, then Green immediately after. **Real pre-existing issue found and fixed along the way, not introduced by this task:** several already-Merged test files (`T05`'s hr-decision, all three of `T06`'s task-completion tests, `T07`'s hr-final-mapping) used non-`ObjectId` string literals (`"hr-user"`, `"payroll-user"`, `"it-user"`, `"facilities-user"`, `"a-different-hr-user"`) as the caller's `userId` in their *success-path* tests — harmless before, since nothing previously stored that value as a database field, but this task's new `actorId: Schema.Types.ObjectId` write would have thrown a Mongoose cast error the moment those paths executed. Fixed by swapping each affected success-path literal for a real `new Types.ObjectId().toString()`, re-confirmed each file's pre-existing tests still passed before adding the new audit assertions, so this was a verified, isolated fixture correction, not a change to any file's actual test intent. 404/404 tests passing project-wide, `tsc`/`eslint` clean. `T09` checked off as Green, awaiting Gate 2 — the last task in `internal-transfer-workflow`.
- **Gate 2 drafted and Merged for `internal-transfer-workflow.T09`.** Full Gate 2 + Security checklist, all Pass, zero Blocking findings. Two findings flagged for explicit reviewer awareness (the intentional 8-file, 10-call-site touch — exactly this task's own stated purpose, not scope creep; the pre-existing test-fixture gap found and fixed, isolated from the new feature work by re-confirming each file's existing suite passed before adding new tests) plus one routine Note (no commit exists yet for any of the 8 files this diff touches). Recorded in `.ai-context/gate-reviews/gate2-evidence-internal-transfer-workflow.md`. Test Reviewer confirmed no changes requested. **`internal-transfer-workflow.T09` Merged — all 9 tasks in this spec are now Merged.** Spec Status moved `In Development` → `In QA` — the sixth spec in this project to reach that state. **Correction, caught by the user immediately after this entry was first written:** this was NOT the last of the 7 BRD-001–007 specs — `transfer-admin-oversight` (BRD-002) was still at `Tasks Generated`, its task implementation not yet started at all. That claim was wrong and is retracted here rather than silently edited away.
- **Started `transfer-admin-oversight` — the 7th and last of the originally-drafted specs to begin implementation.** Task decomposition already existed (3 tasks, prompt files, none started). `T01` Red→Green: new `src/app/api/admin/dashboard/route.test.ts` (11 tests) covering `AC1`/`UT01`/`QT01` (registered-user counts across all 6 roles, zero-count roles still present, not omitted), `QT02` (Admin excluded entirely from `userCounts`, soft-deleted users excluded), `AC2`/`UT02`/`QT03`/`QT04` (status breakdown and total, zero-count statuses still present, terminal statuses included in the total), a full 6-role `AC3`/`UT03`/`QT05` 403 sweep, and 401. All 11 confirmed Red for the right reason (`Cannot find module './route'`). Two genuine test-fixture bugs caught and fixed along the way, not false Reds: (1) the `createUsers` helper didn't supply the schema-required `managerId` for Employee-role fixtures; (2) fixing that naively by auto-creating a manager per Employee call silently inflated the `Manager` role count in the one test that specifically asserts exact role counts — fixed by using one explicit shared manager for that test instead of relying on auto-creation. Implemented `src/app/api/admin/dashboard/route.ts`: `withAuthorization("admin.transferMonitoring", ...)` — the exact action `rbac-api-security.T05`'s permission matrix already defined for this purpose — then real aggregation counts (`User.countDocuments`/`TransferRequest.countDocuments`) against `user-management-console`'s and `internal-transfer-workflow`'s already-Merged collections, both genuinely wired, nothing stubbed. 415/415 tests passing project-wide, `tsc`/`eslint` clean. `T01` checked off as Green, awaiting Gate 2.
- **Gate 2 drafted and Merged for `transfer-admin-oversight.T01`.** Full Gate 2 + Security checklist, all Pass, zero Blocking findings. One routine Note (no commit exists yet). Recorded in `.ai-context/gate-reviews/gate2-evidence-transfer-admin-oversight.md` (new file). Test Reviewer confirmed no changes requested. `transfer-admin-oversight.T01` is Merged. `T02` (`GET /admin/transfer-requests`) is unblocked.
- **`transfer-admin-oversight.T02` Red→Green.** New `src/app/api/admin/transfer-requests/route.test.ts` (9 tests) covering `AC4`/`UT04` (every request returned with `id`/`status`/`employeeId`/`submittedAt`, plus the empty-list case), `AC5`/`UT05`/`QT07` (full 6-role 403 sweep, extending `QT07`'s listed 5 roles with Employee for completeness), and 401. All 9 confirmed Red for the right reason (`Cannot find module './route'`) and passed cleanly on the first Green attempt. Implemented `src/app/api/admin/transfer-requests/route.ts` — a plain, unfiltered `TransferRequest.find()`, reusing `T01`'s same `admin.transferMonitoring` action. `QT06` (behavior at scale, pagination) correctly left as an Open QA Question, per this task's own explicit instruction not to invent a page size or cursor scheme. 424/424 tests passing project-wide, `tsc`/`eslint` clean. `T02` checked off as Green, awaiting Gate 2.
- **Gate 2 drafted and Merged for `transfer-admin-oversight.T02`.** Full Gate 2 + Security checklist, all Pass, zero Blocking findings. Two non-blocking Notes (no pagination, by design; no commit exists yet). Recorded in `.ai-context/gate-reviews/gate2-evidence-transfer-admin-oversight.md`. Test Reviewer confirmed no changes requested. `transfer-admin-oversight.T02` is Merged. `T03` (`GET /admin/transfer-requests/{id}`) is unblocked — the last task in this spec.
- **`transfer-admin-oversight.T03` Red→Green — the last task in this spec.** New `src/app/api/admin/transfer-requests/[id]/route.test.ts` (12 tests) covering `AC6`/`UT06`/`QT08` (full field set — `id`, `status`, and "request fields per `internal-transfer-workflow.API01`'s payload" i.e. `departmentId`/`location`/`jobRoleId`/`effectiveDate`/`reason` — plus `actionHistory`, not a partial projection; the empty-`actionHistory` case; actor-to-username fallback for an orphaned `actorId`), `AC7`/`UT07`/`QT09` (full 6-role 403 sweep, explicitly including the request's own Employee — no ownership exception at all, unlike `internal-transfer-workflow.API02`), `AC8`/`UT08`/`QT10` (404 for a well-formed missing id and for a malformed id, deferring the 404-vs-400 question to the same Open QA Question already tracked in `transfer-audit-trail.test_cases.md`'s `QT06` rather than re-litigating it), and 401. All 12 confirmed Red for the right reason (`Cannot find module './route'`) and passed cleanly on the first Green attempt. **Extracted a new shared helper, `src/services/audit/resolveActionHistory.ts`**, once this became the third call site needing the identical "batch-resolve `AuditLog` entries' `actorId`s to `username`s" logic (after `transfer-audit-trail.T03` and `internal-transfer-workflow.T03`, both of which explicitly flagged in their own Gate 2 reviews that a third consumer would justify this) — deliberately did **not** retrofit those two already-Merged files from other tasks/specs to use the new helper, since that would be more than this task's own scope per this project's one-task-one-prompt discipline; flagging that consolidation as a worthwhile, explicitly deferred follow-up rather than doing it silently as a side effect here. Also flagged, rather than silently picked, exactly which fields "request fields (per `internal-transfer-workflow.API01`'s payload)" means — initially implemented as a literal reading (API01's 5 submitted-payload fields only). **Resolved during Gate 2, before Merge:** user clarified the Admin detail view should also include `assignedManagerId` and `submittedAt` on top of those 5 fields plus `id`/`status`/`actionHistory` (task statuses, `newManagerId`, `completedAt`, decision reasons remain omitted — not asked for). Updated `route.ts` and the exact-shape test assertion accordingly, re-confirmed 12/12 in this file and 436/436 project-wide, `tsc`/`eslint` clean. Implemented `src/app/api/admin/transfer-requests/[id]/route.ts`: direct `authenticateRequest` + `checkPermission(role, "admin.transferMonitoring")` (not `withAuthorization`, since that helper's handler signature doesn't thread a dynamic-route `context.params` through — matching the same pattern `internal-transfer-workflow.T03`'s route already uses for the same reason), the project's standard `Types.ObjectId.isValid()` 404 guard, then the new shared `resolveActionHistory()` helper for `actionHistory`. `T03` checked off as Green, awaiting Gate 2 verdict.
- **Introduced a new `.ai-context/changed_requests/` directory and artefact type**, at the user's explicit direction, to carry Gate 2 Finding 2 forward (the actor-to-username resolution logic is now duplicated across three call sites — `transfer-audit-trail.T03`, `internal-transfer-workflow.T03`, and the new `transfer-admin-oversight.T03` — but consolidating the two older, already-Merged files is out of this task's own scope per the one-task-one-prompt rule). This methodology doesn't define a "change request" artefact by that name — the closest existing fits (hotfix/bug-fix spec, ADR) didn't apply since this isn't a defect and isn't ADR-significant — so a new lightweight, spec-styled file was created instead of stretching an existing type to cover it: `.ai-context/changed_requests/consolidate-action-history-resolution.change_request.md`, Status `Proposed`, not gated through any Gate 1/Gate 2 review itself. Cross-referenced from `transfer-admin-oversight`'s Gate 2 evidence.
- **Gate 2 drafted and Merged for `transfer-admin-oversight.T03`.** Full Gate 2 + Security checklist, all Pass. Finding 1 (response field set) was raised and resolved within the same review cycle before Merge — see above. Finding 2 (DRY: actor-to-username resolution logic now duplicated across three call sites) filed as a new `.ai-context/changed_requests/consolidate-action-history-resolution.change_request.md`, Status `Proposed`, not blocking this Merge. One routine Note (no commit exists yet). Recorded in `.ai-context/gate-reviews/gate2-evidence-transfer-admin-oversight.md`. Test Reviewer confirmed no changes requested. **`transfer-admin-oversight.T03` Merged — all 3 tasks in this spec are now Merged.** Spec Status moved `In Development` → `In QA` — verified directly against the spec file before recording this, not assumed from a subset (per the earlier lesson from the premature "every spec is In QA" claim).
- **Authored `BRD-008: Admin Panel UI and Stakeholder Panel UI`**, at the user's explicit decision to revisit the previously-deferred UI gap (`discovery-analysis.md`'s 2026-09-19 entry). Investigated whether the two-new-specs, no-new-BRD-entry approach the user initially preferred was actually runnable and found `generate-spec.md`'s own Precondition 1 hard-blocks it (requires a headed `BRD-<NNN>` entry as its literal argument; no such entry existed for the UI). Surfaced that blocker rather than routing around it; user chose to author `BRD-008` to satisfy the real precondition. One entry, not two — covers both panels, with a Notes field explaining the expected (not fixed) two-spec decomposition, mirroring `BRD-004`/`BRD-005`'s "read together, decompose separately" precedent. `BRD.md`'s Coverage Note updated to scope §8's "did not translate into a BRD entry" claim to the *remaining* deliverables (APIs, schema, deployment) now that the UI is carved out. `discovery-analysis.md`'s standing open item updated to point at `BRD-008` instead of sitting as an orphaned "never converted" note, and its two sub-questions (spec split, login/session UI) added to the Open Questions Carried Forward table. Discovery Gate Status line updated from "all seven entries" to "all eight." No spec drafted yet — that's the next step, via `@generate-spec.md for BRD-008`.
- **Ran `@generate-spec.md for BRD-008`, producing two specs (the decomposition its own Notes field anticipated but didn't fix): `admin-panel-ui.spec.md` and `stakeholder-panel-ui.spec.md`, both `Status: Draft v1.0`.** Scoped each against `architecture.md`'s existing BRD-002/003/004 vs. BRD-001 module split. Neither exposes a new endpoint — each spec's "API Contract" section is a manifest of already-Approved contracts it consumes (`sdd-methodology.md` #11.2's "link, don't restate"), not a restatement of their payloads. Two real spec-drafting decisions made explicitly, not silently: (1) the shared Login screen (serves all 7 roles, including Admin) is owned by `stakeholder-panel-ui`, with `admin-panel-ui` explicitly deferring to it rather than duplicating a second login flow; (2) per `generate-spec.md`'s Precondition 2 (an unresolved open item that would require guessing to draft an AC), session token storage — left undecided by both BRD-008 and `ADR-0001-jwt-authentication.md` — was not silently invented; `stakeholder-panel-ui.spec.md`'s Explicitly Out of Scope instead states the most conservative default (in-memory only, Zustand, not persisted to `localStorage`) and flags it for confirmation before plan stage, the same "reasonable default + prominent flag" pattern used throughout this project's other genuinely open items. Both specs added to `status.md`'s Active Specs table, `Status: Draft v1.0 — awaiting Gate 1`. Next: Gate 1 review for each (a named human reviewer, not this agent).
- **Both previously-flagged open items in `stakeholder-panel-ui.spec.md` resolved by explicit user decision, same day.** (1) Login confirmed as one single shared screen for all roles — already how `AC1` was drafted, tightened to say so explicitly ("there is exactly one login endpoint — no separate Admin login screen exists"). (2) Session token storage decided: `localStorage` (not the earlier in-memory-only default), with `expiresAt` computed from `expires_in` at login, checked before any API call; expiry (or any 401, as a fallback since `ADR-0001` has no revocation mechanism) triggers a force-logout — new `AC2b`, plus 2 new Unit Test Cases. `admin-panel-ui.spec.md` updated in parallel with a new `AC10`/`UT10` cross-referencing the same mechanism rather than duplicating it, since Admin sessions expire the same way. Both specs' Context/Non-Functional Constraints sections updated to match (Zustand no longer described as holding the token itself; a plain, un-hedged Security Posture note added that `localStorage`'s XSS exposure is an accepted trade-off, not an oversight). `discovery-analysis.md`'s corresponding Open Questions row updated from "flagged, interim default" to "Resolved 2026-09-19." Both specs remain `Draft v1.0`, still awaiting Gate 1 — this was a pre-Gate-1 amendment, the same treatment given to `user-management-console`/`internal-transfer-workflow`'s own pre-Gate-1 amendments earlier in this project.
- **Discovered a real backend gap while answering "how many screens does `stakeholder-panel-ui` introduce": no endpoint anywhere lets Manager/HR/Payroll/IT/Facilities discover which request(s) need their action, and Employee has no way to list their own requests beyond one already-known `id` — `internal-transfer-workflow.API02` is Employee-owner-only, and `transfer-admin-oversight.API02`/`API03` are Admin-only.** This blocks `stakeholder-panel-ui.AC4`/`AC6`–`AC9` from having anything to navigate to. User confirmed: add a new task to the already-`In QA` `internal-transfer-workflow` spec (not silently reopen `transfer-admin-oversight`'s access rule, not deferred), scoped to cover all 6 non-Admin roles including Employee. **Reopened `internal-transfer-workflow.spec.md` from `In QA` to `In Development`** — a real, honest status regression given a new task now exists, not left mismarked. Added `API10` (`GET /transfer-requests/mine`, role-filtered, no client-supplied filter), `AC20`–`AC24`, `UT20`–`UT24`, and a new `Related` line crediting where the gap was found. Added `T10` to `tasks.md` and its own `prompts/internal-transfer-workflow.T10.prompt.md`.
- **`internal-transfer-workflow.T10` Red→Green.** New `src/app/api/transfer-requests/mine/route.test.ts` (9 tests) covering `AC20` (Employee sees every request they submitted, any status), `AC21` (Manager sees only their own assigned `Pending: Manager` requests, and excludes an assigned-but-differently-statused one), `AC22` (HR sees `Pending: HR` plus fully-task-completed `Pending: Payroll, IT, Facilities` requests, excluding a partially-completed one), `AC23` (Payroll/IT/Facilities each see only requests where their own task is still `Pending`, one test per role), `AC24` (403 for Admin), and 401. All 9 confirmed Red for the right reason (`Cannot find module './route'`) and passed cleanly on the first Green attempt. Implemented `src/app/api/transfer-requests/mine/route.ts`: a single `switch` on the caller's role selecting the appropriate Mongoose filter per `API10`'s contract, response shape reusing `transfer-admin-oversight.API02`'s base fields plus the three task-status fields HR's client needs to distinguish "needs decision" from "needs final mapping." 445/445 tests passing project-wide, `tsc`/`eslint` clean. `T10` checked off as Green, awaiting Gate 2 — once Merged, `internal-transfer-workflow.spec.md`'s Status returns to `In QA`.
- **Gate 2 drafted and Merged for `internal-transfer-workflow.T10`.** Full Gate 2 + Security checklist, all Pass, zero Blocking findings. One finding flagged for explicit reviewer awareness (the deliberate, honest spec reopening from `In QA`, not an error) plus one routine Note (no commit exists yet). Recorded in `.ai-context/gate-reviews/gate2-evidence-internal-transfer-workflow.md`. Test Reviewer confirmed no changes requested. **`internal-transfer-workflow.T10` Merged — all 10 tasks in this spec are now Merged.** Spec Status returned `In Development` → `In QA` — verified directly against the spec file before recording this, not assumed.
- **A real, plain gap found on request: neither `admin-panel-ui.spec.md` nor `stakeholder-panel-ui.spec.md` had a voluntary Logout action** — only session-expiry force-logout (`AC2b`) existed. Added `stakeholder-panel-ui.AC11`/`UT11` (Logout control available at all times, reuses `AC2b`'s exact clear-`localStorage`-and-redirect behavior, triggered voluntarily rather than by expiry/401) and `admin-panel-ui.AC11`/`UT11` cross-referencing it rather than re-implementing a second logout mechanism — same ownership pattern already used for `AC2b`/`AC10`. Both specs remain `Draft v1.0`, pre-Gate-1 amendment.
- **Gate 1 review completed for `admin-panel-ui` by Test Reviewer** — all 10 checklist items passed, verdict **Approved**. Recorded in `.ai-context/gate-reviews/gate1-review-admin-panel-ui.md`. Spec Status set `Draft v1.0` → `Approved`. Plan drafting may begin (`stakeholder-panel-ui` remains `Draft v1.0`, its own Gate 1 not yet requested).
- **Gate 1 review completed for `stakeholder-panel-ui` by Test Reviewer** — all 10 checklist items passed, verdict **Approved**. Recorded in `.ai-context/gate-reviews/gate1-review-stakeholder-panel-ui.md`. Spec Status set `Draft v1.0` → `Approved`. **Both `admin-panel-ui` and `stakeholder-panel-ui` are now Approved** — plan drafting may begin for either.
- **`admin-panel-ui.plan.md` drafted via `@generate-plan.md`.** No new datastore/schema — pure frontend consumption layer, per the spec's own scope. Architecture Approach commits to `int-standards.nextjs.md`'s feature-based `src/modules/admin-panel-ui/` structure (service layer, TanStack Query hooks, one file per consumed backend spec) and adds Formik+Yup for the three CRUD screens' forms — sourced from `int-standards.nextjs.md` #11, not a new plan-invented library choice, so no ADR triggered. Full Constitution Check against every applicable `constitution.md` rule, all ✓ or N-A with justification, none blindly ticked. **One real cross-plan dependency flagged prominently, then resolved by explicit user decision (not this agent's judgment call):** the shared route-guard/Logout mechanics (`AC8`–`AC11`) depend on session/token logic that is `stakeholder-panel-ui.spec.md`'s own contract. Presented 3 options (build the shared module here now; a throwaway local stub; plan `stakeholder-panel-ui` first) — user chose to build the real shared `src/shared/auth/` module as part of this plan's `T01`, implemented strictly to `stakeholder-panel-ui.AC1`/`AC2b`/`AC11`'s already-Approved contract. Plan's Architecture Approach, Sequencing, and Explicitly Deferred sections all updated to reflect this — Explicitly Deferred now correctly separates "the Login screen itself" (still `stakeholder-panel-ui`'s scope) from "the session module" (now this plan's `T01`). **Forward-pointer recorded above** so `stakeholder-panel-ui.plan.md` reuses rather than rebuilds this module when it's eventually drafted. 7-step Sequencing (1 shared layout+module + 6 screens). Spec Status moved `Approved` → `Plan Drafted`.
- **Moved application source under `src/`** (`app/` → `src/app/`, `services/` → `src/services/`) — realigns with what `application-constants-management.plan.md` and `internal-transfer-workflow.plan.md` already specified (`src/services/...`, `src/shared/...`); the flat layout for `T01` had only been a stopgap since the fresh scaffold had no `src/` yet. Updated the `@/*` alias in `tsconfig.json` and Jest's `moduleNameMapper` to point at `src/`. Verified clean after the move: `npx jest` (8/8), `npx tsc --noEmit`, `npx eslint .`, and `npx next build` all pass with no errors — the one `tsc` error seen mid-move was a stale `.next/types/validator.ts` cache artifact from before the move, resolved by the `next build` that followed.
- **Amended `stakeholder-panel-ui.spec.md` post-approval to reference `internal-transfer-workflow.API10`, found missing while about to draft this spec's plan.** `AC4`/`AC6`–`AC9` described what each screen shows once viewing a specific request, but never said how a user reaches it — `API10` (added earlier this session to close the same discovery gap for `internal-transfer-workflow`) was never wired back into this spec's own Consumed table or ACs. Presented 3 options (amend now and keep Approved; formal Draft v1.1 revision + fresh Gate 1; leave the spec silent and resolve only at plan stage) — user chose to amend now, keep Status `Approved`, treating this the same as a Gate-2-style "raised-and-resolved" finding rather than a content dispute needing a full revision cycle. Added `API10` to the Consumed table, a new `AC2c` (the "My Requests"/"Pending Actions" list screen, the sole navigation path into `AC4`/`AC6`–`AC9`), one new `UT02d`, and a Context note explaining the post-approval amendment for any future reader. Spec Status remained `Approved` through the amendment.
- **`stakeholder-panel-ui.plan.md` drafted via `@generate-plan.md`, after the `API10` amendment above.** No new datastore/schema — pure frontend consumption layer. Architecture Approach explicitly **fulfills the forward-pointer** from `admin-panel-ui.plan.md`: this plan's Login/Logout tasks reuse the already-planned shared `src/shared/auth/` module rather than rebuilding session mechanics a second time. One adaptive request detail/action screen (`src/app/(stakeholder)/transfer-requests/[id]`) serves `AC4`–`AC9` — a single screen that varies its rendered action panel by viewer role and request status, not five near-duplicate routes, since they all read the same `API02`-shaped data. `managersLookupService.ts` (for `AC9`'s Manager selector) deliberately kept local to this module rather than cross-importing `admin-panel-ui`'s own `usersService.ts`, to avoid a second cross-panel coupling beyond the one already-acknowledged shared auth module. Full Constitution Check, all ✓ or N/A with justification. 8-step Sequencing. Spec Status moved `Approved` → `Plan Drafted`. **Both `admin-panel-ui` and `stakeholder-panel-ui` are now `Plan Drafted`** — next step for either is `@generate-tasks.md`.
- **`admin-panel-ui.tasks.md` + 8 per-task prompt files generated via `@generate-tasks.md`.** The plan's 7-step Sequencing produced 8 tasks: Sequencing step 1 ("shared module + layout/guard/logout") was split into `T01` (the shared `src/shared/auth/` module — plain-Jest-testable logic, no UI) and `T02` (`AdminPanelLayout`/route guard/Logout control, RTL-testable, consuming `T01`), per `generate-tasks.md`'s own instruction to split any step bundling more than one independently-verifiable unit. `T03`–`T08` map 1:1 to the plan's remaining 6 steps (one per screen). `T01` cites `admin-panel-ui.AC10` as a representative-only citation (it implements `stakeholder-panel-ui`'s ACs, not a dedicated AC of its own here) — same treatment as `internal-transfer-workflow.T09`'s earlier representative-citation precedent. All 11 ACs (`AC1`–`AC11`) covered, Coverage Gaps section present and empty. Spec Status moved `Plan Drafted` → `Tasks Generated`. Next: Red tests for `T01`, via `@generate-tests.md for admin-panel-ui.T01`.
- **`stakeholder-panel-ui.tasks.md` + 11 per-task prompt files generated via `@generate-tasks.md`.** The plan's 8-step Sequencing produced 11 tasks: step 1 ("Layout + Login + Logout") split into `T01` (Login screen) and `T02` (layout/guard/logout), and step 7 ("three structurally distinct task-completion forms") split into `T08`/`T09`/`T10` (Payroll/IT/Facilities, one each) — both splits per `generate-tasks.md`'s instruction to split any step bundling more than one independently-verifiable unit; `AC8` is accordingly cited by all three of `T08`–`T10`, since the spec's own `AC8` covers all three roles as one criterion. **`AC2b` reported in Coverage Gaps, correctly, as not a real gap:** its mechanism lives entirely inside the shared `src/shared/auth/` module, which by explicit prior agreement was built under `admin-panel-ui.T01` (a different spec's task, implementing this spec's own Gate-1-approved contract) — no duplicate task generated here for it. All other ACs (`AC1`–`AC11` except `AC2b`) covered by at least one task. Spec Status moved `Plan Drafted` → `Tasks Generated`. **Both `admin-panel-ui` and `stakeholder-panel-ui` are now `Tasks Generated`** — Red-first implementation can start on either.
- **`admin-panel-ui.T01` Red confirmed via `@generate-tests.md`.** New `src/shared/auth/session.test.ts` (7 tests — corrected here from an initial miscount of 8 in this same entry, verified against the actual file with `grep -c`, jsdom environment via a per-file `@jest-environment` pragma — the project's first purely browser/localStorage-oriented test file, default Jest environment stays `node` for everything else) covering: `persistSession`/`readSession` storing the token and computing `expiresAt` from `expires_in`, plus decoding `role` from the stored token (`stakeholder-panel-ui.AC1` support); `readSession` returning `null` with no session ever stored (`stakeholder-panel-ui.AC10`/`admin-panel-ui.AC9` support); `isSessionExpired` true/false against a future/past `expiresAt`, and true with no session at all — this last case flagged as a boundary scenario not literally backed by an AC but a necessary support case for the guard's "no session" branch, written rather than silently skipped since it's directly implied by "no authenticated session" (`AC9`/`AC10`)'s own state; `clearSession` removing both stored values. **Real coverage gap reported, not filled in:** no `test_cases/admin-panel-ui.test_cases.md` or `test_cases/stakeholder-panel-ui.test_cases.md` exists yet — `@generate-test-cases` has never been run for either of these two specs, so only the specs' own Unit Test Cases tables were available as the acceptance-level minimum; QA-expanded scenarios for both remain unwritten, flagged rather than fabricated. All 7 tests confirmed Red for the right reason (`Cannot find module './session'`). `T01` checked off with Red confirmed, awaiting Green.
- **`admin-panel-ui.test_cases.md` generated via `@generate-test-cases.md`**, closing the coverage gap flagged during `T01`'s Red confirmation. 22 QA-expanded rows (`QT01`–`QT22`) across all 11 ACs: zero/large-count and empty-list rendering, API-failure error states, a full 6-role sweep for `AC8` (the spec's own `UT08` only demonstrated Employee), corrupted-token/corrupted-session boundary cases for `AC8`/`AC9`, and cross-references to two already-known, still-open project gaps rather than re-litigating them fresh (the username/name case-sensitivity ambiguity recurring across multiple specs; `user-management-console`'s tracked `role: "Admin"` guard gap; `org-structure-management`'s undecided delete-referential-integrity behavior). 7 genuinely ambiguous scenarios recorded under Open QA Questions rather than guessed — including 3 that route back to `stakeholder-panel-ui.spec.md` since they're actually about the shared session module (`AC2b`/`AC11`), not this spec's own contract. No executable test code written — that's `@generate-tests.md`'s job, not this workflow's.
- **`stakeholder-panel-ui.test_cases.md` generated via `@generate-test-cases.md`.** 23 QA-expanded rows (`QT01`–`QT23`) across all 13 ACs (including `AC2b`/`AC2c`): a full 5-role login sweep for `AC1` (the spec's own `UT01`/`UT01b` only demonstrate Employee and Admin), a 429-rate-limit scenario `AC2` never addressed (only 401), empty-state sweeps per role for `AC2c`, and cross-references rather than re-derivations for two already-known project ambiguities (`internal-transfer-workflow.T08`'s deliberately-left-open withdraw/approve race condition; the recurring whitespace/case-sensitivity rule gap). **This is the actual owning spec for `AC2b`/`AC11`**, so the 3 Open Questions `admin-panel-ui.test_cases.md` cross-referenced here (expiry boundary instant, cross-tab logout propagation, logout mid-mutation) are recorded here as their real home, not just pointed at. 10 Open QA Questions total, none guessed.
- **`admin-panel-ui.T01` Green.** Implemented `src/shared/auth/session.ts`: `persistSession(token, expiresInSeconds)` (writes token + computed `expiresAt` to `localStorage`), `readSession()` (returns `{token, role, expiresAt}` or `null`, decoding `role` from the JWT payload client-side — a pure UX read, no signature verification, never a trust boundary per the code's own comment), `isSessionExpired()` (true if no session or `expiresAt` has passed), `clearSession()` (removes both stored values). All 7 Red tests now pass; 452/452 tests project-wide, `tsc`/`eslint` clean. `T01` checked off as Green in `tasks.md`, awaiting Gate 2. `admin-panel-ui.spec.md`'s Status moved `Tasks Generated` → `In Development` (its first task went Green), matching this project's standing convention. `T02` (`AdminPanelLayout`/guard/logout, consuming this module) and `stakeholder-panel-ui.T01`/`T02` (which also depend on this same module) are now unblocked.

### 2026-09-20
- **Gate 2 drafted for `admin-panel-ui.T01`.** Verified against the 3 `stakeholder-panel-ui` ACs it actually implements (`AC1`/`AC2b`/`AC11`), per the task's own prompt file, not just the representative `admin-panel-ui.AC10` citation. **A real defect was found and fixed within this same review, before Merge:** while checking coverage against this session's own `test_cases.md` scenarios (`admin-panel-ui.QT17`/`QT19`, `stakeholder-panel-ui.QT22` — corrupted token/session handling), `decodeRole` was found to throw on a malformed token instead of degrading to "no session," and a non-numeric `expiresAt` compared as `NaN` — always `false` — meaning a corrupted session was silently treated as *never* expired (fail-open). Confirmed the bug directly with a throwaway probe script before touching the real test file. Added 3 new Red tests to `session.test.ts` (confirmed failing against the original implementation — 2 threw, 1 returned the wrong boolean), then fixed `session.ts`: `decodeRole` now returns `null` on any parse failure instead of throwing; `readSession` treats a `null` role or `NaN` `expiresAt` as "no session"; `isSessionExpired` now correctly fails safe (`true`) for corrupted storage. 10/10 tests passing, 455/455 project-wide, `tsc`/`eslint` clean. Recorded in `.ai-context/gate-reviews/gate2-evidence-admin-panel-ui.md` (new file) as raised-and-resolved within this review cycle, not silently fixed and erased. No Blocking findings; one Note that this task's Green state is correct, tested infrastructure but not yet a complete user-visible behavior until `T02`/`stakeholder-panel-ui.T01`–`T02` consume it (by design, not a gap). Awaiting a named reviewer's verdict.
- **Gate 2 verdict recorded: `admin-panel-ui.T01` Merged (Test Reviewer, 2026-09-20).** No changes requested. `T02` (`AdminPanelLayout`, route guard, Logout control — the first actual UI component in this spec, consuming `T01`'s module) is unblocked, as are `stakeholder-panel-ui.T01` (Login screen) and `T02` (that panel's own layout/guard/logout), since both also depend on `T01`'s shared module.
- **`admin-panel-ui.T02` Red confirmed via `@generate-tests.md`.** One architectural decision made while scoping the tests, extending the same cross-panel-shared-module precedent `T01` already established: the "force-logout when any wrapped screen attempts an API call" behavior (`AC10`) needs an actual API-calling wrapper, not just the guard component itself (`AdminPanelLayout` renders once and doesn't itself make API calls) — built as `src/shared/auth/authenticatedFetch.ts`, alongside `T01`'s `session.ts`, since it's equally reusable by `stakeholder-panel-ui`'s own future screens, not siloed under `src/modules/admin-panel-ui/`. New `src/shared/auth/authenticatedFetch.test.ts` (4 tests): attaches the `Authorization` header and calls `fetch` on a valid session; skips the network call entirely and force-logs-out when `expiresAt` has passed or no session exists at all; force-logs-out on a `401` response even when the stored `expiresAt` was still valid (the `stakeholder-panel-ui.AC2b` fallback case). New `src/modules/admin-panel-ui/components/AdminPanelLayout.test.tsx` (9 tests, the project's first RTL component test file): no-session redirect (`AC9`/`UT09`); full 6-role non-Admin sweep via `it.each` (`AC8`/`UT08`/`QT16` — the spec's own `UT08` only demonstrated Employee); a corrupted-token case collapsing safely into the no-session path (`QT17`/`QT19`, confirming `T01`'s earlier bug fix holds end-to-end); the Admin happy path (children render, no redirect); Logout clears the session and redirects (`AC11`/`UT11`). **3 boundary/failure scenarios explicitly not tested, per `generate-tests.md`'s own instruction not to guess a genuinely ambiguous scenario:** the exact `expiresAt`-equals-now instant, cross-tab logout propagation, and logout-during-an-in-flight-mutation — all three are recorded as Open QA Questions in `test_cases.md` with no committed expected behavior, so no test asserts one. `QT18` (no-session deep-link to a nested route) is not separately tested either, noted as structurally covered by the guard component itself, which doesn't distinguish route depth. All 13 new tests confirmed Red for the right reason (`Cannot find module`). `T02` checked off with Red confirmed, awaiting Green.
- **User requested a check on whether test cases and executable tests exist across every spec.** Verified `.ai-context/test_cases/` has a matching file for all 9 specs — complete. Launched a background audit agent to check executable test-file coverage against every task's actual status and run the full suite. The audit surfaced two real, previously-live production defects (below) and one false alarm (it flagged `admin-panel-ui.T01` over a missing `authenticatedFetch.ts` — that file was always `T02`'s scope, already correctly Red per the entry above; `T01`'s actual Merged diff, `session.ts` alone, is genuinely complete).
- **Two real defects found and fixed, by explicit user decision to fix immediately rather than defer.** `rbac-api-security`'s `userLookup.ts` and `transfer-audit-trail`'s `transferRequestLookup.ts` were both deliberate stubs, explicitly tracked in `status.md` as "revisit once the blocking schema exists" — both blocking schemas Merged long ago, and both stubs were never revisited. Every real request to `POST /auth/login` and `GET /transfer-requests/{id}/audit-log` would have thrown in production; neither existing route test suite caught this because both mock the exact function that was broken. Added one new task per spec (`rbac-api-security.T07`, `transfer-audit-trail.T04`) rather than reopening the already-Merged `T02`/`T03` Gate 2 entries — same precedent as `internal-transfer-workflow.T10`. For each: wrote a new, **unmocked** test file hitting a real `mongoServer`-backed collection (`userLookup.test.ts`, `transferRequestLookup.test.ts`), confirmed Red against the stub's own thrown error, implemented the real Mongoose query, confirmed Green (one recurring test-fixture bug — Employee role requiring `managerId` — caught and fixed along the way in `userLookup.test.ts`), and reconfirmed each endpoint's own existing (mocked) route test suite still passes unmodified. 461/461 tests passing project-wide (455 prior + 6 new), `tsc`/`eslint` clean for both new/changed files (the 2 pre-existing `admin-panel-ui.T02` `tsc` failures are its own already-documented Red state, unrelated). **Caught and corrected my own error mid-edit:** briefly marked `rbac-api-security.T07` as `Merged` in `tasks.md` before any Gate 2 review or human verdict existed — reverted to `Green, awaiting Gate 2` immediately, consistent with this project's non-negotiable that only a named human can mark a task Merged. Both specs reopened `In QA` → `In Development` (a new unmerged task exists in each). Gate 2 drafted for both `T07` and `T04`, each flagging explicitly that this closes a real, previously-live defect, not a cosmetic one — both awaiting a named reviewer's verdict.
- **`admin-panel-ui.T02` Green — reached autonomously (dynamic loop tick) as a direct continuation of the already-established Red state, no new decisions required since the tests fully specified the behavior.** Implemented `src/shared/auth/authenticatedFetch.ts` (checks `isSessionExpired()` before sending; attaches `Authorization`; treats a `401` as a force-logout fallback) and `src/modules/admin-panel-ui/components/AdminPanelLayout.tsx` (guard on mount via `T01`'s `readSession()`, redirects non-Admin/no-session, renders a Logout control). **One new small module added along the way, `src/shared/auth/redirectToLogin.ts`:** extracted because jsdom 30's `Location` object resists the standard mocking pattern (reassigning `window.location` silently failed, so `authenticatedFetch.test.ts`'s original assertions on `window.location.href` were testing nothing) — isolating the actual navigation call into its own tiny, `jest.mock`-able module fixed the test properly rather than working around jsdom. **Two eslint findings fixed properly, not suppressed carelessly:** `react-hooks/set-state-in-effect` on `AdminPanelLayout`'s mount-check (justified inline: session state is `localStorage`-backed and must be adopted post-mount to avoid an SSR hydration mismatch, not something derivable at render time) and `@next/next/no-location-assign-relative-destination` on `redirectToLogin` (justified inline: this function exists specifically for non-React callers like `authenticatedFetch.ts` that have no `useRouter()` available) — both `eslint-disable` comments initially misplaced one line off from the flagged line (a `eslint-disable-next-line` only suppresses the literal next line, not "after this whole comment block") and corrected. 14 new tests, 475/475 project-wide, `tsc`/`eslint` both fully clean (verified `eslint`'s own exit code, not just absence of visible errors in truncated output). `T02` checked off as Green, awaiting Gate 2.
- **Gate 2 verdicts recorded: `rbac-api-security.T07` and `transfer-audit-trail.T04` both Merged (Test Reviewer, 2026-09-20).** No changes requested on either. Both real, previously-live production defects (stubs that had been unblocked for some time but never revisited) are now closed. Both specs returned `In Development` → `In QA` — verified directly against each spec file before recording this, not assumed. All 7 tasks Merged in `rbac-api-security`, all 4 in `transfer-audit-trail`.
- **Gate 2 verdict recorded: `admin-panel-ui.T02` Merged (Test Reviewer, 2026-09-20).** No changes requested. `T03` (Dashboard screen, the first real content screen in this spec) is unblocked next.
- **`admin-panel-ui.T03` Green — the first task in this spec that needed TanStack Query, which turned out to have never actually been installed despite being named as the required server-state mechanism in `constitution.md`'s own Architectural Constraints since project kickoff.** Installed `@tanstack/react-query` — not a new architectural decision (already approved project-wide), just its first real use. New `src/modules/admin-panel-ui/components/Dashboard.test.tsx` (4 tests) covering `AC1`/`UT01` (renders `userCounts`/`totalTransferRequests`/`statusBreakdown` from a mocked `fetchDashboard`), `QT01` (all-zero counts rendered as `0`s, not blank/omitted), a loading state before data resolves, and `QT03` (fetch rejection renders an error state, not a crash). Hit the same `jest.mock` + `@/` alias resolution issue documented earlier this project (moduleNameMapper resolves `@/...` before honoring a not-yet-existing module) — fixed with the same established pattern, a relative import path in `jest.mock(...)`. All 4 confirmed Red for the right reason (`Cannot find module '../services/dashboardService'`). Implemented `dashboardService.ts` (calls `T02`'s `authenticatedFetch`, not raw `fetch`, since this is an authenticated Admin-only read), `useDashboard.ts` (a thin `useQuery` wrapper), and `Dashboard.tsx`. **Also wired this up for real, beyond just the test:** added `src/shared/providers/QueryProvider.tsx` (mounted once at the root `layout.tsx`, since TanStack Query is the mandated mechanism for every future screen project-wide, not just this one) and the actual route file `src/app/(admin)/dashboard/page.tsx` composing `AdminPanelLayout` + `Dashboard`. Verified with more than the usual bar since this touches the shared root layout: 479/479 tests, `tsc`/`eslint` (project-wide, not just the new files) both clean, and a full `next build` confirming `/dashboard` compiles as a real route with no regressions to any other route. `T03` checked off as Green, awaiting Gate 2.
- **Gate 2 verdict recorded: `admin-panel-ui.T03` Merged (Test Reviewer, 2026-09-20).** No changes requested. `T04` (Transfer Request Monitoring list) is unblocked next.
- **`admin-panel-ui.T04` Green.** New `src/modules/admin-panel-ui/components/MonitoringList.test.tsx` (4 tests) covering `AC2`/`UT02` (renders 2 requests from a mocked `fetchTransferRequestsList`, each row linking to `/transfer-requests/{id}`), `QT04` (empty array → explicit empty-state message), a loading state, and `QT06` (fetch rejection → error state, not a crash). All 4 confirmed Red for the right reason (`Cannot find module '../services/monitoringService'`). Implemented `monitoringService.ts` (via `authenticatedFetch`, same pattern as `T03`), `useTransferRequestsList.ts`, and `MonitoringList.tsx` — a plain unfiltered table, no client-side filtering invented, per the spec's own Explicitly Out of Scope. Added the real route `src/app/(admin)/transfer-requests/page.tsx` composing `AdminPanelLayout` + `MonitoringList`. 483/483 tests, `tsc`/`eslint` (project-wide) both clean, `next build` confirms `/transfer-requests` compiles with no regressions. `T04` checked off as Green, awaiting Gate 2.
- **Gate 2 verdict recorded: `admin-panel-ui.T04` Merged (Test Reviewer, 2026-09-20).** No changes requested. `T05` (Transfer Request Detail screen) is unblocked next.
- **`admin-panel-ui.T05` Green.** New `src/modules/admin-panel-ui/components/TransferRequestDetail.test.tsx` (5 tests) covering `AC3`/`UT03`/`QT08` (renders every field plus every `actionHistory` entry, not a partial projection), `QT07` (empty `actionHistory` → explicit "No actions yet" state), a loading state, `QT09` (a `null` result — the service's own encoding of a 404, whether from a malformed or missing `id` — renders a distinct not-found state, not a crash), and a generic fetch-rejection error state. All 5 confirmed Red for the right reason (`Cannot find module './TransferRequestDetail'`). Extended `monitoringService.ts` with `fetchTransferRequestDetail(id)`, which returns `null` specifically for a `404` response (letting the component distinguish "not found" from other failures) and throws otherwise; added `useTransferRequestDetail.ts` and `TransferRequestDetail.tsx`. Added the real dynamic route `src/app/(admin)/transfer-requests/[id]/page.tsx` (an async Server Component awaiting `params`, per Next.js 16's dynamic-params convention already used by every backend route in this project), composing `AdminPanelLayout` + `TransferRequestDetail` — this is the link target `T04`'s `MonitoringList` rows already pointed to. 488/488 tests, `tsc`/`eslint` (project-wide) both clean, `next build` confirms `/transfer-requests/[id]` compiles with no regressions. `T05` checked off as Green, awaiting Gate 2.
- **Gate 2 verdict recorded: `admin-panel-ui.T05` Merged (Test Reviewer, 2026-09-20).** No changes requested. `T06` (User Management screen — the first CRUD/forms screen in this spec) is unblocked next.
- **`admin-panel-ui.T06` Green — the first task needing Formik/Yup, which turned out to have never actually been installed either, despite being named in `int-standards.nextjs.md` #11 as the required form library.** Installed `formik`/`yup` (not a new decision, already approved project-wide). **Two interpretive gaps found and resolved explicitly, not silently, before writing the test:** (1) the task's own "Acceptance" line cited `user-management-console.API01`–`API05`, but neither `AC4`/`AC5` nor the task's own Scope bullets ever mention `API05` (the detail-profile view) — the Scope literally lists only create/edit/delete/list. Treated as an imprecise range citation from task-generation time, not a real requirement; implemented without `API05`, corrected the citation in `tasks.md`. (2) `user-management-console.API02`'s payload is documented as `{ "role": "string" }`, with prose noting "and/or other account fields defined by this spec only (username/role)" — genuinely ambiguous whether `username` is also editable. Implemented Edit as role-only, matching the literal JSON example rather than the looser prose, since inventing a username-edit capability the concrete contract doesn't show would be the riskier assumption. New `src/modules/admin-panel-ui/components/UserManagement.test.tsx` (6 tests, using `userEvent` for real form interaction) covering `AC4`/`UT04` (lists every user), `AC5`/`UT05` (create submits the entered payload to `createUser`; a client-side Yup rule requires `managerId` when `role: "Employee"` is selected, mirroring `user-management-console.API01`'s own 400 rule rather than inventing a new one; a duplicate-username 4xx — the spec's own named example — is displayed on the form via Formik's `status`, not silently dropped; edit calls `editUserRole` with the new role; delete calls `deleteUser`). All 6 confirmed Red for the right reason (`Cannot find module '../services/usersService'`) and passed cleanly on the first Green attempt. Implemented `usersService.ts` (including a shared error-body-parsing helper handling this project's several different error-shape conventions), `useUsers.ts` (query + 3 mutation hooks, each invalidating the list on success), and `UserManagement.tsx`. Added the real route `src/app/(admin)/users/page.tsx`. 494/494 tests, `tsc`/`eslint` (project-wide) both clean, `next build` confirms `/users` compiles with no regressions. `T06` checked off as Green, awaiting Gate 2.
- **Proactive check, acting on `T06`'s own Gate 2 Note, before `T07` starts: are there other named-but-never-installed dependencies?** Found two more: `zustand` and `shadcn/ui`, both named in `constitution.md`'s Architectural Constraints since project kickoff, neither ever installed. Unlike TanStack Query/Formik, this wasn't a blocked-implementation gap — `T01`–`T06` already work without them (direct `readSession()` calls instead of a Zustand store; plain HTML elements instead of shadcn/ui components), all ACs passing. Flagged this explicitly as a real, quiet architecture-fidelity divergence rather than continuing silently. Presented 3 options (adopt from `T07` onward; retrofit `T01`–`T06` now; formally drop them from the approved stack) — user chose to adopt both starting `T07`, leaving already-Merged work as-is rather than reopening reviewed tasks for unplanned rework. Installed `zustand` and ran `npx shadcn@latest init -d` (non-interactive defaults: Next.js template, CSS variables, Tailwind v4 auto-detected) — created `components.json`, `src/components/ui/button.tsx`, `src/lib/utils.ts`, and updated `src/app/globals.css`/font config. Re-verified after this shared-file change: 494/494 tests, `tsc`/`eslint` (project-wide) both clean, `next build` succeeds with no regression to any existing route.
- **Gate 2 verdict recorded: `admin-panel-ui.T06` Merged (Test Reviewer, 2026-09-20).** No changes requested. `T07` (Department Management — the first screen to actually adopt Zustand and shadcn/ui, per the standing decision above) is unblocked next.
- **Found a real blocker before writing any test for `T07`: `org-structure-management.API05` (the Department detail view) was never actually built.** Confirmed directly against the code — `src/app/api/departments/[id]/route.ts` exports only `PATCH`/`DELETE`, no `GET` — matching that spec's own text, which marks the detail view "Not yet set — deferred" (BRD-004's "Optional" note, never resolved). `T07`'s own task scope asked for a detail view against this non-existent endpoint. Presented 3 options (skip the detail view and build list+CRUD only; build `API05` first as a new backend task; defer `T07` entirely and do `T08` first) — user chose to skip the detail view, matching what the backend actually supports. **Also resolved: Zustand was NOT force-fit into this task.** Re-examined the "adopt Zustand from `T07` onward" decision against this task's actual content and found no genuine global/cross-cutting state need in Department Management — its only local state (`editingId`, which row is being edited) is correctly `useState`, per `int-standards.nextjs.md`'s own "avoid unnecessary global state" rule; using Zustand for a single-component-local toggle would have violated that same rule. Flagged this explicitly rather than either silently skip it or contort the code to use Zustand just to comply mechanically — "adopt when needed," not "force into every task regardless of fit." **shadcn/ui genuinely adopted here, for real**, via `npx shadcn@latest add input label table -y` (Button was already generated at init) — `DepartmentManagement.tsx` is built entirely from `Button`/`Input`/`Label`/`Table` primitives, confirmed they render semantic HTML under the hood (Base UI, not Radix, per this project's `base-nova` preset) so RTL queries work unchanged. New `src/modules/admin-panel-ui/components/DepartmentManagement.test.tsx` (5 tests, `userEvent`-driven) covering `AC6`'s list/create/edit/delete, plus a duplicate-name 4xx (`org-structure-management.API01`'s own named `name_exists` example) displayed on the form via Formik's `status`. All 5 confirmed Red for the right reason (`Cannot find module '../services/orgStructureService'`) and passed cleanly on the first Green attempt. Implemented `orgStructureService.ts` (Departments portion only — Job Roles is `T08`'s own scope, per this task's explicit "Do not touch"), `useDepartments.ts`, and `DepartmentManagement.tsx`. Added the real route `src/app/(admin)/departments/page.tsx`. 499/499 tests, `tsc`/`eslint` (project-wide) both clean, `next build` confirms `/departments` compiles with no regressions. `T07` checked off as Green, awaiting Gate 2.
- **Gate 2 verdict recorded: `admin-panel-ui.T07` Merged (Test Reviewer, 2026-09-20).** No changes requested. `T08` (Job Role Management — the last task in this spec) is unblocked next.
- **`admin-panel-ui.T08` Green.** Confirmed directly against the code before writing the test, not just trusting the spec text: `src/app/api/job-roles/route.ts`'s 409 handler returns `"name_exists"` even for Job Roles (the already-known, previously-flagged discrepancy from `org-structure-management`'s own review — the spec's literal wording says "identical to API01, substituting `title` for `name`," which would imply `title_exists`, but the real implementation never made that substitution). Used the real value (`name_exists`) in the duplicate-title error test, not the spec's idealized-but-inaccurate `title_exists`. New `src/modules/admin-panel-ui/components/JobRoleManagement.test.tsx` (6 tests) covering `AC7`'s list/create/edit/delete, an explicit assertion that no per-row "view detail" action exists (no detail endpoint was ever built for this entity either — same reasoning as `T07`'s Department detail-view gap), and the duplicate-title 4xx display. All 6 confirmed Red for the right reason (`Cannot find module './JobRoleManagement'`) and passed cleanly on the first Green attempt. Extended the shared `orgStructureService.ts` with the Job Role portion (Department methods untouched, per this task's own "Do not touch"), added `useJobRoles.ts` and `JobRoleManagement.tsx` (reusing `T07`'s shadcn/ui `Button`/`Input`/`Label`/`Table` pattern), and the real route `src/app/(admin)/job-roles/page.tsx`. Re-ran `T07`'s own `DepartmentManagement.test.tsx` as a regression check since the shared service file changed — still passes unmodified. 505/505 tests, `tsc`/`eslint` (project-wide) both clean, `next build` confirms `/job-roles` compiles with no regressions — **all 6 screens in `admin-panel-ui` now exist as real routes.** `T08` checked off as Green, awaiting Gate 2 — the last task in this spec.
- **Gate 2 verdict recorded: `admin-panel-ui.T08` Merged (Test Reviewer, 2026-09-20).** No changes requested. **All 8 tasks in `admin-panel-ui` are now Merged — spec Status moved `In Development` → `In QA`**, verified directly against the spec file, not assumed. All 6 screens (Dashboard, Monitoring list, Request Detail, User Management, Department Management, Job Role Management) exist as real, working routes, sharing the `T01`/`T02` session/guard/logout infrastructure throughout.
- **`stakeholder-panel-ui.T01` Green — the first task in this spec.** New `src/modules/stakeholder-panel-ui/components/LoginForm.test.tsx` (3 tests) covering `AC1`/`UT01`/`UT01b` (valid Employee credentials persist the session and route to `/my-requests`, this panel's own landing screen — not yet built, matching the same "link a route ahead of its destination existing" pattern already used by `admin-panel-ui.T04`→`T05`; valid Admin credentials route to `admin-panel-ui`'s `/dashboard` instead) and `AC2`/`UT02` (a 401 shows an on-screen error via Formik's `status` and does not establish a session). Caught and fixed a real race-condition risk in my own first draft of these tests before running them: two assertions on `router.replace`/`readSession()` were written to run synchronously right after a button click, without waiting for the async mutation's `onSuccess` to actually fire — wrapped them in `waitFor` instead of leaving a flaky, order-dependent test. All 3 confirmed Red for the right reason (`Cannot find module '../services/authService'`) and passed cleanly on the first Green attempt afterward. Implemented `authService.ts` (plain `fetch`, deliberately not `authenticatedFetch` — there's no session yet to attach one), `useLogin.ts`, and `LoginForm.tsx` (Formik, deliberately no Yup schema — whether empty-field submission should be blocked client-side is `test_cases.md`'s own explicit, unresolved `QT02` Open Question, not guessed here). `onSuccess` calls `T01`'s (from `admin-panel-ui`) `persistSession`, then `readSession()` to decode the role and route accordingly — no session/token logic duplicated, per the standing cross-spec agreement. Added the real route `src/app/(stakeholder)/login/page.tsx` — deliberately not wrapped in any guard/layout, since it's the one screen reachable without a session. 508/508 tests, `tsc`/`eslint` (project-wide) both clean, `next build` confirms `/login` compiles with no regressions. `T01` checked off as Green, awaiting Gate 2. `stakeholder-panel-ui.spec.md`'s Status moved `Tasks Generated` → `In Development` (its first task went Green), matching this project's standing convention.
- **`QT02` resolved during `T01`'s Gate 2, before Merge: client-side empty-field validation should be introduced.** Added a Yup schema (`required` on `username`/`password`) to `LoginForm.tsx`; 2 new tests (blocked with both fields empty, blocked with only password empty — API never called in either case) added to `LoginForm.test.tsx`, confirmed Red against the original no-validation implementation before the fix. `test_cases.md`'s `QT02` row and Open QA Questions entry both updated to record the resolution, dated, not silently rewritten. Re-verified: 510/510 tests project-wide, `tsc`/`eslint` clean, `next build` unaffected. Gate 2 evidence updated in place.
- **Gate 2 verdict recorded: `stakeholder-panel-ui.T01` Merged (Test Reviewer, 2026-09-20).** No changes requested. `T02` (`StakeholderPanelLayout`, route guard, Logout control, reusing `admin-panel-ui.T01`'s shared session module) is unblocked next.
- **`stakeholder-panel-ui.T02` Green.** New `src/modules/stakeholder-panel-ui/components/StakeholderPanelLayout.test.tsx` (9 tests) covering `AC10`/`UT10` (no session → redirect, content not rendered; a corrupted stored session also redirects rather than crashing, confirming `admin-panel-ui.T01`'s earlier bug fix holds here too), a full 6-role happy-path sweep (Employee/Manager/HR/Payroll/IT/Facilities all render children — **no role restriction**, unlike `admin-panel-ui`'s guard, since this panel serves every non-Admin role equally, not just one), and `AC11`/`UT11` (Logout clears the session and redirects). All 9 confirmed Red for the right reason (`Cannot find module './StakeholderPanelLayout'`) and passed cleanly on the first Green attempt. Implemented `StakeholderPanelLayout.tsx`, reusing `admin-panel-ui.T01`'s `readSession`/`clearSession` and this project's shadcn/ui `Button`. **Hit the same `eslint-disable` directive-placement mistake as `admin-panel-ui.T02`** (a multi-line comment pushed the directive away from the flagged line) — caught via a real `eslint` exit-code check, not assumed clean from truncated output, and fixed the same way (directive on the line immediately before the code). 519/519 tests, `tsc`/`eslint` (project-wide, exit code 0) both clean, `next build` unaffected — no new route yet, since this is a wrapping component `T03` onward will consume, same pattern as `admin-panel-ui.T02`. `T02` checked off as Green, awaiting Gate 2.
- **Gate 2 verdict recorded: `stakeholder-panel-ui.T02` Merged (Test Reviewer, 2026-09-20).** No changes requested. `T03` ("My Requests"/"Pending Actions" list, over `internal-transfer-workflow.API10`) is unblocked next.
- **Found a real, previously-uncaught route collision before writing any code for `T03`.** `stakeholder-panel-ui.plan.md` and `admin-panel-ui.plan.md` both independently named `transfer-requests/[id]` as a route path — Next.js route groups (`(admin)`, `(stakeholder)`) don't affect the actual URL, so both would have resolved to the identical path, and `admin-panel-ui.T05` already Merged that exact URL for its own, different (Admin-only, read-only) detail screen. This was never caught when either plan was drafted, since their route structures were never cross-checked against each other. Presented 3 options (nest under `/my-requests/[id]`; use a neutral `/requests/[id]`; something else) — user chose `/my-requests/[id]`. Corrected `stakeholder-panel-ui.plan.md`'s Route structure line in place, dated, explaining what changed and why, rather than silently editing history. This affects `T03`'s own link targets now and will fix `T05`'s actual route later.
- **`stakeholder-panel-ui.T03` Green.** New `src/modules/stakeholder-panel-ui/components/MyRequestsList.test.tsx` (5 tests) covering `AC2c`: renders every item `internal-transfer-workflow.API10` returns, each linking to `/my-requests/{id}` (not the colliding `/transfer-requests/{id}`); an explicit test that no client-side filtering is applied (API10 already role-filters server-side); an empty-state message; a loading state; and an error state on fetch failure. All 5 confirmed Red for the right reason (`Cannot find module '../services/transferRequestsService'`) and passed cleanly on the first Green attempt. Implemented `transferRequestsService.ts` (via `authenticatedFetch`, same pattern as every `admin-panel-ui` service), `useMyRequests.ts`, and `MyRequestsList.tsx` (shadcn/ui `Table`). Added the real route `src/app/(stakeholder)/my-requests/page.tsx`, wrapped in `T02`'s `StakeholderPanelLayout`. 524/524 tests, `tsc`/`eslint` (project-wide) both clean, `next build` confirms `/my-requests` and `admin-panel-ui`'s `/transfer-requests/[id]` now coexist with no collision. `T03` checked off as Green, awaiting Gate 2.
- **Gate 2 verdict recorded: `stakeholder-panel-ui.T03` Merged (Test Reviewer, 2026-09-20).** No changes requested. `T04` (Submit Request, over `internal-transfer-workflow.API01`) is unblocked next.
- **Found a real, serious blocker before designing `T04`'s form: `GET /departments` and `GET /job-roles` are Admin-only, confirmed directly against the route code, not just the spec text.** `internal-transfer-workflow.API01` requires an Employee to submit `departmentId`/`jobRoleId`, but no non-Admin role had any way to read either list to populate the required dropdowns — never caught in any of `org-structure-management`'s, `internal-transfer-workflow`'s, or `rbac-api-security`'s own reviews. Checked the spec's own ACs first: `AC2`/`AC8` restrict only create/edit/delete to Admin; the list-view ACs (`AC6`/`AC12`) never state an Admin-only restriction at all — the 403 was an implementation choice stricter than anything actually specified. Presented 3 options (open the two GET endpoints to any authenticated role; add a new, narrower read endpoint instead; leave it flagged and hold `T04`) — user chose to open the existing endpoints. Added `org-structure-management.T03`: changed both `GET` handlers from `withAuthorization("admin.departmentRoleManagement", ...)` to a plain `authenticateRequest` check (mutations untouched, still Admin-only). **Deliberately changed two pre-existing, passing test assertions, not silently weakened:** each route's old single "responds 403 for a non-Admin caller" test was replaced with a full 6-role sweep asserting 200 — confirmed Red against the original implementation first (12 failures, `403` received where `200` expected), then Green after the fix. One transient test-infra flake observed and ruled out: a single full-suite run showed 24 failures with a 77s runtime (vs. the normal ~12s); an immediate re-run passed cleanly — treated as `mongodb-memory-server` resource contention on that one run, not a regression, since the suite has since passed cleanly multiple times. 534/534 tests, `tsc`/`eslint` (project-wide) both clean, `next build` unaffected. `org-structure-management.spec.md`'s Status moved `In QA` → `In Development` (a new unmerged task exists). Gate 2 drafted, explicitly flagging this as a genuine access-control widening deserving its own deliberate sign-off, not a routine Pass. `T03` checked off as Green, awaiting Gate 2 — `stakeholder-panel-ui.T04` itself has not been started yet; this was a necessary prerequisite fix, done before touching that task's own files.
- **Gate 2 verdict recorded: `org-structure-management.T03` Merged (Test Reviewer, 2026-09-20).** No changes requested. All 3 tasks in `org-structure-management` are now Merged; Status returned `In Development` → `In QA` — verified directly against the spec file, not assumed. `stakeholder-panel-ui.T04`'s own actual scope (the Submit Request form) is now unblocked.
- **`stakeholder-panel-ui.T04` Green.** New `src/modules/stakeholder-panel-ui/components/SubmitRequestForm.test.tsx` (10 tests) covering `AC3`: submits the entered fields (`departmentId`/`location`/`jobRoleId`/`effectiveDate`/optional `reason`) to `internal-transfer-workflow.API01`, redirecting to `/my-requests` on success; client-side required-field validation; a 400 validation error and a 404 (department/job-role not found) both displayed on the form via Formik's `status`, not silently dropped. Implemented `orgStructureLookupService.ts` — a new, minimal, read-only service kept local to `stakeholder-panel-ui` rather than reusing `admin-panel-ui`'s CRUD-focused `orgStructureService.ts`, same boundary already established for `managersLookupService.ts` in the plan. Department/Job Role dropdowns are populated from this service (now readable by any authenticated role, thanks to `org-structure-management.T03`); Location is populated directly from `src/shared/constants/referenceValues.ts`'s codebase-defined placeholder list, no network call. **Found and closed a second gap in the same task, before finalizing Gate 2:** this task's own title says "Employee-only," but the initial implementation rendered the form for any authenticated role — only the server's 403 would have caught a non-Employee, after they'd already filled out the whole form. Added 5 more Red tests (a full non-Employee role sweep expecting a restriction message and no form, plus an explicit Employee-renders-fine case), confirmed Red against the ungated version, then added a client-side `readSession()?.role !== "Employee"` check — explicitly documented as UX only, not a replacement for `API01`'s own real enforcement. Added the real route `src/app/(stakeholder)/transfer-requests/new/page.tsx` — confirmed via `next build` that it coexists cleanly with `admin-panel-ui`'s `/transfer-requests/[id]` (a static segment naturally takes priority over a dynamic one at the same level in Next.js, not a repeat of the earlier `/my-requests` collision). 544/544 tests, `tsc`/`eslint` (project-wide) both clean. `T04` checked off as Green, awaiting Gate 2.
- **Gate 2 verdict recorded: `stakeholder-panel-ui.T04` Merged (Test Reviewer, 2026-09-20).** No changes requested. `T05` (the adaptive request detail/action screen base — Employee own-status view + Withdraw, at `/my-requests/[id]`) is unblocked next.
- **`stakeholder-panel-ui.T05` Green.** Corrected this task's own prompt file's stale route reference (`/transfer-requests/[id]` → `/my-requests/[id]`, matching last turn's collision fix) before writing anything. New `src/modules/stakeholder-panel-ui/components/RequestDetail.test.tsx` (6 tests) covering `AC4`/`UT04` (renders status, action history, and pending stakeholders; an empty `actionHistory` shows an explicit "No actions yet" state) and `AC5`/`UT05` (Withdraw shown only while `Pending: Manager`, calling `withdrawRequest`; hidden for every other status), plus loading and error states. **Caught and fixed a real test-authoring ambiguity of my own, not an implementation bug:** an early assertion `screen.getByText(/Manager/)` matched both the "Pending: Manager" status text and the "Manager" pending-stakeholder list item — two elements, one query — fixed by scoping with `{ selector: "li" }` rather than loosening the assertion's intent. All 6 confirmed Red for the right reason (`Cannot find module './RequestDetail'`) before the fix, passed cleanly after. Extended `transferRequestsService.ts` with `fetchRequestDetail`/`withdrawRequest`; added `useRequestDetail.ts` (keyed by `id`) and `useWithdraw.ts` (invalidating both its own detail query and `useMyRequests` on success). Built `RequestDetail.tsx` as the explicit shared base `T06`–`T11` will plug role-specific action panels into, per the plan's "one adaptive screen" design. Added the real dynamic route `src/app/(stakeholder)/my-requests/[id]/page.tsx`. 550/550 tests, `tsc`/`eslint` (project-wide) both clean, `next build` confirms `/my-requests/[id]` coexists cleanly with `admin-panel-ui`'s `/transfer-requests/[id]`. `T05` checked off as Green, awaiting Gate 2.
- **Gate 2 verdict recorded: `stakeholder-panel-ui.T05` Merged (Test Reviewer, 2026-09-20).** No changes requested. `T06` (Manager Decision panel, plugging into `RequestDetail`'s shared base) is unblocked next.
- **Found a third serious architectural blocker before writing any code for `T06`, this one worse than the previous two: it contradicted an already Gate-1-Approved acceptance criterion, not just an over-restrictive implementation choice.** Re-read `internal-transfer-workflow.spec.md`'s `API02` contract directly rather than relying on memory: `AC19`'s literal text rejects with 403 *any* role other than the requesting Employee, no exception at all — confirmed against the real code (`src/app/api/transfer-requests/[id]/route.ts`), which carries an explicit comment that this is by design. But `stakeholder-panel-ui.T05`'s `RequestDetail.tsx` (the shared base every one of `T06`–`T11`'s role panels plugs into) calls exactly this endpoint unconditionally to render anything at all — so a Manager opening `/my-requests/{id}` to decide on a request would 403 before ever seeing an Approve/Reject button. `AC19`'s own test case (`UT19`) illustrates the concern as "Manager requests **another employee's** request," suggesting the intent was always to block cross-employee snooping, not the legitimately assigned stakeholder — but the AC's literal wording drew no such distinction, so this needed a real spec amendment, not a silent reinterpretation. Presented 3 options (amend `AC19` now, flag for Gate 1 re-confirmation; log as a `changed_request` and park until picked up; have the user confirm the carve-out directly as the accountable human) — user chose to amend now and flag for Gate 1 re-confirmation. **Amended `internal-transfer-workflow.spec.md`'s `AC19`**, dated, to carve out "the currently-relevant assigned stakeholder for that specific request," deliberately reusing `AC21`–`AC23`'s already-defined per-role eligibility rule rather than inventing a new one, and explicitly preserving `AC24`'s "no Admin carve-out" precedent. Updated `UT19` to clarify it tests an unrelated Manager, and added `UT19a` for the new carve-out case. **This amendment needs the Gate 1 reviewer's re-confirmation at the next review touchpoint — flagged here explicitly, not treated as self-approved.**
- **Added `internal-transfer-workflow.T11`** (plan Sequencing step 11, `tasks.md`, new prompt file) to implement the `AC19` carve-out — scoped to Manager only, since that's the only role with an active consumer right now; HR/Payroll/IT/Facilities get the identical treatment (reusing `AC22`/`AC23`) when `stakeholder-panel-ui.T07`–`T10` need it, rather than building all four role carve-outs speculatively in one task. **`T11` Green:** in the existing `route.test.ts`, repointed the pre-existing `UT19` test to use a genuinely unrelated Manager (it had previously, and incorrectly for the new rule, used the request's own assigned Manager to prove the 403 case) and added two new tests — the assigned Manager gets 200 while `Pending: Manager` (`UT19a`), and still gets 403 once the request has moved past that status (`assignedManagerId` alone isn't sufficient, status must match too). Confirmed Red against the original implementation first (new carve-out test failed, 403 received where 200 expected; all 18 other tests, including the repointed `UT19`, already passed). Implemented the carve-out directly in `route.ts`'s `GET` handler: an `isAssignedManagerOnPendingRequest` check (role is Manager, `assignedManagerId` matches, status is exactly `Pending: Manager`) alongside the existing owning-Employee check. 552/552 tests project-wide, `tsc`/`eslint` both clean. `internal-transfer-workflow.spec.md`'s Status reopened `In QA` → `In Development` (a new unmerged task exists, and its own `AC19` amendment awaits Gate 1 re-confirmation). `T11` checked off as Green, awaiting Gate 2. Gate 2 drafted in `gate2-evidence-internal-transfer-workflow.md`, explicitly flagging the still-open Gate 1 re-confirmation for the `AC19` wording change as a finding needing reviewer attention, separate from the code's own Pass. `stakeholder-panel-ui.T06`'s actual scope (the Manager Decision panel itself) is now unblocked — proceeding to it next.
- **`stakeholder-panel-ui.T06` Green.** Extended `transferRequestsService.ts` with `submitManagerDecision(id, { decision, reason? })` (calls `internal-transfer-workflow.API03`); added `useManagerDecision.ts` (invalidates both this request's detail query and `useMyRequests` on success, same pattern as `useWithdraw`). New `src/modules/stakeholder-panel-ui/components/ManagerDecisionPanel.tsx`: Approve is a plain `type="button"` handler bypassing Formik's validation entirely (no reason needed); Reject goes through a Yup-validated Formik `Form` (`reason` required only there), matching `API03`'s own conditional-required rule exactly rather than inventing a stricter or looser one. New `ManagerDecisionPanel.test.tsx` (5 tests): renders both controls; Approve calls the service with no `reason` key at all; Reject with an empty reason shows "Reason is required" and never calls the service; Reject with a reason calls the service with it; a rejected mutation promise renders its message via Formik's `status`. Extended `RequestDetail.test.tsx` (3 new tests, plus real `signToken`/`persistSession` login helpers added to the whole file, replacing no previous session mocking since none existed before): the panel renders for a Manager viewer while `Pending: Manager`; it does **not** render for the owning Employee (even though `Employee` is now the file's default logged-in role via `beforeEach`); it does not render once the request has moved past `Pending: Manager`. Wired `ManagerDecisionPanel` into `RequestDetail.tsx`, gated on `readSession()?.role === "Manager" && data.status === "Pending: Manager"` — **deliberately not a separate "is this viewer the assigned Manager" check**, since `T11`'s own carve-out already guarantees the fetch itself only succeeds for the assigned Manager; role + status is sufficient once that invariant holds, documented inline as such. All 8 new/changed tests confirmed Red for the right reason first (`Cannot find module './ManagerDecisionPanel'` for the new file; one wrong-rendering assertion for the new `RequestDetail` case) before implementing, then passed cleanly on the first Green attempt — no interpretive gaps found this task. 560/560 tests project-wide, `tsc`/`eslint` (project-wide, exit code 0) both clean, `next build` confirms `/my-requests/[id]` still compiles with every other route, no regressions. `T06` checked off as Green, awaiting Gate 2.
- **Gate 1 re-confirmed: `internal-transfer-workflow.AC19`'s 2026-09-20 amendment, Approved (Test Reviewer, 2026-09-20).** Transcribed the reviewer's verdict into `gate1-review-internal-transfer-workflow.md`'s new "Re-confirmation — AC19 amendment" section — scoped to the one AC change, not a full spec re-review, per this project's standing distinction between a fresh Gate 1 and a re-confirmation of an amendment to an already-Approved spec. Also corrected an oversight caught while recording this: `internal-transfer-workflow.spec.md`'s `Status` field had been left at `In QA` when `T11` was added — `status.md` described the reopening but the spec file itself was never actually edited to say so. Fixed to `In Development`, dated, with the same reopening-reason note `T10`'s own earlier reopening used. Updated both `gate2-evidence-internal-transfer-workflow.md`'s `T11` report (the "needs Gate 1 re-confirmation" finding marked resolved) and `gate2-evidence-stakeholder-panel-ui.md`'s `T06` report (its cross-reference to `T11`'s status updated) to reflect this. `T11`'s own Gate 2 code-review verdict remains the only outstanding sign-off before either `T11` or `T06` can Merge.
- **Gate 2 verdict recorded: `internal-transfer-workflow.T11` Merged (Test Reviewer, 2026-09-20).** No changes requested. All 11 tasks in `internal-transfer-workflow` are now Merged; Status returned `In Development` → `In QA` — verified directly against the spec file, not assumed. `stakeholder-panel-ui.T06`'s own Gate 2 verdict is the only remaining sign-off before that task can Merge.
- **Gate 2 verdict recorded: `stakeholder-panel-ui.T06` Merged (Test Reviewer, 2026-09-20).** No changes requested. `T07` (HR Decision panel, over `internal-transfer-workflow.API04`) is unblocked next — flagged in the Gate 2 evidence as likely to need its own `API02` carve-out for HR (reusing `AC22`, per `T11`'s own deferred note), the same class of blocker `T06` hit for Manager, so this should be checked directly against the code before assuming `RequestDetail` already works for an HR viewer.
- **User asked to proactively check the same `API02` blocker class for all of `T07`–`T11`, plus any other dependency, before starting any of them.** Confirmed by reading every remaining action endpoint's own access rules directly (`hr-decision`, `payroll-task`, `it-task`, `facilities-task`, `hr-final-mapping`): all five are role-gated only (not person-specific, matching HR's existing non-routing pattern), each with its own status/task-status precondition. Cross-checked against `AC19`'s already-amended, generic wording and `AC21`–`AC23`'s eligibility rules — confirmed **`T07`/`T08`/`T09`/`T10` would all hit the identical `API02` 403 blocker `T06` hit**, one per role, with `T07`'s (HR) and `T11`'s (HR, different status branch) needs both satisfied by `AC22`'s single existing clause. **Found a second, separate, more serious blocker unique to `T11`:** its already-approved scope needs a Manager selector via `user-management-console.API04` filtered to `role: "Manager"`, but the real `GET /users` implementation hardcodes `{ role: "Employee" }` for every HR caller with zero query-parameter support — HR can never retrieve a Manager record through this endpoint at all. Confirmed this is backed by an explicit, already-Approved AC (`user-management-console.AC13`: HR rejected 403 from viewing any non-Employee record), not just an over-cautious implementation choice — the same category of conflict as `internal-transfer-workflow.AC19` was for `T11`'s Manager carve-out. Presented both findings with resolution options — user chose to build all three remaining `API02` role carve-outs together in one `internal-transfer-workflow` task (rather than one per `stakeholder-panel-ui` task), and to amend `AC13` with a narrow carve-out (rather than a separate endpoint), same process as `AC19`.
- **Added `internal-transfer-workflow.T12`** (plan Sequencing step 12, `tasks.md`, new prompt file): HR (`AC22`'s rule, covers `T07` and `T11`) and Payroll/IT/Facilities (`AC23`'s rule, one clause each) `API02` carve-outs. No further spec amendment needed — `AC19`'s already-amended, already-Gate-1-re-confirmed text already generically covers these roles. **`T12` Green:** 5 new tests in the existing `route.test.ts` (no existing assertions changed this time, only additions) — HR 200 at `Pending: HR` and 403 at `Pending: Manager`; HR 200 once all 3 tasks `Completed` and 403 with only 2 of 3; a role-swept 200/403 pair each for Payroll/IT/Facilities proving their own task-status condition is real, not just "any request of this role." Confirmed Red first (exactly the 5 new positive cases failed 403≠200; all 25 existing tests, including `T11`'s own, already passed), then Green. Implemented as four named booleans (`isEligibleHr`/`isEligiblePayroll`/`isEligibleIt`/`isEligibleFacilities`) combined into one `isEligibleViewer`, same style as `T11`'s own two-boolean pattern. `internal-transfer-workflow.spec.md`'s Status reopened `In QA` → `In Development` a third time this session (via `T10`, `T11`, now `T12`) — each reopening is a real, honest status regression by design, not an error, and returns to `In QA` once each task Merges. 574/574 tests project-wide (matches the count after `T07` below, run together), `tsc`/`eslint` both clean, `next build` clean. `T12` checked off as Green, Gate 2 drafted, awaiting a named reviewer's verdict.
- **Amended `user-management-console.spec.md`'s `AC13`**, dated, carving out HR's ability to request the list view (`API04`) filtered to exactly `role=Manager` — returning the endpoint's existing minimal `{id, username, role}` shape, no new fields, no `API05` detail-view access granted, and no other `role` value permitted for HR. Updated `API04`'s contract prose (documented the new optional `role` query parameter and its 400 exception) and added `UT16a`/`UT16b`. **This amendment needs Gate 1 reviewer re-confirmation, same process as `AC19`.** Added `user-management-console.T07`(plan Sequencing step 7, `tasks.md`, new prompt file). **`T07` Green:** 3 new tests in `route.test.ts` — HR with `?role=Manager` gets only Manager records; HR with any other explicit `role` value (tested `HR`) gets 400 naming `role` rather than silently falling back; Admin's response confirmed unaffected by the parameter (already passed before the change, a genuine regression guard). Confirmed Red first (2 of the 3 new tests failed for the right reason — wrong records returned, and 200 instead of 400 — the Admin-unaffected case correctly already passed), then Green. Implemented as one additional `role` query-parameter branch inside the existing HR/Admin filter logic in `src/app/api/users/route.ts`, an invalid value rejected with 400 before any query runs (never passed unvalidated into the Mongoose filter). `user-management-console.spec.md`'s Status reopened `In QA` → `In Development`. 574/574 tests project-wide, `tsc`/`eslint` (project-wide, exit code 0) both clean, `next build` confirms no regressions. `T07` checked off as Green, Gate 2 drafted (explicitly flagging the still-open `AC13` Gate 1 re-confirmation, mirroring `T11`'s own report structure), awaiting a named reviewer's verdict. **All three of `T07`–`T11`'s discovered blockers are now fixed and Green, pending Gate 1 re-confirmation (`AC13`) and Gate 2 verdicts (`internal-transfer-workflow.T12`, `user-management-console.T07`) before `stakeholder-panel-ui.T07` itself can actually start.**
- **Gate 1 re-confirmed: `user-management-console.AC13`'s 2026-09-20 amendment, Approved (Test Reviewer, 2026-09-20).** Transcribed the reviewer's verdict into `gate1-review-user-management-console.md`'s new "Re-confirmation — AC13 amendment" section, scoped to the one AC change, same treatment as `internal-transfer-workflow.AC19`'s earlier re-confirmation. Updated the spec's own amendment note and `gate2-evidence-user-management-console.md`'s `T07` report (the "needs Gate 1 re-confirmation" finding marked resolved). `user-management-console.T07`'s own Gate 2 code-review verdict is now the only outstanding sign-off before it — and `internal-transfer-workflow.T12` separately, on its own Gate 2 — can Merge.
- **Gate 2 verdicts recorded: `internal-transfer-workflow.T12` and `user-management-console.T07` both Merged (Test Reviewer, 2026-09-20).** No changes requested on either. `internal-transfer-workflow` returns to `In QA` with all 12 tasks Merged; `user-management-console` returns to `In QA` with all 7 tasks Merged — both verified directly against their spec files, not assumed. All three blockers discovered while auditing `stakeholder-panel-ui.T07`–`T11` are now fully resolved and Merged: `T07`–`T10` each have their own `API02` carve-out (`T12`), and `T11`'s Manager-selector need is unblocked (`user-management-console.T07`). `stakeholder-panel-ui.T07` (HR Decision panel) is unblocked next, with no known remaining dependency.
- **Gate 2 verdicts recorded: `internal-transfer-workflow.T12` and `user-management-console.T07` both Merged (Test Reviewer, 2026-09-20).**
- **`stakeholder-panel-ui.T07` Green — no new blocker found, since the prerequisite audit had already cleared everything.** Extended `transferRequestsService.ts` with `submitHrDecision(id, { decision, reason? })` (calls the hr-decision endpoint); added `useHrDecision.ts` (same invalidation pattern as `useManagerDecision`). New `src/modules/stakeholder-panel-ui/components/HrDecisionPanel.tsx`, structurally near-identical to `T06`'s `ManagerDecisionPanel.tsx` by design — Approve bypasses Formik validation, Reject is Yup-gated on a required reason, same as the server's own conditional-required rule. New `HrDecisionPanel.test.tsx` (5 tests, mirroring `T06`'s own test shape). Extended `RequestDetail.test.tsx` (3 new tests): the panel renders for HR while `Pending: HR`; not for the owning Employee; not once the request has moved to `Pending: Payroll, IT, Facilities`. Wired into `RequestDetail.tsx`, gated on `readSession()?.role === "HR" && data.status === "Pending: HR"` — deliberately narrower than `AC22`'s full HR eligibility window (which also covers the all-3-tasks-completed case), since that broader case belongs to `T11`'s own future Final Mapping panel, a different task; documented inline so this distinction isn't lost. All 8 new/changed tests confirmed Red for the right reason first, then passed cleanly on the first Green attempt. 582/582 tests project-wide, `tsc`/`eslint` (project-wide, exit code 0) both clean, `next build` confirms no regressions. `T07` checked off as Green, Gate 2 drafted, awaiting a named reviewer's verdict.
- **Gate 2 verdict recorded: `stakeholder-panel-ui.T07` Merged (Test Reviewer, 2026-09-20).** No changes requested. `T08` (Payroll task-completion form, over `internal-transfer-workflow.API05`) is unblocked next — its own `API02` carve-out was already cleared by `internal-transfer-workflow.T12`, so no new blocker is expected, but worth confirming `API05`'s actual payload contract directly against the code before assuming the spec's field list is accurate (per this project's standing practice of verifying rather than trusting spec prose alone).
- **`stakeholder-panel-ui.T08` Green — no new blocker; confirmed a pre-existing, already-documented server behavior before designing the form.** Checked `payroll-task/route.ts` directly: it performs zero payload validation (doesn't even parse the request body for content), already flagged as a deliberate design choice at `internal-transfer-workflow.T06`'s own Gate 2 ("no payload validation on any of the three endpoints, by design"). Built the client-side Yup validation anyway, for the real Payroll user's data-entry experience, not to satisfy a server contract that doesn't exist. Extended `transferRequestsService.ts` with `submitPayrollTask` (a discriminated-union payload type: `{action: "no_action_needed"}` or `{action: "update", salary, compensation, tax, costCenter}`); added `usePayrollTask.ts` (same invalidation pattern as the other decision hooks). New `PayrollTaskForm.tsx`: a `Field as="select"` action dropdown (matching `SubmitRequestForm.tsx`'s existing select pattern) toggling four conditionally-required fields via Yup's `.when()`. New `PayrollTaskForm.test.tsx` (5 tests) and 3 new `RequestDetail.test.tsx` cases (shown for Payroll while `Pending: Payroll, IT, Facilities`; hidden for the owning Employee; hidden once `Completed`). All 4 new/changed test files confirmed Red for the right reason first, then passed cleanly on the first Green attempt — 20/20 new/changed tests. Wired into `RequestDetail.tsx`, gated on `readSession()?.role === "Payroll" && data.status === "Pending: Payroll, IT, Facilities"` — same "the fetch itself already enforces eligibility" reasoning as the Manager/HR gates, documented inline. 590/590 tests project-wide, `tsc`/`eslint` (project-wide, exit code 0) both clean, `next build` confirms no regressions. `T08` checked off as Green, Gate 2 drafted, awaiting a named reviewer's verdict.
- **Gate 2 verdict recorded: `stakeholder-panel-ui.T08` Merged (Test Reviewer, 2026-09-20).** No changes requested. `T09` (IT task-completion form, over `internal-transfer-workflow.API06`) is unblocked next — its own `API02` carve-out already cleared by `internal-transfer-workflow.T12`; will confirm `API06`'s actual payload/validation behavior directly against the code before assuming it matches `API05`'s pattern.
- **`stakeholder-panel-ui.T09` Green — no new blocker; confirmed `API06` also does no server-side payload validation (same as `API05`), and hit a genuine first for this project: array-typed form fields.** `internal-transfer-workflow.API06`'s payload (`systemsAccess`/`permissions`/`devices`, each `string[]`) has no existing multi-value input precedent anywhere in the codebase. Chose the minimal option — one comma-separated text input per field, split/trimmed/empty-filtered into an array via a small named `toList()` helper — over a `FieldArray`-based repeatable-row UI, since the latter would be a heavier abstraction for a single, first-time consumer. Flagged this as an explicit judgment call in the Gate 2 evidence, not a silent choice, in case a richer input UX is wanted later. Extended `transferRequestsService.ts` with `submitItTask`; added `useItTask.ts` (same invalidation pattern). New `ItTaskForm.tsx` and `ItTaskForm.test.tsx` (4 tests, including a trailing-space-and-comma parsing case); 3 new `RequestDetail.test.tsx` cases (shown for IT while `Pending: Payroll, IT, Facilities`; hidden for the owning Employee; hidden once `Completed`). All new/changed tests confirmed Red for the right reason first, then passed cleanly on the first Green attempt — 22/22 new/changed tests. Wired into `RequestDetail.tsx`, same role+status gating pattern as `T06`–`T08`. 597/597 tests project-wide, `tsc`/`eslint` (project-wide, exit code 0) both clean, `next build` confirms no regressions. `T09` checked off as Green, Gate 2 drafted, awaiting a named reviewer's verdict.
- **Gate 2 verdict recorded: `stakeholder-panel-ui.T09` Merged (Test Reviewer, 2026-09-20).** No changes requested — the comma-separated-input judgment call accepted as-is. `T10` (Facilities task-completion form, over `internal-transfer-workflow.API07`) is unblocked next — its own `API02` carve-out already cleared by `internal-transfer-workflow.T12`; will confirm `API07`'s payload/validation behavior directly against the code first, same practice as `T08`/`T09`.
- **`stakeholder-panel-ui.T10` Green — no new blocker; confirmed `API07` also does no server-side payload validation, same pattern as `API05`/`API06`.** Unlike `T09`, `API07`'s fields (`workspace`/`officeLogistics`/`locationSetup`) are plain strings, so no comma-separated-input judgment call was needed this time — a straightforward three-field form, structurally closest to `T08`'s Payroll form minus the action toggle. Extended `transferRequestsService.ts` with `submitFacilitiesTask`; added `useFacilitiesTask.ts` (same invalidation pattern). New `FacilitiesTaskForm.tsx`/`FacilitiesTaskForm.test.tsx` (4 tests) and 3 new `RequestDetail.test.tsx` cases (shown for Facilities while `Pending: Payroll, IT, Facilities`; hidden for the owning Employee; hidden once `Completed`). All new/changed tests confirmed Red for the right reason first, then passed cleanly on the first Green attempt — 25/25 new/changed tests. Wired into `RequestDetail.tsx`, same gating pattern as `T06`–`T09`. 604/604 tests project-wide, `tsc`/`eslint` (project-wide, exit code 0) both clean, `next build` confirms no regressions. **All three of `AC8`'s roles (`T08`/`T09`/`T10`) are now complete.** `T10` checked off as Green, Gate 2 drafted, awaiting a named reviewer's verdict.
- **Gate 2 verdict recorded: `stakeholder-panel-ui.T10` Merged (Test Reviewer, 2026-09-20).** No changes requested. `AC8`'s three-role coverage is now complete. `T11` (HR Final Mapping panel — the last task in this spec) is unblocked next: both of its prerequisites (`internal-transfer-workflow.T12`'s HR `API02` carve-out, `user-management-console.T07`'s Manager-selector carve-out) are already Merged, so no new blocker is expected, but will re-verify both directly before building, consistent with this session's standing practice.
- **`stakeholder-panel-ui.T11` Green — the last task in this spec, no new blocker, but two judgment calls and one self-caught test bug worth recording.** First: `API02`'s response has no field at all indicating individual task-completion status, so there's no direct way for the frontend to check "all 3 tasks Completed" before showing this panel — resolved by relying on `internal-transfer-workflow.T12`'s own carve-out, which guarantees HR can only successfully fetch a `Pending: Payroll, IT, Facilities` request once all 3 tasks are already done; an HR viewer where even one task is still pending gets 403 before ever reaching this component. Gated the panel on `role === "HR" && status === "Pending: Payroll, IT, Facilities"` alone, same "the fetch itself enforces eligibility" reasoning used since `T06`, now explicitly load-bearing for a condition with no other available signal — flagged in Gate 2 for reviewer awareness. Second: `internal-transfer-workflow.API08`'s payload treats `newManagerId` as optional, but this panel requires a selection anyway since choosing a new manager is the entire point of `AC9` — also flagged. Built `managersLookupService.ts`/`useManagersList.ts` (mirroring `orgStructureLookupService.ts`'s exact local-read-only-service shape), extended `transferRequestsService.ts` with `submitHrFinalMapping`, added `useHrFinalMapping.ts`, and `HrFinalMappingPanel.tsx` (Formik+Yup, Manager `<select>` populated from the new list, required before submit). **Self-caught test-authoring bug, recorded not silently fixed:** 2 of the first 4 `HrFinalMappingPanel.test.tsx` tests initially failed for the wrong reason — asserting on the `<select>` via `findByLabelText` before the async manager list had actually populated its options — not the intended Red state; fixed by waiting for a specific `option` to appear first, then re-confirmed genuine Red (`Cannot find module`/missing button) before implementing. All new/changed tests passed cleanly after that — 28/28. 611/611 tests project-wide, `tsc`/`eslint` (project-wide, exit code 0) both clean, `next build` confirms no regressions. **All 11 tasks in `stakeholder-panel-ui` are now Green.** `T11` checked off as Green, Gate 2 drafted (both judgment calls and the self-caught test bug flagged explicitly), awaiting a named reviewer's verdict.
- **User, acting as the Test Reviewer during `stakeholder-panel-ui.T11`'s Gate 2 review, gave two directions:** (1) introduce a new `Pending: Transfer` status before `Completed` for HR's final step, resolving the flagged "no direct signal" gating concern; (2) confirmed the Manager-selection-required judgment call as correct, no change needed.
- **Added `internal-transfer-workflow.T13`** (plan Sequencing step 13, spec amendments to `AC13`/`AC14`/`AC18`/`AC22` plus new `AC25`, `tasks.md`, new prompt file) — the new `Pending: Transfer` status, set automatically by whichever of `payroll-task`/`it-task`/`facilities-task` is the last of the 3 parallel tasks to complete, via a new shared helper `allParallelTasksCompleted` (a genuinely warranted extraction — the same 3-field check would otherwise have been tripled across three handlers). `hr-final-mapping`'s own precondition simplified from checking all 3 task fields directly to checking `status === "Pending: Transfer"` — status is now the single source of truth. `API02`'s HR carve-out (`T12`) and `API10`'s HR branch (`AC22`) both simplified the same way, removing the now-redundant `allTasksCompleted` computation from each. `pendingStakeholdersFor` gained a new case (`Pending: Transfer` → `["HR"]`). **This amendment touches 4 existing ACs plus adds a new one — a larger content change than any single-AC carve-out earlier this session — flagged for Gate 1 re-confirmation same as before.**
- **`T13` Green.** Red confirmed across 7 test files (schema enum rejection; each of the 3 task-completion routes' new transition tests; `hr-final-mapping`'s precondition tests, including a new deliberately adversarial case proving status — not the task fields — is now authoritative; `[id]/route.test.ts`'s exhaustive status table and HR carve-out tests; `mine/route.test.ts`'s `AC22` fixture) — 13 failures, all for the right reason. **Self-caught test-authoring mistake, recorded not silently fixed:** the newly-added transition tests in the 3 task-completion route files initially used non-ObjectId placeholder strings (`"payroll-user"` etc.) as the caller id, which fails `AuditLog` validation once the call path reaches an actual save — unlike the same files' pre-existing 409-path tests, which never reach that code. Fixed by using `new Types.ObjectId().toString()`, matching the file's own existing convention. **Existing `hr-final-mapping/route.test.ts` fixtures deliberately updated to the new invariant, not silently left inconsistent:** success-path fixtures that previously constructed a request directly with `status: "Pending: Payroll, IT, Facilities"` and all 3 task fields already `Completed` (a state the new atomic-transition design should never actually produce) were updated to `status: "Pending: Transfer"`; 409-path fixtures correctly kept the old status. 620/620 tests project-wide, `tsc`/`eslint` (project-wide, exit code 0) both clean, `next build` confirms no regressions — including confirming `admin-panel-ui`'s Dashboard (which derives its `statusBreakdown` dynamically from `TRANSFER_REQUEST_STATUSES`) picks up the new status with zero code change needed there. `internal-transfer-workflow.spec.md`'s Status reopened `In QA` → `In Development` a fourth time this session. `T13` checked off as Green, Gate 2 drafted (the 4-AC/1-new-AC Gate 1 re-confirmation requirement flagged prominently), awaiting a named reviewer's verdict.
- **Revised `stakeholder-panel-ui.T11` in place, during its own still-open Gate 2 review, per the reviewer's direction above.** `RequestDetail.tsx`'s gate for `HrFinalMappingPanel` changed from the indirect `role === "HR" && status === "Pending: Payroll, IT, Facilities"` to the now-direct `role === "HR" && status === "Pending: Transfer"`. Updated `RequestDetail.test.tsx`'s three affected tests to the new status and added a fourth proving the panel stays hidden while parallel tasks are still in progress. All 25 tests in that file re-confirmed Green; full suite 621/621, `tsc`/`eslint`/`next build` all re-verified clean. Updated `T11`'s own Gate 2 evidence in place: the original "fetch already enforces eligibility" finding marked resolved via this revision, the Manager-selection judgment call marked confirmed-correct by the reviewer, and a new finding added noting `T11` now also depends on `internal-transfer-workflow.T13`'s own Merge (same "review/merge together" pattern as `T06`/`T11` and `T07`/`T12` earlier). `T11` remains Green (revised), awaiting a named reviewer's verdict — now effectively a joint decision alongside `T13`.
- **Gate 1 re-confirmed: `internal-transfer-workflow.AC13`/`AC14`/`AC18`/`AC22`'s amendments and new `AC25`, Approved (Test Reviewer, 2026-09-20) — transcribed as covering both the Gate 1 re-confirmation and the Gate 2 code-review verdict, since the user's message referred to the spec as a whole passing rather than citing a specific gate.** Recorded the Gate 1 re-confirmation in `gate1-review-internal-transfer-workflow.md`'s new section, scoped to this one amendment batch (the largest this spec has had — 4 amended ACs plus 1 new one).
- **Gate 2 verdict recorded: `internal-transfer-workflow.T13` Merged (Test Reviewer, 2026-09-20).** No changes requested. All 13 tasks in `internal-transfer-workflow` are now Merged; Status returned `In Development` → `In QA` — verified directly against the spec file. Updated `stakeholder-panel-ui.T11`'s own Gate 2 evidence to clear the merge-ordering dependency this task's Merge resolves — `T11` is now independently mergeable, pending only its own reviewer verdict.
- **Gate 2 verdict recorded: `stakeholder-panel-ui.T11` Merged (Test Reviewer, 2026-09-20).** No changes requested. **All 11 tasks in `stakeholder-panel-ui` are now Merged — spec Status moved `In Development` → `In QA`**, verified directly against the spec file, not assumed. Combined with `admin-panel-ui`'s own 8/8 tasks already Merged, **both UI specs for this project are now fully built out and in QA.** Every screen across both panels (Dashboard, Monitoring, Request Detail, User Management, Department/Job Role Management for Admin; Login, My Requests, Submit Request, adaptive Request Detail with role-specific Manager/HR/Payroll/IT/Facilities/HR-Final-Mapping panels for the Stakeholder panel) exists as a real, working, tested route.
- **User asked what the first screen would be when visiting the bare site URL.** Checked directly: `src/app/page.tsx` was still the unmodified `create-next-app` scaffold — no spec had ever claimed the root route, so there was no path at all from the bare site link into either panel. Presented a suggested approach (redirect based on session at `/`, plus the same already-authenticated check at `/login` itself, extracting the shared Admin-vs-everyone-else routing decision into one helper) before implementing, per this session's standing practice for design questions. User confirmed the approach and explicitly instructed: no BRD entry needed, record as a small task under `stakeholder-panel-ui` instead.
- **Amended `stakeholder-panel-ui.spec.md` with a new `AC12`** (root-route + already-authenticated-Login redirect) and `UT12`/`UT12b`/`UT12c`/`UT12d`, framed explicitly as a small extension of `BRD-008`'s already-resolved session/redirect behavior rather than a new capability, per the user's own instruction not to touch the BRD. Added `stakeholder-panel-ui.T12` (plan Sequencing step 9, `tasks.md`, new prompt file). Reopened spec Status `In QA` → `In Development`.
- **`T12` Green.** New `src/shared/auth/homeRouteForRole.ts` — a small, genuinely warranted extraction of the Admin-vs-everyone-else decision that was about to exist in two places. Rewrote `src/app/page.tsx` (previously the literal Next.js scaffold) to check the session on mount and redirect to `/login` (no/expired session) or the role's home page (valid session) — new `page.test.tsx`, 4 tests including an explicit expired-session case. Updated `LoginForm.tsx` to use the shared helper for its own existing post-login redirect, and added a new already-authenticated-on-mount check redirecting away from the form entirely — 2 new tests in `LoginForm.test.tsx`, its pre-existing fresh-login tests re-confirmed passing unmodified. All 11 new/changed tests confirmed Red for the right reason first (against the literal scaffold / no existing check), then passed cleanly on the first Green attempt. 627/627 tests project-wide, `tsc`/`eslint` (project-wide, exit code 0) both clean, `next build` confirms `/` now compiles as a real static route alongside every other existing route, no regressions. `T12` checked off as Green, Gate 2 drafted, awaiting a named reviewer's verdict.
- **Gate 2 verdict recorded: `stakeholder-panel-ui.T12` Merged (Test Reviewer, 2026-09-20).** No changes requested. **All 12 tasks in `stakeholder-panel-ui` are now Merged — spec Status returned `In Development` → `In QA`**, verified directly against the spec file. The bare site URL now redirects every visitor correctly: to Login with no/expired session, or straight to their role's home page with a valid one.

### 2026-09-21
- **User reported a real `500` error on `POST /admin/self-register`.** Investigated directly rather than guessing: reproduced it against a live dev server, and the real server log showed `MongooseError: Operation \`users.insertOne()\` buffering timed out after 10000ms`. Root cause, confirmed by reading the code: `src/services/db/connect.ts`'s `connectToDatabase()` — already correct, already unit-tested — was never called anywhere in the entire codebase. **Every one of this project's Jest tests passes regardless of this bug**, because `src/test-utils/mongoServer.ts` connects Mongoose directly to a real in-memory `mongodb-memory-server` instance for the whole test run, completely bypassing `connect.ts`. This means every previously-Merged task in the project's history shipped against a real app with zero database connection — a severity-significant, previously-undetected gap, distinct in kind from the earlier stub-lookup bugs (`rbac-api-security.T07`, `transfer-audit-trail.T04`) since it blocks *every* DB-touching endpoint, not two specific ones.
- **User asked me to create the missing environment file first.** Grepped the whole codebase for every `process.env.*` reference (only two: `MONGODB_URI`, `JWT_SECRET`) and created `.env.local` (already covered by `.gitignore`'s `.env*` rule) with a default local `MONGODB_URI` and a freshly-generated random `JWT_SECRET`. Explicitly flagged to the user that this alone would not fix the 500 — no MongoDB was even running locally at that point, and `connectToDatabase()` was still never called by anything regardless of the env file's presence.
- **User then reported MongoDB is running via Docker.** Confirmed a real MongoDB was reachable on `localhost:27017` (`docker ps` showed a running `mongo:latest` container). Proceeded to fix the actual wiring gap: added `rbac-api-security.T08` (cited representatively against the spec's own "MongoDB is the sole approved datastore" Non-Functional Constraint, since this is infrastructure plumbing with no dedicated AC of its own) — chosen as the owning spec since it's the foundational spec every other spec's data access depends on, and its sibling env var (`JWT_SECRET`) is already owned there. Reopened spec Status `In QA` → `In Development`.
- **`T08` Green.** New `src/instrumentation.ts` exporting `register()`, calling `connectToDatabase()` once at server startup — the standard Next.js pattern, chosen over sprinkling a connect call into every route file. New `src/instrumentation.test.ts` (2 tests: calls `connectToDatabase` once; propagates a connection failure rather than swallowing it — fail-fast, matching `connect.ts`'s own existing behavior). Confirmed Red first (`Cannot find module '@/instrumentation'`), then Green. 629/629 tests project-wide, `tsc`/`eslint` both clean, `next build` confirms no regressions. **Live-verified against the user's real Docker MongoDB, not just unit-tested:** killed and restarted the dev server fresh, called the exact endpoint that originally failed — `POST /admin/self-register` returned `201` in ~340ms (previously hung ~10s then `500`'d) — then called it again, correctly got `409 admin_already_exists`, proving the single-Admin unique-index invariant holds against the real database too. `T08` checked off as Green, Gate 2 drafted (explicitly flagging the project-wide severity of what this closes, not just treating it as a routine fix), awaiting a named reviewer's verdict.
- **User asked why the browser showed no styling on the Admin Dashboard.** Confirmed directly rather than assuming a build/CSS bug: fetched the real page's HTML and verified a stylesheet was correctly compiled and linked, then read `Dashboard.tsx`'s own source and found zero `className` usage anywhere. Extended the check project-wide: grepped every component in both `admin-panel-ui` and `stakeholder-panel-ui` for `className=` and found **all 18 had zero matches** — every screen in the project was built to satisfy purely functional ACs, with no visual/design requirement ever stated anywhere. User confirmed this should become a proper scoped set of tasks.
- **Presented a recommendation before implementing, per this session's standing "recommend, then confirm" practice for design questions, and got two decisions:** (1) one task per spec, not one per screen — chosen to keep styling consistent and avoid ~13 small Gate-2 cycles for changes with no new testable behavior; (2) include real navigation links alongside the styling, since neither panel had ever had any (only a Logout button) — found while scoping, not something the user had originally asked about, flagged before folding it in.
- **Added `admin-panel-ui.T09`** (new `AC12` — nav links, testable; visual styling cited representatively against `int-standards.nextjs.md`'s already-approved Tailwind + shadcn/ui stack) and **`stakeholder-panel-ui.T13`** (new `AC13`, same pattern). Both specs reopened `In QA` → `In Development`.
- **`admin-panel-ui.T09` Green.** `AdminPanelLayout` gained a nav bar (all 5 screens, via `next/link`) — Red confirmed first (1 new test failed, no nav existed), then Green. Restyled all 6 screens with Tailwind + shadcn/ui `Table`: `Dashboard`, `MonitoringList`, `TransferRequestDetail` (plain HTML → styled), `UserManagement` (also upgraded from raw HTML form/table to shadcn `Label`/`Input`/`Button`/`Table`, matching `DepartmentManagement`/`JobRoleManagement`'s existing pattern), `DepartmentManagement`/`JobRoleManagement` (spacing only, already used shadcn primitives). **Self-caught mistake, recorded not silently fixed:** an early draft of `Dashboard.tsx`'s restyle split `"{role}: {count}"` into two separate `<span>` elements, which would have broken an existing test's `getAllByText(/:\s*0/)` regex (depends on both parts sharing one text node) — caught by reading the test file before assuming the restyle was safe, reverted to one styled text node. Every existing test across all 7 components re-run unmodified and confirmed still passing. 631/631 tests project-wide, `tsc`/`eslint` both clean, `next build` confirms no regressions. `T09` checked off as Green, Gate 2 drafted, awaiting a named reviewer's verdict.
- **`stakeholder-panel-ui.T13` Green.** `StakeholderPanelLayout` gained a nav bar — "My Requests" for every role, "Submit Request" for Employee only (the layout now tracks the session's `role`, not just an `authorized` boolean, to make that decision) — Red confirmed first (6 new tests failed), then Green. Restyled all 11 components (`LoginForm`, `MyRequestsList`, `SubmitRequestForm`, `RequestDetail`, and the 6 embedded role panels) — all of them already used shadcn/ui `Button`/`Input`/`Label`, so this was layout/spacing only, reusing the exact same conventions `admin-panel-ui.T09` established (page container, heading sizes, field-wrapper spacing, error-text styling) for a consistent look across both panels rather than a second, drifting style. Every existing test across all 11 components re-run unmodified and confirmed still passing. 637/637 tests project-wide, `tsc`/`eslint` (project-wide, exit code 0) both clean, `next build` confirms no regressions. `T13` checked off as Green, Gate 2 drafted, awaiting a named reviewer's verdict. **This is the last task in `stakeholder-panel-ui`** — once both `T09` and `T13` Merge, the visual-polish pass across both UI specs is complete.
- **Gate 2 verdicts recorded: `admin-panel-ui.T09` and `stakeholder-panel-ui.T13` both Merged (Test Reviewer, 2026-09-21).** No changes requested on either. All 9 tasks in `admin-panel-ui` and all 13 tasks in `stakeholder-panel-ui` are now Merged — both specs' Status returned to `In QA`, verified directly against each spec file. The visual-polish + navigation pass across both UI panels is complete: every screen now has consistent Tailwind/shadcn-ui styling and a working nav bar, and the bare site URL, Login, and every panel screen are all reachable from one another without typing a URL directly.
- **User asked for the Create User form's Manager field (shown when `role: "Employee"`) to be a list of available Managers instead of a free-text ID input.** Added `admin-panel-ui.T10`, citing `AC5` directly (no new AC needed — a usable create-user action implies a usable way to supply `managerId`). Reopened spec Status `In QA` → `In Development`.
- **`T10` Green.** `UserManagement.tsx` already fetches every user (unfiltered, as Admin) via its own existing `useUsers()` — no new endpoint/service needed, just filtering that already-in-memory list to `role === "Manager"` and rendering it as a `<select>`, options labeled by `username` (the only identifying field `UserListItem` has) with the Manager's `id` as the value. Updated the one pre-existing test that exercised the old free-text field (`user.type(...)` → `user.selectOptions(...)`, against a mocked list now including a Manager, not left silently passing against the old widget) and added a new test confirming non-Manager users are excluded from the options. The pre-existing "blocks submission when managerId is left empty" test (empty user list) continues to pass unmodified — an empty selector still correctly blocks submission via the same Yup rule. Both new/changed tests confirmed Red for the right reason first, then passed cleanly on the first Green attempt. 638/638 tests project-wide, `tsc`/`eslint` (project-wide, exit code 0) both clean, `next build` confirms no regressions. `T10` checked off as Green, Gate 2 drafted, awaiting a named reviewer's verdict.
- **Gate 2 verdicts recorded: `admin-panel-ui.T10` and `rbac-api-security.T08` both Merged (Test Reviewer, 2026-09-21).** No changes requested on either. All 10 tasks in `admin-panel-ui` and all 8 tasks in `rbac-api-security` are now Merged — both specs' Status returned to `In QA`, verified directly against each spec file. `rbac-api-security.T08` had been left outstanding since earlier in the session (the real MongoDB connection fix); catching that it was still unresolved, rather than assuming only the most recently-discussed task was meant, avoided silently leaving a real, previously-flagged production-breaking fix unmerged.
